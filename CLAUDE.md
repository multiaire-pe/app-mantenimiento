# Contexto del proyecto — app-mantenimiento (MultiAire Perú)

## Qué es
App web interna de MultiAire Perú para gestión de asistencia del personal técnico.
Vanilla HTML/JS, sin framework. Firebase Firestore como base de datos (SDK compat 9.23.0).
No hay servidor — todo corre en el browser con Firebase Auth + Firestore directo.

## Archivos principales
- `asistencia_multiaire.html` — gestión de asistencia del personal
- `comprobantes.html` — Rendición de Caja: escáner de facturas/boletas con Gemini IA
- `proveedores.html` — Proveedores: directorio CRUD (colección `proveedores`)
- `firebase-config.js` — config pública de Firebase (project: `multiaire-fee43`)
- `wsp_import/importar_asistencia.py` — script Python para importar asistencia desde ZIP de WhatsApp

## Rendición de Caja (`comprobantes.html`)
> Antes llamada "ComprobaScan" — renombrada 2026-05-27
> **2026-07-08: pasó a tener persistencia real** (antes era captura sin guardado) — ver bloque de persistencia más abajo.
- **IA**: Google Gemini REST API directo desde el browser
- **Auth API**: header `x-goog-api-key: <key>` (NO query param `?key=`)
- **Endpoint**: `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`
- **Modelo default**: `gemini-2.5-flash-lite` (1000 RPD free) | Alternativa: `gemini-2.5-flash` (250 RPD free, más capaz)
- **Modelos retirados**: `gemini-2.0-flash` (deprecated, shutdown jun-2026), `gemini-1.5-flash-latest`, `gemini-1.5-pro-latest` (discontinuados)
- **Selector de modelo**: pills custom (NO `<select>` — el CSS global `appearance:none` lo rompe)
- **File input**: `<label for="file-input">` (NO `.click()` JS — Brave lo bloquea)
- **Imágenes**: comprimidas a máx 1600px JPEG 0.85 antes de enviar
- **API key**: en `localStorage` clave `cs_gemini_key` — persiste entre sesiones; botón 🗑 Borrar en key-status
- **Modelo guardado**: en `localStorage` clave `cs_gemini_model` — validar contra lista VALID_IDS al cargar
- **Columnas Excel**: FECHA, N°BOLETA / FACTURA, NUMERO, RUC, PROVEEDOR, DESCRIPCION (manual, obligatoria), VALOR
- **Tipos de comprobante**: `FT`=Factura, `BO`=Boleta, `TK`=Ticket, `NC`=Nota de Crédito, `OT`=Otro
- **Gemini output**: `responseMimeType:'application/json'` + `responseSchema` con enum de tipos — fuerza JSON válido siempre
- **Prompt**: describe cada campo con ejemplos reales (NO template JSON con valores placeholder)
- **Panel Gestión de accesos**: disponible para ADMIN/SUPER_ADMIN — idéntico al de otras apps
- **Footer**: body `display:flex flex-direction:column` + wrapper `flex:1` siempre visible — footer nunca salta al header durante carga
- **← Inicio**: usa clase `.back-link` (pill semi-transparente) igual que mantenimiento/asistencia
- **Persistencia (2026-07-08)**: cada fila se guarda en Firestore **apenas Gemini responde** (colección `comprobantes`; la foto va aparte en `comprobantes_fotos`, doc id = id del comprobante — mismo patrón que `insumos_catalogo_fotos`); editar un campo en la tabla actualiza el doc al salir del campo (`onblur`, no por tecla); el ✕ de una fila borra el doc + su foto de Firestore (corregir un escaneo mal leído es operación normal, no admin — `firestore.rules`: `canWrite('comprobantes')`, no solo super). `fechaISO` (derivada de la `fecha` DD/MM/YYYY) es la que se usa para filtrar por rango, porque un string DD/MM/YYYY no ordena igual que la fecha real.
- **Dos tabs**: 🧾 **Escanear** (el flujo de siempre: subir/analizar/tabla de la sesión, con "＋ Agregar fila" y "🧹 Limpiar tabla" — este último solo limpia la vista, NO borra Firestore) y 🕘 **Historial** (rango de fechas Desde/Hasta sobre `fechaISO`, default 1º del mes actual→hoy; checkboxes + "seleccionar todo"; ahí viven los exports).
- **Columna Imagen** en ambas tablas: miniatura + lightbox (mismo patrón que `openInstFotoLightbox` de `insumos.html`).
- **Exports (solo en Historial)**: Excel-texto, Excel+fotos (Excel + un PDF aparte solo de fotos, 1 por página), PDF-texto (tabla dibujada en canvas→jsPDF, mismo patrón que el reporte de material de `insumos.html`) y PDF+fotos (mismo PDF de la tabla + páginas de fotos al final). Sin selección marcada, exporta todo el rango visible. Requirió agregar `jspdf.umd.min.js` (antes solo tenía `pdf.js` para *leer* PDFs, no para generarlos).
- **Estado en develop**: activa | **Estado en producción**: activa

## Proveedores (`proveedores.html`)
Directorio CRUD de proveedores. Construida sobre el scaffold de `comprobantes.html` (header/auth Google contra `usuarios`, panel "Gestión de accesos", footer, toast) — **sin** nada de Gemini/scanner/pdf.js.
- **Colección**: `proveedores` (ver tabla de colecciones). Doc id auto-generado.
- **Roles**: ADMIN/SUPER_ADMIN editan (botón "＋ Nuevo proveedor", lápiz ✎, eliminar); SUPERVISOR solo lectura (sin botón nuevo; el ojo 👁 abre el modal en modo readonly — inputs `disabled`, sin botón Guardar/Eliminar). Gate central: `isProvAdmin()`.
- **UI**: toolbar (búsqueda en vivo + Exportar Excel + Nuevo proveedor) + tabla (estado·RUC·razón social·nombre comercial·rubro·teléfono·contacto·cond. pago·**notas**·acción). Doble clic en fila abre el detalle. Modal de alta/edición con todos los campos (`PROV_FIELDS`); `notas` es un `<textarea>` y en la tabla se muestra truncado con `title` completo.
- **Notas**: texto libre para observaciones internas (horarios de atención, vendedor asignado, descuentos acordados, plazos de entrega, incidencias, web/catálogo, etc.).
- **Búsqueda** (`provFilter`): filtra por ruc, razón social, nombre comercial, rubro, contacto, teléfono, email, notas.
- **Validación**: RUC obligatorio y `^\d{8,11}$` (no valida dígito verificador todavía); razón social obligatoria (se guarda en MAYÚSCULAS); RUC duplicado bloqueado al crear.
- **Excel** (`exportProveedores`, SheetJS): RUC, RAZÓN SOCIAL, NOMBRE COMERCIAL, RUBRO, TELÉFONO, EMAIL, CONTACTO, DIRECCIÓN, CONDICIONES DE PAGO, BANCO, CUENTA/CCI, ACTIVO.
- **Backup**: incluida en export/import de `configuracion.html` (`proveedores.csv`).
- **Estado en develop**: activa | **Estado en producción**: activa (merge a main 2026-06-10).

### Historial de compras (2026-07-03, provisional)
La tabla principal reemplazó la columna **Notas** por **Acciones** (2 botones por fila: 👁 Vista rápida, 📋 Detalles — el 🗑 Eliminar se sacó de la fila; sigue disponible en el modal de edición y en el panel Detalles, ambos gateados a `MA.can('borrar')`). Doble clic en una fila también abre **Detalles**.
- **Desacoplado a propósito**: toda la lógica de compras vive en `compras.js` (`window.COMPRAS`), un módulo sin UI que recibe `db` como parámetro (no lee globals de `proveedores.html`), igual que `auth-guard.js`. La idea es poder reutilizarlo tal cual si en el futuro existe un módulo Compras independiente — **por evaluar, no decidido**. `proveedores.html` es hoy la única UI que lo consume.
- **Colección `compras`** (top-level, NO anidada en `proveedores`: el historial no es un campo del proveedor). Ver esquema en la tabla de colecciones.
- **👁 Vista rápida**: popover flotante (`#qv-pop`, `openQuickView`) con 4 indicadores: productos diferentes, compras realizadas, última compra, monto total comprado.
- **📋 Detalles** (`modal-prov-detalle`, `openProvDetalle`): información general de solo lectura (reemplaza la vista de "ver" que antes vivía en el modal de edición — `openEditProv` ahora solo se abre para quien puede editar, vía el botón **✎ Editar** de este panel), los mismos 4 indicadores como stat tiles, tabla de **productos más comprados** (top 5 por frecuencia, `COMPRAS.topProductos`; columnas producto/servicio, último precio, **precio mínimo** y última compra), botón **🕘 Ver historial completo** y botón **➕ Registrar compra** (estos dos últimos y ✎ Editar solo si `isProvAdmin()`).
- **🕘 Historial completo** (`modal-compras-historial`, `openHistorial`): lista todas las compras del proveedor, cada una expandible (acordeón) mostrando sus ítems.
- **➕ Registrar compra** (`modal-registrar-compra`): proveedor precargado; ítems dinámicos (＋/🗑 por fila) con **Tipo** (Artículo del catálogo — buscador contra `insumos_catalogo` vía `COMPRAS.catalogo()`, obliga a seleccionar una coincidencia real — o Servicio — descripción libre), cantidad, precio unitario y subtotal calculado; fecha y N° de comprobante (opcional). Al guardar llama `COMPRAS.crear()`.
- **Relación con Inventario (preparación, no activa)**: los ítems tipo ARTICULO guardan `catalogoId` (referencia a `insumos_catalogo`); los SERVICIO siempre lo llevan `null`. No se modifica `insumos.html` ni se crean instancias/movimientos desde una compra — queda solo la referencia para una futura integración.
- **Eliminar un proveedor borra en cascada sus compras** (`COMPRAS.eliminarPorProveedor`, troceado en bloques de 450) — evita historial huérfano en Firestore; el confirm avisa cuántas compras se van a borrar junto con el proveedor.
- **Fuera de alcance de esta primera versión**: no hay edición/eliminación de compras individuales ya registradas (solo alta + consulta, y el borrado en cascada al eliminar el proveedor); no hay validación de dígito verificador de RUC; un proveedor sigue teniendo un solo campo `contacto` libre (no lista de contactos). **Conocido (no arreglado):** restaurar un backup anterior a esta función en modo "Reemplazar todo" no limpia `compras` si el ZIP no trae `compras.csv` — comportamiento preexistente del sistema de backup para cualquier colección nueva, no exclusivo de `compras`.
- **Backup**: colección `compras` incluida en export/import de `configuracion.html` (`compras.csv`, doc completo en JSON por fila — mismo criterio que `insumos_movimientos`, por el array `items[]` de esquema variable).
- **Estado en develop**: activa (código + `firestore.rules` desplegado) | **Estado en producción**: no (pendiente de PR + merge a main).

## Insumos (`insumos.html`)
Gestión de inventario de herramientas/insumos. **Estado**: activa en develop y producción (activada 2026-06-06).
Modelo de 3 niveles:
- **Catálogo** (`insumos_catalogo`) — el *tipo* de ítem. Código único obligatorio. id = código.
- **Instancia** (`insumos_instancias`) — cada *unidad física* del tipo, con su propio id/código, sede, estado y opcional `paqueteId`. Estados: `DISPONIBLE`, `EN_USO`, `MANTENIMIENTO`, `DETERIORADO`, `BAJA`.
- **Foto de referencia del catálogo + herencia (2026-07-04)**: cada ítem de catálogo admite una foto de referencia opcional (`insumos_catalogo_fotos`, doc id = id del catálogo). Una instancia sin foto propia **hereda en pantalla** la del catálogo (fallback dinámico, sin copiar nada al crearse) vía `fotoDeInstancia(inst)` = `instFotos[inst.id] || catFotos[inst.itemId]`; si corriges la foto de referencia después, todas las instancias sin foto propia la reflejan al instante. Subir una foto a una instancia puntual la independiza (queda en `insumos_instancias_fotos` como siempre); quitarle su foto propia la devuelve a heredar la del catálogo, sin botón aparte. `instThumb`, el modal de instancia (`refreshInstFotoPreview`, con etiqueta "📎 Del catálogo" cuando es heredada), el lightbox y los exports de material (CSV "FOTO SÍ/NO" y el reporte PDF/PNG) pasan por ese mismo helper. La foto de referencia solo es editable con el ítem ya guardado (modal "Nuevo ítem" la oculta); se borra en cascada al eliminar el ítem de catálogo.
- **Paquete** (`insumos_paquetes`) — contenedor que agrupa instancias. Relación **bidireccional**: `paquete.instancias[]` ↔ `instancia.paqueteId`. Tipos: MOCHILA, CAJA, CAJON, **ANAQUEL**, MALETÍN, OTRO.
- Pestañas: Catálogo · Instancias · Movimientos · Por Sede · Paquetes · Por Persona (antes "Por Técnico"; ids internos `tecnicos`/`tab-tecnicos` sin cambiar).
- **Exportar material**: por persona en el tab *Por Persona* (botón "⬇ Exportar material" al seleccionar una persona → sus instancias sueltas + todas las de sus paquetes) y por paquete en el tab *Paquetes* y en las tarjetas de paquete del detalle de persona (botón "⬇ Material"/"⬇" → instancias de ese paquete). El botón abre un **selector de formato** (modal `modal-export-mat`): **PDF** (principal, recomendado), **PNG** (segunda), **Excel/CSV** (última opción), + checkbox **"Incluir foto de cada instancia"** (solo PDF/PNG). `exportMaterialPersona`/`exportMaterialPaquete` arman el contexto `_exportMat` y abren el selector; `doExportMat(fmt)` lee el checkbox (`_exportMat.conFotos`) y despacha. PDF/PNG comparten un reporte dibujado en canvas (**`_matReportCanvas` es async** — precarga las fotos base64 con `_loadImg` antes de dibujar; columnas CÓDIGO/ÍTEM/CATEGORÍA/SEDE/ESTADO[/PAQUETE], y una columna **FOTO** con miniatura recortada cuadrada cuando `conFotos`, placeholder "s/f" sin foto); el PDF usa **jsPDF** (UMD CDN, `window.jspdf.jsPDF`, addImage paginado A4); el CSV (UTF-8 BOM, columnas completas + RESPONSABLE + **FOTO SÍ/NO**) reutiliza `_descargarMaterial`. Las fotos salen de `instFotos` (colección `insumos_instancias_fotos`).
- **Etiquetas**: cada ítem/instancia genera etiqueta descargable (PNG individual o ZIP). Formato seleccionable **QR** (qrcodejs) o **código de barras Code128** (JsBarcode) vía selector `setLabelFmt()`/`labelFmt`; ambos codifican el `id`. Generadores: `generateQRCanvas`/`generateInstQRCanvas`/`generateBarcodeCanvas`, despachados por `genLabelCanvas()`.
- **Etiqueta de barras = media hoja**: `generateBarcodeCanvas` produce una etiqueta de proporción **400×150 mm** (8:3, mitad de un papel 400×300). El código de barras va **alargado** y llena la etiqueta (estirado a ~94% del ancho × ~66% del alto, márgenes mínimos; `imageSmoothingEnabled=false` para bordes nítidos) y el código en texto chiquito debajo, **sin nombre**. Se descarga individual o en ZIP; el usuario acomoda 2 por hoja al imprimir.
- **Ubicación física en almacén** = paquete tipo ANAQUEL (no hay campos `anaquel`/`sitio` en instancia; se modela como contenedor).
- **Salida / Baja en lote** (tab *Instancias*): la barra de selección múltiple (`selectedInstQRIds`) suma botones "⬆ Salida en lote" / "⚠️ Baja en lote" (gating `can('salida')===1`, se muestran/ocultan en `updateInstQRSelectionBar`). `openModalSalidaLote(tipo)` abre `modal-salida-lote` con la lista de instancias seleccionadas + responsable + motivo (obligatorio si BAJA) + **N° de guía de remisión** y **foto de guía** + **foto de entrega**, todo opcional. `saveSalidaLote()` crea **UN** movimiento `SALIDA`/`BAJA` con `instancias[]` (igual que la salida individual, que sigue existiendo) + `instanciasSnapshot[]` (copia de cada instancia borrada para poder revertir fielmente lotes mixtos en ítem/sede) + `guiaNumero`/`tieneFotos`; borra las instancias por batch (troceado a 450) y guarda las fotos en `insumos_movimientos_fotos` (doc id = id del movimiento). `renderMovimientos` muestra "📎 Guía Nº" + miniaturas clicables (lightbox vía `openMovFoto`). La reversión (`revertirMovimiento`) y la cascada (`eliminarMovsCascada`) manejan `instancias[]` + snapshot y borran la foto del movimiento. **N° de guía + foto de guía + foto de entrega (2026-07-04) también disponibles en Entrada, Salida y Transferencia individuales** (`modal-entrada`/`modal-salida`/`modal-transf`), reutilizando la misma `insumos_movimientos_fotos` y las mismas `loteFotos`/`compressFit` (prefijos `fent`/`fsal`/`ft`).
- **Transferencia en lote** (tab *Instancias*): botón "🔄 Transferir en lote" en la misma barra (gating propio `can('transferencia')===1`). `openModalTransfLote()` abre `modal-transf-lote` (checkboxes Sede/Responsable como en la individual, destino, "realizado por", observación + guía/fotos opcionales). `saveTransfLote()` crea UN movimiento `TRANSFERENCIA` con `instancias[]` + `instanciasSnapshot[]` (valores **previos** de los campos transferidos, no objeto completo) + `camposTransferidos` (`['sede','responsable']`); aplica el destino a cada instancia y la saca de su paquete si pertenece a uno. La reversión restaura sede/responsable previos por instancia (igual que la individual, **no** re-inserta en el paquete previo) y borra la foto. La infraestructura de fotos del lote es compartida (`loteFotos`, `setLoteFoto`, `refreshLoteFotoPreview` con `loteFotoPrefix` = `flote`/`ftl` según el modal activo; `compressFit`).
- **Carga masiva (Excel/`.xlsx`, SheetJS)**: botón "📊 Carga masiva" en el sidebar (gate `can('crearItem')`, ADMIN/SUPER_ADMIN). Modal `modal-carga-masiva` con 3 acciones: **descargar plantilla** (`descargarPlantillaInsumos`), **exportar base actual** (`exportarBaseInsumos`) e **importar** (`onCargaMasivaFile` → preview → `confirmarImportInsumos`). El archivo tiene 3 hojas: **Catalogo** (`CM_CAT_COLS`, **sin columna ID** — la llave es `CODIGO`), **Instancias** (`CM_INST_COLS`; la 1ª columna `ID` es interna del sistema y va **oculta** en el Excel) y **Valores validos** (categorías/unidades/tipos/estados/sedes + lista de ítems con CODIGO/NOMBRE como referencia para `ITEM_ID`). **Upsert**: catálogo por `CODIGO` (los nuevos autogeneran su docId, el usuario nunca toca el ID); instancias por su `ID` oculto (las nuevas lo dejan vacío → se autogenera con prefijo del código + correlativo, como `saveEntrada`; gracias al ID oculto, re-importar un export NO duplica). `ITEM_ID` = el CODIGO del ítem. `_cmValidar` valida fila a fila y devuelve `{nuevos, upd, err}` por colección; `_cmRenderPreview` muestra "＋nuevos · ↻actualizar · ⚠error" antes de escribir. Escritura por batch (450), `origen:'IMPORT_XLSX'`. **Plantilla guiada**: el `.xlsx` incluye una primera hoja **"Instrucciones"** (`_cmInstruccionesSheet`, generada desde `CM_GUIA_CAT`/`CM_GUIA_INST`/`CM_REGLAS`) que explica cada columna (obligatoria/opcional + valores) con ejemplos; las hojas de datos van **vacías** (solo encabezados + desplegables). Botón **"📖 Manual (PDF)"** (`descargarManualCargaMasiva`, jsPDF) genera una guía paso a paso descargable reutilizando las mismas constantes de guía. **Menús desplegables en el Excel**: el `.xlsx` trae *data validation* (dropdowns) en CATEGORIA/UNIDAD/TIPO_CANTIDAD/ESTADO (listas inline) y SEDE/ITEM_ID/RESPONSABLE (referencian una hoja oculta `_listas`). Como SheetJS Community **no escribe** validación de datos, se inyecta el XML `<dataValidations>` post-proceso: `_cmXlsxBlob` escribe el wb con `XLSX.write({type:'array'})`, lo abre con **JSZip**, ubica el worksheet de cada hoja vía `_cmSheetFile` (parsea `workbook.xml`+rels, robusto al orden) e inserta el bloque tras `</sheetData>` (queda antes de `ignoredErrors`/`pageMargins` → orden OOXML válido). Las listas cerradas bloquean valores inválidos; ITEM_ID/RESPONSABLE solo sugieren. `descargarPlantillaInsumos`/`exportarBaseInsumos` son **async** y descargan el Blob resultante. **Decisiones**: carga **silenciosa** (NO genera movimientos en el historial, igual que la carga inicial); el **nombre de instancia siempre se deriva del catálogo** (mantiene la denormalización + propaga a hijas como la Etapa 1); la importación **NO toca `paqueteId`** (merge preserva la pertenencia; los paquetes se gestionan en su pestaña). SheetJS vía CDN (`xlsx.full.min.js 0.18.5`). No requiere cambios de backup (usa colecciones ya respaldadas; `ORIGEN` ya es columna del CSV).

### Carga inicial de inventario (2026-06-06)
- Origen: Excel "CONTROL DE INVENTARIO DE HERRAMIENTAS POR TECNICO" → Hoja2 (CONTROL INVENTARIO).
- "ALMACÉN CENTRAL" del Excel = sede **OFICINA** (Chinchón Oficina, `TIE013`).
- Importadas 46 tipos de catálogo + 52 instancias + 1 paquete-anaquel (`ANAQUEL 02`). El plano físico (PLANO 0X) NO es contenedor; quedó reflejado en `notas`. **El campo `plano` fue eliminado de Firestore y de la app el 2026-06-18** (no se mostraba en ninguna pantalla; ver changelog).
- Mapeo estado: BUENO→DISPONIBLE, MALO/INCOMPLETO→DETERIORADO, vacío→DISPONIBLE. Docs marcados con `origen:'IMPORT_HOJA2'`.
- Script de importación: `~/Documents/migrar_db/import_hoja2.js` (firebase-admin + serviceAccount).
- Las otras hojas del Excel (HERR. ROT., CONTROL EQUIPOS, HERR FIJAS por técnico) **NO se importan** — fuera de alcance. La carga del Excel queda completa con el almacén central (Hoja2).
- **Backup**: las 4 colecciones `insumos_*` se exportan/importan en `configuracion.html` (catálogo/instancias en columnas; paquetes con `instancias[]` unidas por `|`; movimientos como doc JSON por su esquema variable). El `parseCSV` se reescribió como parser correcto (maneja `""` y saltos de línea citados) para soportar las celdas JSON.
- **CSV Phomemo**: `exportInstCSVPhomemo()` exporta un CSV (`CODIGO,NOMBRE`, con BOM UTF-8) de las instancias seleccionadas (o todas) para impresión por lotes en impresoras térmicas Phomemo (etiquetas 40×30 mm) vía Print Master/Labelife, donde el barcode lo genera la app desde la columna `CODIGO`. Botón "📄 CSV (Phomemo)" en la barra de selección de Instancias.
- ~~Opcional pendiente: recategorizar los 4 ítems sin categoría (manómetros) que el Excel dejó en blanco.~~ Hecho 2026-06-08: `GEN-01` "MANOMETRO" (4 instancias) → categoría **MEDICION**. Script `~/Documents/migrar_db/recat_manometros.js`.

## Observaciones (`observaciones.html`)
"Manta de Observaciones" — sábana de observaciones de mantenimiento sobre el inventario real (`inventario`, 557 equipos · 12 sedes · 14 tipos). **MULTI-CLIENTE** (proyectado a crecimiento: hoy RIPLEY, el próximo año TOTTUS, etc.): selector Cliente → Sede → Tipo → Equipo (con un solo cliente, el campo Cliente se autoselecciona/oculta). Reemplaza el Excel manual `MANTA GENERAL`. Construida sobre el scaffold de `proveedores.html` (auth Google/`usuarios`, panel de accesos, footer/toast, responsive). **Estado**: activa en develop y **producción** (merge a main 2026-06-20). **Bot WhatsApp (Fases 1-5)**: EN VIVO en el número real (+51 972 416 669), Gemini paid tier (`gemini-2.5-flash`). Backend en **Vercel Functions**, no Firebase, para no requerir Blaze.
- **Modelo = log**: cada observación es un documento de `manta_observaciones` (un equipo puede tener varias). El default "Sin observaciones." es relleno de exportación para equipos sin hallazgos (NO se guarda como dato).
- **Maestro de equipos = `inventario`** (557 equipos · 12 sedes · 14 tipos — la app inventario_multiaire.html), NO `manta_equipos` (deprecado, era solo 56 Roof Tops Ripley). Alimenta los **desplegables dependientes sede→tipo→equipo** (`fillSedeSelect`/`fillTipoSelect`/`fillEquipoSelect`/`onObsSedeChange`/`onObsTipoChange`; el `<option>` de equipo lleva el `eqId` como value) y el export. Al guardar, la observación referencia el `eqId` + denormaliza `sede`/`equipo`(nombre)/`tipo` + `tienda`=`cliente+sede`.
- **Roles**: `isObsAdmin()` (ADMIN/SUPER_ADMIN editan; SUPERVISOR solo lectura, modal readonly).
- **Toolbar**: búsqueda viva (`obsFilter`) + filtros tienda/estado/fecha (`obs-f-*`) + Exportar **Excel** (`exportObsExcel`, sábana completa con relleno) + **PDF** (`exportObsPDF`, jsPDF agrupado por tienda/equipo) + "Nueva observación". Tabla: estado(dot) · tienda · equipo · observación · estado(badge) · fecha · foto(thumb→lightbox) · origen · acción. Doble clic = detalle.
- **Fotos**: `manta_observaciones_fotos` (base64 aparte). Subida con `setObsFoto`/`obsCompress` (proporción, 1100px/0.7), preview + lightbox; estado del modal en `_obsFotoVal`/`_obsFotoDirty`.
- **Card** en `index.html` (entre Proveedores y Configuración). `observaciones` añadida a `ALL_APPS` de `proveedores.html` y `comprobantes.html` (los `ALL_APPS` del resto de apps están desactualizados, estado preexistente). Backup en `configuracion.html` (las 4 colecciones `manta_*`, incluida `manta_guia`).
- **Editor de la guía del bot** (`manta_guia`): menú de usuario → **🤖 Guía del bot** (solo ADMIN/SUPER_ADMIN). Modal lista de temas + modal alta/edición (título, palabras clave, checklist una línea por punto, orden, activo). Escribe en `manta_guia` (doc id `MGUIA-<slug título>` para nuevos; conserva id/`tipo` al editar). `guiaList`, `openGuiaPanel`/`renderGuiaList`/`openGuiaEdit`/`saveGuia`/`deleteGuia`.
- **Nota de esquema**: se usa `equipo` (etiqueta del maestro) en vez de `equipoId`/`equipoCodigo` del plan original, porque los equipos son etiquetas dentro de una tienda. Nombre de tienda = "RIPLEY <SEDE>".

### Bot de WhatsApp (Parte B) — backend serverless en `api/`
Alimenta `manta_observaciones` desde WhatsApp. **No** es Firebase Functions: son **Vercel Functions** en el mismo repo (`api/whatsapp.js` + módulos en `api/_lib/`, ESM, `package.json` con `type:module`). El prefijo `_lib` evita que Vercel los trate como endpoints. Secretos solo en env vars de Vercel (ver `api/README.md`), NUNCA en el front.
- **Fase 1** — webhook GET (verificación Meta) + POST (firma `X-Hub-Signature-256`, body crudo, `timingSafeEqual`).
- **Fase 2** — identidad por `maestros_personal.telefono` (últimos 9 dígitos) + idempotencia (`wa_mensajes`).
- **Fase 3** — `gemini.js` estructura el mensaje en `{tienda,equipo,observacion,estado}` (`gemini-2.5-flash` + `responseSchema`); `manta.js` empareja tienda/equipo contra `manta_equipos`.
- **Fase 4** — **motor conversacional** (`conversacion.js`): máquina de estados sobre `wa_sesiones` (RECOLECTANDO→CONFIRMANDO, TTL 30 min). Repregunta lo mínimo (tienda/equipo no resueltos, o **un** detalle sugerido por la guía `manta_guia` vía `guia.js`); siempre permite "guardar así"; **confirma antes de escribir** y el técnico fija/corrige el estado; al confirmar, `escritura.js` crea la observación en `manta_observaciones` (origen WHATSAPP). `analizar`/`guardar` se inyectan en `manejarMensaje` → testeable sin Meta ni Gemini. Probado contra Firestore real (`migrar_db/test_fase4.mjs`).
- **Fase 5** — **foto desde WhatsApp** (`media.js` descarga vía Graph API; `fotos.js` guarda la foto pendiente en `wa_sesiones_fotos` hasta el "SÍ"; al confirmar, `escritura.js` la escribe en `manta_observaciones_fotos` como dataURL + `tieneFoto:true`, igual que la app) + **aviso a supervisores** (`avisos.js`: destinatarios = `maestros_personal` con `recibeAvisos` + teléfono; `enviarPlantilla` para plantilla "utility" de Meta vía `WHATSAPP_TEMPLATE_AVISO`, o texto libre si no hay plantilla; no avisa a quien reportó; el aviso no rompe el flujo si falla). `guardar` del handler = escribir + notificar. Probado contra Firestore real (`migrar_db/test_fase5.mjs`, 17/17). **Falta para producción:** deploy con env vars + setup Meta (número + plantilla aprobada) + prueba end-to-end.

## Firestore — colecciones
| Colección | Descripción |
|---|---|
| `maestros_personal` | Colaboradores (id, nombre, cargo, telefono, activo, foto) — `foto` = imagen base64 (dataURL JPEG ~320×320) guardada en el propio documento. **No usa Firebase Storage** (el plan Spark ya no lo incluye; Storage exigiría Blaze). Campo opcional **`recibeAvisos`** (bool): el bot de WhatsApp avisa a estos colaboradores (con teléfono) cuando se registra una observación (Fase 5). Editable con el botón **📣/🔕** por fila en el editor de personal de `configuracion.html` (`toggleAviso`); en el backup como columna `RECIBE_AVISOS` (SI/NO) |
| `asistencia_registros` | Registros de asistencia diaria (1 doc por `colabId`+`fecha`). **Bot de asistencia** añade: `origen:'WHATSAPP'` y **`marcajeEntrada`/`marcajeSalida`** = evidencia del marcaje `{sedeId, sede, cliente, lat, lng, distancia, radio, dentroRadio, geovalidada, fueraDePlan, hora, ts}`. El upsert del bot es idéntico al de la app |
| `asistencia_registros_fotos` | **Selfies del bot de asistencia** (base64 dataURL, colección aparte, doc id = id del registro). Campos: `fotoEntrada`, `fotoSalida`, `registroId`. En el backup de `configuracion.html` |
| `maestros_tiendas` | Sedes/tiendas. Campos: `id` (TIE…), `cliente`, `tienda`, `sede`, `activo`, `obs`, y **para el bot de asistencia geolocalizada** `latitud`/`longitud`/`radio` (m, default 250). Editor de Tiendas en `configuracion.html` (pegar "lat, long" de Google Maps + radio + link 🗺️) |
| `wa_asistencia_sesiones` | **Estado de conversación del marcaje** (doc id = wa_id, TTL 20 min). Campos: `tipo` (ENTRADA/SALIDA), `fase` (RECOLECTA/ELIGE_SEDE), `planSedes`, `sede`, `punto`, `ubicacion`, `fueraDePlan`, `tieneSelfie`, `expiraEn`. Transitoria (no se respalda) |
| `wa_asistencia_fotos` | **Selfie pendiente** del marcaje (doc id = wa_id, TTL 20 min, base64) hasta que se escribe el registro. Transitoria (no se respalda) |
| `maestros_feriados` | Feriados (campo `fecha`: YYYY-MM-DD) |
| `usuarios` | Usuarios del sistema con roles |
| `insumos_catalogo` | Tipos de ítem/herramienta (nombre, categoria, marca, codigo único, unidad, stockMin, tipoCantidad) |
| `insumos_instancias` | Unidades físicas individuales (itemId→catalogo, sede, estado, paqueteId, responsable, notas) |
| `insumos_movimientos` | Entradas/salidas/transferencias/actualizaciones de instancias |
| `insumos_paquetes` | Contenedores (MOCHILA/CAJA/CAJON/ANAQUEL/MALETÍN) que agrupan instancias vía array `instancias[]` |
| `insumos_instancias_fotos` | Foto (identificación) de cada instancia. Doc id = id de instancia. Campo `foto` = base64 (dataURL JPEG 320×320). **Colección aparte** (no dentro del doc de instancia) para no inflar las lecturas/exportaciones de `insumos_instancias`; se carga junto al resto en `loadAll` |
| `insumos_movimientos_fotos` | Adjuntos opcionales de un movimiento (hoy: salida/baja en lote). Doc id = id del movimiento. Campos: `guiaNumero`, `guiaFoto` (base64), `entregaFoto` (base64), `movimientoId`, `createdAt/By`. **Colección aparte** (igual criterio que las fotos de instancia); se carga en `loadAll` al global `movFotos` (`{guia,entrega}` por movId). Las fotos usan `compressFit` (mantiene proporción, no recorte cuadrado: son documentos/escenas). Se borran al revertir el movimiento o por cascada |
| `proveedores` | Directorio de proveedores (`proveedores.html`). Campos: `ruc`, `razonSocial` (MAYÚSCULAS), `nombreComercial`, `rubro`, `telefono`, `email`, `contacto`, `direccion`, `condicionesPago`, `banco`, `cuenta`, `notas` (texto libre multilínea), `activo` (`SI`/`NO`), + `createdAt/By`, `updatedAt/By`. Doc id = auto-generado (no el RUC, para permitir corregir el RUC sin orfandato; se evita RUC duplicado al crear). ADMIN/SUPER_ADMIN editan; SUPERVISOR solo lectura |
| `compras` | Historial de compras a proveedores (`proveedores.html` → panel Detalles; capa de datos en `compras.js`/`window.COMPRAS`, **top-level, no anidada en `proveedores`**). Campos: `proveedorId`, `proveedorRazonSocial` (denormalizado), `fecha` (`YYYY-MM-DD`), `numeroComprobante` (opcional), `items[]` — cada uno `{tipo:'ARTICULO'\|'SERVICIO', catalogoId (solo ARTICULO, referencia a `insumos_catalogo`, preparación para Inventario — no activa), nombre, cantidad, precioUnitario, subtotal}` —, `total`, `origen:'PROVEEDORES'`, `createdAt/By`. Doc id = auto-generado. Sin edición/eliminación todavía (solo alta + consulta). ADMIN/SUPER_ADMIN y SUPERVISOR con acceso registran; el resto solo consulta |
| `comprobantes` | Comprobantes escaneados en Rendición de Caja (`comprobantes.html`). Campos: `fecha` (DD/MM/YYYY, editable), `fechaISO` (YYYY-MM-DD, derivada, usada para el filtro por rango de Historial), `tipo` (FT/BO/TK/NC/OT), `numero`, `ruc`, `proveedor`, `descripcion` (manual), `monto`, `createdAt/By`, `updatedAt/By`. Doc id = auto-generado; se guarda apenas Gemini responde, se actualiza al editar, se borra con el ✕ de la fila |
| `comprobantes_fotos` | Foto de cada comprobante (base64, `compressFit`). Doc id = id del comprobante dueño (**colección aparte**, mismo criterio que `insumos_catalogo_fotos`) |
| `mtto_actividades_periodo` | **Ajuste de las actividades de un TIPO en una sede, solo para un período** (doc id `SEDE\|PERIODO\|AÑO\|TIPO`). Ej: "este bimestre, a las cortinas de Atocongo hay que hacerles además X". Campos: `sede`, `periodo`, `anio`, `tipo`, `agregadas[{nombre,minutos}]`, `quitadas[nombre]`, `updatedAt/By`. **Vence con el período** (el siguiente vuelve a la plantilla del cliente) — esa es la diferencia con `mtto_actividades_equipo`, que es permanente. Se aplica sobre la plantilla del cliente y ANTES del ajuste por equipo. Editable desde Mantenimiento (⚙️ Ajustar actividades → alcance "este período"). En el backup |
| `manta_equipos` | Maestro de equipos de la manta Ripley (`observaciones.html`). Doc id `MEQ-<slug tienda>__<slug equipo>`. Campos: `tienda` (ej. "RIPLEY SANTA ANITA"), `equipo` (ej. "AA Roof Top 03"), `orden`, `activo`, `origen`. Sembrado del Excel (56 equipos en 8 tiendas) con `migrar_db/seed_manta_equipos.js`. Alimenta los desplegables dependientes tienda→equipo y el export con relleno "Sin observaciones." |
| `manta_observaciones` | Observaciones de mantenimiento (`observaciones.html`). **Log**: 1 doc = 1 observación (un equipo tiene varias). Campos: `tienda`, `equipo`, `observacion`, `estado` (`PENDIENTE`/`EN_PROCESO`/`OK`), `fecha` (YYYY-MM-DD), `tieneFoto`, `tecnicoId` (opcional → `maestros_personal`), `origen` (`APP`/`WHATSAPP`), `registradoPor`, `createdAt/By`, `updatedAt/By`. **Obs v2 (bot):** además `sede` (ATOCONGO…), `eqId` (referencia a `inventario`, ej. MA-ATO-CAI-001), `tipo` (CORTINA DE AIRE…); `tienda`=`cliente + sede` (ej. "RIPLEY ATOCONGO") y `equipo`=nombre del equipo. ADMIN/SUPER_ADMIN editan; SUPERVISOR solo lectura |
| `manta_observaciones_fotos` | Foto opcional de una observación. Doc id = id de la observación. Campo `foto` = base64 (dataURL JPEG, `obsCompress` mantiene proporción, máx 1100px). **Colección aparte** (igual criterio que las fotos de instancia); se carga en `loadAll` al global `obsFotos` |
| `manta_guia` | Guía editable del bot de WhatsApp (Fase 4). Checklist por tipo de hallazgo: qué debe cubrir una buena observación. El bot la inyecta en el prompt de Gemini para decidir si repreguntar UN dato. Campos: `tipo`, `titulo`, `palabrasClave[]`, `checklist[]`, `orden`, `activo`, `origen`. Doc id `MGUIA-<tipo>`. Sembrada con `migrar_db/seed_manta_guia.js` (7 temas: filtros, refrigerante, eléctrico, compresor, drenaje, ventilación, general). **Editable por admin** desde `observaciones.html` (menú usuario → 🤖 Guía del bot, ADMIN/SUPER_ADMIN). En backup de `configuracion.html` (`manta_guia.csv`, arrays unidos por `\|`) |
| `wa_mensajes` | **Idempotencia** del bot (Fase 2): 1 doc por `messageId` de WhatsApp (Meta reenvía). `doc.create()` atómico → cada mensaje se procesa una sola vez. Transitoria/operativa (no se respalda) |
| `wa_sesiones` | **Estado de conversación** del bot (Fase 4): 1 doc por número (doc id = wa_id). Campos: `from`, `tecnicoId`, `fase` (RECOLECTANDO/CONFIRMANDO), `borrador{tienda,equipo,observacion,estado}`, `faltante`, `intentos`, `preguntoDetalle`, `historial[]`, `updatedAt`, `expiraEn` (TTL 30 min, chequeo en lectura). Transitoria/operativa (no se respalda) |
| `wa_sesiones_fotos` | **Foto pendiente** de una conversación del bot (Fase 5): 1 doc por número (doc id = wa_id). Campos: `base64`, `mime`, `updatedAt`. La foto llega en un mensaje y se confirma la obs en otro → se guarda aparte (no en `wa_sesiones`) hasta el "SÍ", y se borra al guardar/cancelar. Tope ~900KB. Transitoria/operativa (no se respalda) |
| `wa_ultima_obs` | **Última observación guardada** por número (doc id = wa_id), para "agregar foto a la última" que el técnico olvidó. Campos: `obsId`, `etiqueta` (nombre del equipo), `expiraEn` (TTL 30 min). Se escribe al guardar una obs y se borra al adjuntar la foto/cancelar. Transitoria/operativa (no se respalda) |

### Schema `asistencia_registros`
```js
{
  id, colabId, nombre, cargo, fecha,   // fecha: "YYYY-MM-DD"
  horaEntrada, horaSalida,             // decimal: 8.5 = 08:30
  estado,                              // "A" | "DM" | "F" | "P"
  observacion, heManual, horasExtra,
  registradoPor, timestamp
}
```

### Estados de asistencia
- `A` — Asistencia
- `DM` — Descanso médico
- `F` — Falta
- `P` — Permiso

## Colaboradores (maestros_personal)
| ID | Nombre | Cargo |
|---|---|---|
| PER001 | RAFAEL SANTOS | TECNICO |
| PER002 | ORLANDO MERA | TECNICO |
| PER003 | ENRIQUE YARANGA | TECNICO |
| PER004 | LUIS AMPUERO | AYUDANTE |
| PER005 | MATTIW CABRERA | AYUDANTE |
| PER006 | SEGUNDO INUMA | AYUDANTE |
| PER007 | LUIS CHAVEZ | AYUDANTE |
| PER008 | PETER PIZARRO | AYUDANTE |
| PER009 | HERNAN ZAVALETA | PDR |
| PER010 | JESUS MARTINEZ | PDR |
| PER011 | JOSE VIVAR | PDR |
| PER017563 | JEFFERSON | AYUDANTE |
| PER781233 | JOSE MARCHENA | ASISTENTE |
| PER885474 | ANDRES SAN MARTIN | ADMINISTRADOR |
| PER921832 | LESLIE ZAPATA | ASISTENTE ADMINISTRATIVO |

## Horas extra — lógica
```js
function calcHorasExtra(entrada, salida, fecha) {
  const total = salida - entrada;
  const dow = new Date(fecha + 'T12:00:00').getDay();
  const base = dow === 0 || isFeriado(fecha) ? 0 : dow === 6 ? 4.5 : 9.5;
  return Math.round((total - base) * 100) / 100;
}
// Domingo/feriado: base=0 (todo cuenta como HE)
// Sábado: base=4.5
// Lunes-Viernes: base=9.5
```

## Horarios decimales
- `8.5` = 08:30, `18.0` = 18:00, `19.5` = 19:30
- Redondeo a media hora: `round(h * 2) / 2`

## Orden de cards en index.html
**Configuración es siempre la última card del panel de apps.** Cualquier app nueva se inserta antes de Configuración.
Orden actual: Inv. Equipos → Mantenimiento → Itinerario → Insumos → Asistencia → Rendición de Caja → Proveedores → Observaciones → Personal → Configuración

## Personal (`personal.html`)
**Expediente del Colaborador 360°** — módulo dedicado (scaffold de `proveedores.html`) que reemplaza al editor de personal plano de `configuracion.html`. Escribe sobre la **misma colección `maestros_personal`** (no crea colección nueva en Fase 1). **Estado**: activa en develop, **pendiente de merge a main**. Diseño completo y decisiones en memoria del proyecto (`project_expediente_personal`).
- **Modelo (Fase 1)**: el doc de `maestros_personal` se amplía con — Identidad (`nombre` MAYÚSCULAS [canónico, lo usan asistencia/itinerario/insumos/bot], `tipoDoc`, `numDoc`, `fechaNacimiento`, `genero`, `nacionalidad`, `estadoCivil`, `foto` base64 320×320), Contacto (**`telefonos[]`** `{pais,prefijo,numero,e164,tipo,principal}` multi-país con catálogo `PAISES` —Perú +51 default, Chile +56—; **`correos[]`** `{tipo,email,principal}`; `direccion`; `contactoEmergencia`), Laboral (`cargo`, `area`, `estadoLaboral`, `fechaIngreso/Cese`, `tipoContrato`, `sedeBase`, **`jefeId`** jerarquía, `recibeAvisos`), Operativo (`rolOperativo`, **`competencias[]`**, `licenciaConducir{tiene,categoria,vence}`, `vehiculo`), `notas`, auditoría.
- **Retrocompatibilidad CRÍTICA con el bot**: al guardar se mantiene el campo legacy **`telefono`** = el `e164` del teléfono marcado `whatsapp`/principal → `api/_lib/identidad.js` (que identifica por "últimos 9 dígitos") sigue funcionando sin cambios. Migración de lectura: si el doc no tiene `telefonos[]`, se deriva del `telefono` plano viejo (PE/whatsapp/principal).
- **Escritura `{merge:true}`**: no pisa campos de otras vistas/fases. ✅ **Reconciliado con `configuracion.html`**: `saveMaestros` ya **no** hace delete-all+recrear (borra solo los colaboradores eliminados de la lista y hace `set {merge:true}` → preserva el expediente al guardar desde ahí); y el backup de `maestros_personal` gana una columna **`JSON`** con el documento completo (export **e** import) → restauración fiel de todos los campos del expediente. Las columnas legibles (ID/NOMBRE/CARGO/TELEFONO/ACTIVO/FOTO/RECIBE_AVISOS) se conservan; los backups viejos sin `JSON` importan los 8 campos como antes.
- **Roles**: `isPersAdmin()` = `MA.can('editar','personal')` (ADMIN/SUPER editan; SUPERVISOR con `personal` en `apps[]`; TECNICO solo lectura, modal readonly). Borrar = solo SUPER_ADMIN. `personal` agregado a `ALL_APPS` de los 8 paneles de accesos. Sueldo/datos económicos NO incluidos en Fase 1 (irán en `personal_confidencial`, solo ADMIN — Fase 2).
- **UI**: toolbar (búsqueda viva + Exportar Excel + Nuevo) + tabla (estado·avatar·nombre·documento·cargo·área·WhatsApp·correo) + modal-expediente con fieldsets Identidad/Contacto/Laboral/Operativo, repeaters de teléfonos/correos, chips de competencias, foto. Card 👥 en `index.html` antes de Configuración.
- **Pendiente (fases siguientes del diseño)**: F2 `personal_confidencial` + `firestore.rules`; F3 timeline `personal_eventos`; F4 legajo `personal_documentos` (Drive vía rclone) + `personal_certificaciones` + alertas de vencimiento; F5 catálogos `cat_*` editables + `identidad.js` a E.164 + enganche con el bot de asistencia geolocalizada (`project_asistencia_bot`).

## Tabs de la app
1. **Hoy** — registro del día, entrada/salida por colaborador
2. **Historial** — registros por rango de fechas
3. **Vista E/S** — pivot por colaborador mostrando entrada/salida
4. **Resumen HE** — horas extra por período
5. **Feriados** — gestión de feriados
6. **Ausencias** — reporte de días sin registro, agrupado por colaborador o fecha

## Exportaciones
- **Excel** — pivot table con inline CSS (HTML→XLS via Blob), naranja en domingos/feriados
- **PDF** — mismo formato que Excel, ancho dinámico `Math.max(297, ncols*12+24)mm`
- Ambos exportan: Nombre, Cargo, y por cada día: E/S/HE

## Import WhatsApp (wsp_import/)
Script Python `importar_asistencia.py`:
- Lee `_chat.txt` del ZIP exportado desde celular (formato: `[D/MM/YY, HH:MM:SS] sender: <adjunto: NNNNN-PHOTO-YYYY-MM-DD-HH-MM-SS.jpg>`)
- Extrae timestamp del nombre del archivo (más confiable que el mensaje)
- entrada = min foto antes de 12:00, salida = max foto desde 12:00, redondeado a ½h
- Regex con `\d{1,2}` para día (el formato usa 1 dígito para días 1-9)
- Requiere `serviceAccount.json` (busca en wsp_import/, ~/Documents/migrar_db/, ~/Downloads/)
- Mapeo sender → colabId hardcodeado en `SENDER_ID_MAP`
- Horarios fijos para Leslie, Andres, Jose Marchena (no usan WhatsApp para marcar)

### Importación realizada (2026-05-16)
- Período: 2026-03-25 → 2026-05-14
- 192 registros importados desde WhatsApp (25 abr en adelante)
- Jefferson: estado DM todos los días laborales (descanso médico)
- Andres/Leslie: horario fijo 08:30–18:00
- Jose Marchena: horario fijo 11:00–18:30, heManual=true, horasExtra=0

## Roles de usuario
- `SUPER_ADMIN` — acceso total (marchenaangulojoseluis@gmail.com, adrian30_04@hotmail.com)
- `ADMIN` — gestión completa
- `SUPERVISOR` — solo lectura

## Responsive — regla general
**Todas las apps deben ser responsivas y funcionar en teléfono.** Breakpoint principal: `@media(max-width:768px)`.
Patrones obligatorios:
- Header: ocultar textos secundarios, reducir padding, avatar circular sin nombre
- Cards/secciones: reducir padding (`14px` en móvil)
- Botones de acción: `width:100%` y `justify-content:center` en móvil
- Tablas: siempre dentro de `<div class="table-scroll">` con `overflow-x:auto`
- Formularios con `flex-row`: `flex-direction:column` en móvil
- Upload zone: soportar cámara con `<input capture="environment">` + botón "📷 Tomar foto"

## Notas técnicas
- Firebase SDK compat (no modular) — `firebase.initializeApp()`
- Brave browser bloquea Firebase longpolling → ERR_BLOCKED_BY_CLIENT en consola, inofensivo
- CSS Grid con `min-width:0` para que las celdas no desborden
- Comentarios en filas: `word-break:break-word` en vez de truncar
- Selector de colaborador: `<select>` dropdown (antes era search+grid)
- `celdasColab()` — verifica registro Firestore ANTES de checar si es día no laboral

## Deploy — Vercel

| Entorno | URL fija |
|---|---|
| Producción | https://multiaire-peru-app.vercel.app |
| Develop | https://multiaire-peru-app-develop.vercel.app |

- Cada `git push origin develop` → deploy automático en develop (GitHub Action)
- Cada `git push origin main` (merge manual) → deploy automático en producción (GitHub Action)
- `vercel --prod` solo si se necesita deploy manual urgente
- SSO protection deshabilitada — URLs públicas sin login de Vercel
- `.vercelignore` excluye `wsp_import/`, `_chat.txt`, `CLAUDE.md`, `*.py`, `*.json` (excepto `firebase-config.js`)

## index.html — lógica por entorno

- **Login obligatorio (2026-07-02)**: el panel de apps exige login ANTES de mostrar las tarjetas — mismo scaffold de auth que los módulos (overlay + `signInWithPopup` + verificación contra `usuarios` + `MA.setUser`). Como la sesión de Firebase persiste en el dominio, un solo login sirve para el índice y todos los módulos. El header gana user-pill (avatar + menú con rol y Cerrar sesión).
- **Tarjetas por rol**: cada card lleva `data-app`; `applyCardAccess()` oculta las que el usuario no puede abrir (`MA.canEnter(app)`; Configuración solo `SUPER_ADMIN`, igual que su gate interno). El `card-meta` muestra el slug limpio (`inventario`), ya no el nombre de archivo `.html`.
- **Máscara de URL (auth-guard.js, decisión de UX 2026-07-02)**: en todas las páginas la barra de direcciones muestra siempre solo el dominio (`history.replaceState` a `/`). Cosmético, NO seguridad. Consecuencias asumidas: F5, Adelante, duplicar pestaña y restaurar sesión dentro de un módulo vuelven al índice; los deep-links SÍ abren (la máscara reescribe después de cargar), solo no se pueden copiar de la barra. Solo aplica cuando la app se sirve en la raíz del dominio (en subrutas tipo GitHub Pages `/repo/` se desactiva sola — `replaceState('/')` rompería los href relativos). No aplica en localhost.
- **Producción**: todas las cards activas (Insumos activado 2026-06-06; el bloque `if(!isDev)` itera un array vacío).
- **Develop / Localhost**: todas las cards activas
- **Branding (2026-07-03, iterado con el usuario)**: header solo con el título **"Panel de Gestión" centrado** (negrita, blanco, 19px; `.logo` absoluto al 50%); el **logo oficial** (`logo-multiaire.png`, mismo de la web pública) va **arriba del panel de apps** (`.brand`, 72px desktop / 56px móvil, donde estaba el hero) y encabezando la tarjeta de login. Se **eliminaron** el hero de bienvenida y la tarjeta "Información del sistema" (incluido el **link al repo de GitHub** — no exponerlo a los usuarios). En móvil se oculta el badge "Sistema activo" para que no choque con el título centrado.
- Versión y entorno se muestran dinámicamente según hostname, **en el footer** (`#footer-ver`):
  - Producción → `v3.3.0`
  - Develop → `v3.3.0-dev · Develop`
  - Localhost → `v3.3.0-dev · Local`

## Firebase — Dominios autorizados

Dominios activos en Firebase Auth:
- `localhost`, `127.0.0.1` — desarrollo local
- `multiaire-fee43.firebaseapp.com`, `multiaire-fee43.web.app` — Firebase defaults
- `marchenaangulojoseluis-dev.github.io` — GitHub Pages (empresa aún en uso)
- `multiaire-peru-app.vercel.app` — producción Vercel
- `multiaire-peru-app-develop.vercel.app` — develop Vercel
- `app.multiaire.com.pe` — **dominio corporativo de producción** (conectado 2026-06-19; CNAME→cname.vercel-dns.com en ChileCL)

Todos los dominios de Cloudflare tunnel fueron eliminados.

> **Dominio corporativo `app.multiaire.com.pe`**: el subdominio se sirve por **CNAME** (no se delegan los nameservers del dominio a Vercel — el correo corporativo vive en cPanel de ChileCL). Apunta al proyecto Vercel `app-mantenimiento` (producción/main = la misma deployment que `multiaire-peru-app.vercel.app`). SSL emitido por Vercel.

## Trabajo colaborativo (José + Adrián)

Equipo de dos: **José** (Mac, admin del repo/org) y **Adrián** (GitHub `Adro3004`, laptop Windows, colaborador con push). Ambos trabajan con Claude Code.

- **Ramas:** `main` = producción VIVA (bots de WhatsApp + Firestore real) — **solo José mergea a main**, siempre tras Council (`/auditar`). `develop` = integración. Toda tarea = rama `feature/<tarea>` desde `develop` → PR a `develop`.
- **Territorio por módulos:** nunca dos personas editando el mismo `.html` a la vez (archivos monolíticos → merges dolorosos). El reparto vigente vive en el Tablero de equipo.
- **Tablero de equipo:** nota `Tablero de equipo.md` en el vault compartido **"Cerebro MultiAire"** (Google Drive, accesible vía MCP `obsidian-cerebro`). Al empezar una tarea → registrar fila en *En curso* (quién/módulo/rama); al abrir PR → moverla a *Listo para revisar*; decisiones nuevas → anotarlas en *Acordado*.
- **Datos (crítico):** hay UN solo Firestore y es el de producción. Backup desde `configuracion.html` antes de migraciones/cargas masivas; toda colección nueva se agrega al backup (regla vigente); avisar en el tablero antes de tocar maestros (personal, tiendas, insumos).
- **PRs chicos y frecuentes:** una tarea = un PR. Autorevisión con `/code-review` antes de abrirlo; José pasa el Council antes de mergear a develop.
- **Validación:** probar en la URL de develop (deploy automático al pushear; ver sección Deploy), no asumir que local basta.

## Migración índice→nombre (cutover)

Las actividades de mantenimiento se identifican por **NOMBRE**, no por su posición en la lista. Antes se
guardaban por índice, y quitar una actividad corría los índices de todas las siguientes: la marca que
decía "hice la #2" pasaba a apuntar a otra actividad.

**Dónde vive la identidad de una actividad hoy:**
- `mantenimiento/{SEDE|PERIODO|AÑO}.tareaStatus[eq_id]` → `{ 'Mantenimiento integral': true }`
  (`true` a secas sigue siendo "solo marcar": el equipo entero, cuando no tiene actividades)
- `mtto_plan` y `mtto_ejecuciones` → doc-id `EQ|PERIODO|AÑO|<actKey(nombre)>`
- `mantenimiento_fotos` → campo `tarea` (el `tareaIdx` ya no se usa)

`actKey` escapa `%`, `|` y `/` de forma **inyectiva** (el `%` primero): `Lavado a/c` → `Lavado a%2Fc`.
Dos actividades distintas nunca caen en el mismo doc-id. La función está duplicada en
`mantenimiento_multiaire.html`, `itinerario.html`, `api/_lib/mtto.js` y en el script de migración —
**si cambia en uno, cambia en los cuatro**.

**El código lee los dos formatos** (`tareaHecha` acepta la marca por nombre y la vieja por índice), así
que funciona con datos sin migrar. Pero **escribe solo por nombre**.

### El cutover

Firestore es **uno solo**, compartido por develop y producción. Producción (`main`) todavía lee por
índice, así que **migrar los datos antes del merge dejaría el checklist en blanco para los técnicos**.
El orden es:

1. Merge de la rama a `main` (producción pasa a leer por nombre — con fallback a índice, así que nada
   se rompe aunque el paso 2 tarde).
2. `cd ~/Documents/migrar_db && node migrar_indices_a_nombres.js` → **simulacro**, no escribe. Revisar
   el reporte: índices corridos, marcas sin evidencia, fotos desfasadas, destinos que ya existen.
3. `node migrar_indices_a_nombres.js --confirmar` → escribe. Deja respaldo JSON en
   `migrar_db/_backup_indices_<fecha>.json`.

**Cómo resuelve el nombre de cada índice** (esto es lo importante): NO por la posición en la lista de
hoy — eso sería creer que la lista nunca cambió, y si una actividad se quitó después de marcarse, le
pondría a la marca el nombre de OTRA actividad. Usa **evidencia**: `mtto_ejecuciones` y
`mantenimiento_fotos` guardan `tareaIdx` **y** `tarea`, o sea qué nombre tenía esa posición **en el
momento** del registro. Solo cae a la posición actual cuando no hay evidencia, y lo reporta.

**Corre contra el Firestore vivo, con el bot escribiendo.** Por eso `mantenimiento` se migra **doc por
doc en transacción**: relee el documento fresco, convierte sobre ESE dato (una marca que el técnico
acabe de reportar por WhatsApp ya está ahí, y se copia tal cual) y escribe con **`update()`**.
Tiene que ser `update()` y no `set(merge:true)`: **Firestore hace merge PROFUNDO de los mapas
anidados**, así que un `set` con merge dejaría las claves numéricas viejas conviviendo con las nuevas
— la migración no limpiaría nada. `update()` reemplaza el mapa y no toca `seleccionados`/`clave`.

Es **idempotente** (correrlo dos veces no rompe nada) y no borra ninguna marca que no pueda resolver.

## Changelog

| Fecha | Cambio |
|---|---|
| 2026-05-16 | Importación masiva desde WhatsApp: 192 registros, período 2026-03-25 → 2026-05-14 |
| 2026-05-16 | Creación de CLAUDE.md con documentación completa del proyecto |
| 2026-05-16 | Configuración de Vercel — URLs fijas de producción y develop |
| 2026-05-16 | GitHub Actions para deploy automático en develop y producción |
| 2026-05-16 | Cards Insumos y Asistencia deshabilitadas en producción, activas en develop |
| 2026-05-16 | Versión y entorno dinámicos en index.html según hostname |
| 2026-05-16 | Limpieza de dominios Firebase — eliminados tunnels de Cloudflare |
| 2026-05-16 | SSO protection de Vercel deshabilitada — URLs públicas sin login |
| 2026-05-16 | .gitignore actualizado: excluye _chat.txt y settings.local.json |
| 2026-05-17 | Optimización queries Firestore: todas las consultas usan .where('fecha') en lugar de traer colección completa |
| 2026-05-17 | Corrige doble conteo de Faltas en Resumen H.E.: cRegs solo procesa días trabajados, diasRango maneja todas las ausencias |
| 2026-05-17 | Corrige deleteReg: captura fecha antes de closeEditReg para evitar leer editRegData=null |
| 2026-05-17 | Elimina variable dom duplicada en Vista T/A (condición f==='dom' nunca verdadera) |
| 2026-05-17 | Confirmación antes de registrar DM/Permiso/Falta para evitar clicks accidentales |
| 2026-05-17 | Tabla móvil: oculta columnas Cargo y Comentario en pantallas pequeñas |
| 2026-05-17 | Estado del colaborador en sidebar: badge (entrada/salida/DM/permiso/falta/sin registro) al seleccionar colaborador, solo visible cuando fecha=hoy |
| 2026-05-17 | Modo SUPERVISOR solo lectura: sidebar oculto, botones Guardar/Eliminar del modal ocultos via CSS (supervisor-mode class), campos del modal deshabilitados |
| 2026-05-18 | HE automático en modal de edición: se recalcula en tiempo real al cambiar entrada/salida (onchange → updateHeAutoInfo) |
| 2026-05-18 | Auto-refresca el tab activo al guardar o eliminar registro — Vista E/S, T/A, Historial y Resumen se actualizan sin tener que regenerar |
| 2026-05-18 | Fix: tab Ausencias ahora se refresca automáticamente al guardar/eliminar registro |
| 2026-05-18 | Fix: guard defensivo en saveEditReg si editRegData es null o colab no se encuentra |
| 2026-05-18 | Agrega campo `telefono` al maestro de personal — preparación para chatbot WhatsApp |
| 2026-05-18 | Fix: faltas en sábado descuentan -4.5h (no -8h) en Resumen HE y exportaciones — función global heAusencia() |
| 2026-05-18 | Fix: Resumen HE respeta horasExtra guardado en registros individuales — correcciones manuales en faltas prevalecen sobre el cálculo automático |
| 2026-05-18 | Asistencia habilitada en producción — solo Insumos permanece EN DESARROLLO |
| 2026-05-18 | Backup/import ampliado: incluye asistencia_registros, maestros_personal, maestros_tiendas, maestros_grupos, maestros_feriados — con batching para colecciones grandes |
| 2026-05-18 | Backup/import agrega bd_itinerarios y bd_bloques — batchWrite soporta docId separado del campo id interno |
| 2026-05-21 | Mantenimiento: sidebar muestra "Última actualización" del período seleccionado (sede + periodo + año) — se actualiza al cargar desde Firestore/cache y al guardar |
| 2026-05-21 | Mantenimiento: guarda y muestra `updatedBy` (correo del usuario) junto con `updatedAt` en sidebar, Firestore y sessionStorage |
| 2026-05-21 | Mantenimiento PDF: header muestra recuadro de última actualización (fecha/hora + correo); footer muestra línea de última actualización; nombre del archivo incluye fecha/hora de actualización |
| 2026-05-21 | Mantenimiento PDF: se elimina "Generado: [hora]" del header — solo queda en footer |
| 2026-05-27 | Nueva app ComprobaScan (`comprobantes.html`): escáner de facturas/boletas con IA, tabla editable, exportación a Excel (SheetJS) — activa en develop, deshabilitada en producción |
| 2026-05-27 | ComprobaScan: IA usa Google Gemini (REST directo, sin backend). Auth: header `x-goog-api-key`. Modelo default: `gemini-2.5-flash`. Lista: 2.5-flash, 2.0-flash, 1.5-flash-latest, 1.5-pro-latest |
| 2026-05-27 | ComprobaScan: selector de modelo como pills (no `<select>` — CSS global lo rompía). File input con `<label for>` (no `.click()` JS — Brave lo bloqueaba) |
| 2026-05-27 | ComprobaScan: imágenes comprimidas a máx 1600px / JPEG 0.85 antes de enviar a Gemini |
| 2026-05-27 | ComprobaScan: columnas Excel — FECHA, COMP, NUMERO, RUC, PROVEEDOR, DESCRIPCION (manual obligatoria), MONTO |
| 2026-05-27 | SSH configurado: clave ed25519 en ~/.ssh/id_ed25519_github, remote cambiado a git@github.com — no pide contraseña |
| 2026-05-21 | Mantenimiento WSP: mensaje incluye fecha/hora de última actualización en cursiva (_Actualizado: ..._); confirmación de copiado cambia a banner verde centrado en pantalla |
| 2026-05-27 | ComprobaScan: rewrite completo desde cero — header/auth/footer idéntico a insumos.html, todos los bugs corregidos |
| 2026-05-27 | ComprobaScan: tipos de comprobante a 2 siglas — FT=Factura, BO=Boleta, TK=Ticket, NC=Nota de Crédito, OT=Otro |
| 2026-05-27 | ComprobaScan: Gemini fuerza JSON con responseMimeType+responseSchema; prompt reescrito con ejemplos reales |
| 2026-05-27 | ComprobaScan: panel Gestión de accesos para ADMIN/SUPER_ADMIN — lista usuarios, cambia roles, asigna apps |
| 2026-05-27 | ComprobaScan: fix footer jump — wrapper flex:1 permanente evita que footer suba al header durante carga |
| 2026-05-27 | ComprobaScan: ← Inicio usa .back-link (pill transparente) igual que mantenimiento/asistencia |
| 2026-05-27 | ComprobaScan: soporte PDF multi-página — cada página → Gemini independiente → fila propia en tabla |
| 2026-05-27 | ComprobaScan: API key y modelo pasan de sessionStorage a localStorage — persisten entre sesiones; botón 🗑 Borrar |
| 2026-05-27 | ComprobaScan: rate limiter free tier — 6.5s entre llamadas Gemini con cuenta regresiva visible en status |
| 2026-05-27 | ComprobaScan: actualiza lista de modelos — retira gemini-2.0-flash (deprecated jun-2026) y 1.5-flash/pro; agrega gemini-2.5-flash-lite y gemini-3.5-flash |
| 2026-05-27 | Rendición de Caja: renombrada desde ComprobaScan/FacturasIA — card movida antes de Configuración |
| 2026-05-27 | Rendición de Caja: responsive móvil mejorado + botón 📷 Tomar foto (capture=environment) |
| 2026-05-27 | CLAUDE.md: regla general — todas las apps deben ser responsivas, patrones obligatorios documentados |
| 2026-05-27 | index.html: regla permanente — Configuración siempre es la última card del panel |
| 2026-05-27 | Rendición de Caja: default modelo gemini-2.5-flash-lite (1000 RPD), delay 4.5s free tier |
| 2026-05-27 | Rendición de Caja: fix API key input alineado izquierda en móvil (align-items:stretch) |
| 2026-05-27 | Rendición de Caja: col-ruc min-width 130px, col-prov 200px en móvil |
| 2026-05-27 | Rendición de Caja: botón 📷 Tomar foto oculto en desktop, visible solo en móvil |
| 2026-05-27 | Rendición de Caja: habilitada en producción — probada en celular ✓ |
| 2026-06-06 | Insumos: documentado el modelo de 3 niveles (catálogo/instancias/paquetes) en CLAUDE.md |
| 2026-06-06 | Insumos: carga inicial desde Excel Hoja2 — 46 tipos + 52 instancias + 5 paquetes-anaquel en sede OFICINA (Chinchón). Script migrar_db/import_hoja2.js |
| 2026-06-06 | Insumos: ubicación física del almacén = un único paquete tipo ANAQUEL (`ANAQUEL 02`); el plano (PLANO 0X) es solo un detalle de la instancia (campo `plano` + notas), no un contenedor |
| 2026-06-06 | Insumos: etiquetas con formato seleccionable QR o código de barras Code128 (JsBarcode). Selector QR/Barras en modal de etiqueta y en exportación ZIP (catálogo e instancias) |
| 2026-06-06 | Insumos: escáner renombrado "Escanear QR / Barras" (html5-qrcode ya leía ambos); qrbox horizontal adaptable (mejor encuadre de barras 1D en móvil); botones de resultado "Ver QR"→"Ver etiqueta"; flex-wrap en barras de selección para móvil |
| 2026-06-06 | Insumos: scroll horizontal en tablas en TODAS las pantallas — `.table-wrap` base pasa de `overflow:hidden` a `overflow-x:auto` (antes solo en móvil; en desktop con muchas columnas se cortaban) |
| 2026-06-06 | Insumos: columnas fijas (checkbox + Código/ID) al hacer scroll horizontal en tablas Catálogo e Instancias — `position:sticky` con fondo sólido, hover consistente y separador; checkbox ancho fijo 44px |
| 2026-06-06 | Backup (`configuracion.html`): agrega export+import de las 4 colecciones `insumos_*` (catálogo/instancias en columnas, paquetes con instancias por `\|`, movimientos en JSON). `parseCSV` reescrito como parser CSV correcto (comillas escapadas + saltos de línea citados). Verificado round-trip con datos reales |
| 2026-06-06 | Backup: fix — la importación solo cargaba 4 de los 15 CSV (`expected` incompleto), así que asistencia/maestros_*/bd_* se exportaban pero NO se restauraban. Ahora `expected` lista los 15; el backup/restore es completo |
| 2026-06-06 | Insumos: etiqueta de código de barras rediseñada a media hoja (400×150 mm, prop. 8:3) — barra dominante + código chiquito, sin nombre; alta resolución (2000×750) para impresión nítida. Se descarga individual/ZIP y el usuario acomoda 2 por hoja. Se retira la impresión "2/hoja" previa |
| 2026-06-06 | Insumos: barra estirada para llenar la etiqueta (~94% ancho × ~66% alto, márgenes mínimos, `imageSmoothingEnabled=false`) |
| 2026-06-06 | Insumos: **activado en producción** — `index.html` deja de marcarlo "EN DESARROLLO" (array vacío); todas las cards activas en prod. Merge develop→main |
| 2026-06-06 | Insumos: export "📄 CSV (Phomemo)" en barra de selección de Instancias — CSV `CODIGO,NOMBRE` (BOM UTF-8) para impresión por lotes en impresoras térmicas Phomemo 40×30 mm (la app dibuja el barcode desde la columna CODIGO) |
| 2026-06-08 | Fotos del personal: campo `foto` en `maestros_personal` = **imagen base64** (dataURL JPEG 200×200, recorte centrado, calidad 0.8) guardada en el propio documento de Firestore. Editor de personal en `configuracion.html`: botón 📷 por fila comprime y guarda la imagen; miniatura circular al inicio de cada fila. Se persiste al "Guardar cambios" (preservada en `loadMaestros`/`saveMaestros`). **Sin Firebase Storage** — Storage exige plan Blaze; base64 en Firestore funciona en Spark (gratis) y basta para esta escala (~15 colaboradores, fotos diminutas) |
| 2026-06-08 | Insumos: tab "Por Técnico" muestra cabecera con avatar del técnico (foto de `maestros_personal` o inicial de respaldo) en `renderPorTecnico` |
| 2026-06-08 | Backup (`configuracion.html`): columna `FOTO` agregada al export/import CSV de `maestros_personal` |
| 2026-06-08 | Insumos (datos): recategorizado `GEN-01` "MANOMETRO" (4 instancias) de SIN CATEGORIA → MEDICION en Firestore. Script `migrar_db/recat_manometros.js` |
| 2026-06-10 | Fotos del personal: avatar del tab "Por Técnico" agrandado de 56px → 96px (inicial 38px); captura de foto subida de 200×200 → 320×320 (sigue base64 en Firestore, ~40 KB). La miniatura del editor de personal se mantiene en 34px |
| 2026-06-10 | Insumos tab "Por Técnico": opción **"👥 Todos"** en el selector (es el valor por defecto al abrir el tab) — muestra una grilla de tarjetas de todo el personal (avatar + cargo + nº paquetes/instancias sueltas); clic en una tarjeta abre su detalle. `renderPorTecnico` ahora es dispatcher (`renderTodosTecnicos`/`renderTecnicoDetalle`); helper `tecnicoResponsables()` unifica la lista (personal activo + responsables de paquetes/instancias) |
| 2026-06-10 | Insumos: tab renombrado **"👤 Por Técnico" → "👥 Por Persona"** (textos visibles: botón, placeholder del selector y empty state). Los ids internos `tecnicos`/`tab-tecnicos`/`f-tec-persona` se mantienen |
| 2026-06-10 | Personal: rellenadas 11 fotos faltantes desde "DATOS PARA FOTOCHECK.docx" (emparejadas por nombre, recorte cuadrado 320×320 base64). Scripts en `~/Documents/migrar_db/fotocheck/`. Sin foto: ENRIQUE YARANGA, JESÚS MARTÍNEZ, JOSE MARCHENA (no estaban en el docx) |
| 2026-06-10 | Insumos: **foto por instancia** (identificación). Nueva colección `insumos_instancias_fotos` (base64, colección aparte). Subida desde el modal de edición de instancia (botón 📷, `setInstanciaFoto`/`removeInstanciaFoto`); miniatura en la celda ID/QR de la tabla (`instThumb`). Añadida al backup/import de `configuracion.html` |
| 2026-06-10 | Insumos foto de instancia: **lightbox** para ampliar (clic en preview del modal o en miniatura de la tabla → visor `foto-lightbox` a pantalla completa), botón **📸 Tomar foto** (`capture=environment`, clase `.cam-only-mobile` visible solo en móvil) además del de subir, y resolución de captura subida a **480×480** para que ampliar se vea bien. Responsive verificado (modal tipo sheet en móvil, lightbox en vw/vh) |
| 2026-06-10 | Deploy: **eliminado el deploy duplicado**. Cada push generaba 2 deployments (el GitHub Action `vercel deploy` + el auto-deploy de la integración Git de Vercel), duplicando el consumo del plan gratuito. Se añade `vercel.json` con `"git":{"deploymentEnabled":false}` (y excepción en `.vercelignore` para no excluirlo) → los GitHub Actions quedan como **único** mecanismo de deploy (conservan el alias a las URLs fijas) |
| 2026-06-10 | **Nueva app Proveedores (`proveedores.html`)**: directorio CRUD sobre la colección `proveedores`. Construida sobre el scaffold de `comprobantes.html` (header/auth/accesos/footer/toast), retirado todo Gemini/scanner/pdf.js. Toolbar con búsqueda en vivo + Exportar Excel + "Nuevo proveedor" (solo admin) + tabla; modal de alta/edición; SUPERVISOR solo lectura (`isProvAdmin`). Validación de RUC (`^\d{8,11}$`), razón social en MAYÚSCULAS, RUC duplicado bloqueado al crear |
| 2026-06-10 | Proveedores: card 🏢 en `index.html` antes de Configuración; `{id:'proveedores'}` agregado a `ALL_APPS` del panel de accesos; backup export/import de `proveedores.csv` en `configuracion.html` (14ª colección). Activa en develop, pendiente de merge a main |
| 2026-06-10 | Proveedores: campo **`notas`** (texto libre multilínea) — `<textarea>` en el modal, columna "Notas" en la tabla (truncada con `title` completo), incluida en búsqueda, export Excel y backup CSV (`proveedores.csv`). Para observaciones internas (horarios, vendedor, descuentos, plazos, incidencias, links) |
| 2026-06-10 | Proveedores: **merge develop→main → activa en producción**. Probado con un proveedor piloto (FRIO COMPONENTES DEL PERU S.A.C.) creado vía firebase-admin y luego eliminado; la colección `proveedores` queda vacía para datos reales |
| 2026-06-11 | Insumos: **exportar material** a CSV (Excel-compatible, BOM UTF-8). Por persona (tab *Por Persona*, botón "⬇ Exportar material": sueltas + las de sus paquetes) y por paquete (tab *Paquetes* y tarjetas de paquete en detalle de persona, botón "⬇ Material"). Columnas CODIGO/ITEM/COD. CATALOGO/CATEGORIA/SEDE/ESTADO/PAQUETE/RESPONSABLE. Funciones `exportMaterialPersona`/`exportMaterialPaquete`/`_descargarMaterial`, sin dependencias |
| 2026-06-11 | Insumos: el export de material pasa a **selector de formato** (modal `modal-export-mat`): **PDF** principal (jsPDF UMD, reporte en canvas paginado A4), **PNG** segunda (mismo canvas, `toDataURL`), **Excel/CSV** última. `doExportMat(fmt)` despacha; PDF/PNG comparten `_matReportCanvas`. Se agrega jsPDF 2.5.1 vía CDN |
| 2026-06-11 | Insumos: opción **"Incluir foto de cada instancia"** (checkbox en el selector, solo PDF/PNG) → columna FOTO con miniatura recortada por fila (de `insumos_instancias_fotos`/`instFotos`); `_matReportCanvas` pasa a **async** (precarga base64 con `_loadImg`). El CSV gana columna FOTO (SÍ/NO) |
| 2026-06-16 | Insumos (fix): al editar el **nombre** de un ítem del catálogo (`saveInsumo`), ahora se **propaga a las instancias hijas** (`insumos_instancias` con `itemId===docId`) vía batch (troceado a 450 por el límite de 500 de Firestore) + actualización en memoria; antes el nombre denormalizado de cada instancia quedaba desactualizado (se veía viejo en exports de material e historial). Toast informa cuántas instancias se renombraron. El historial de `insumos_movimientos` conserva el `itemNombre` del momento (registro histórico) |
| 2026-06-17 | Insumos: **salida / baja en lote** desde la selección múltiple del tab *Instancias* (botones "⬆ Salida en lote" / "⚠️ Baja en lote", gating `can('salida')===1`). Modal `modal-salida-lote`: lista de seleccionadas + responsable + motivo (obligatorio si baja) + **N° de guía + foto de guía** y **foto de entrega**, todo opcional. `saveSalidaLote` crea UN movimiento `SALIDA`/`BAJA` con `instancias[]` + `instanciasSnapshot[]` (para reversión fiel de lotes mixtos) y borra las instancias por batch (450). Fotos en nueva colección **`insumos_movimientos_fotos`** (doc id = id del movimiento; `compressFit` mantiene proporción). `renderMovimientos` muestra 📎 guía + miniaturas (lightbox). Reversión/cascada adaptadas a lote + borrado de fotos. Se limpia código muerto duplicado en `guardarEdicionMovimiento` |
| 2026-06-17 | Backup (`configuracion.html`): agrega export/import de **`insumos_movimientos_fotos.csv`** (columnas ID/GUIA_NUMERO/GUIA_FOTO/ENTREGA_FOTO/CREATED_AT/CREATED_BY); añadida a `expected` (18ª colección) y a la lista visual de archivos |
| 2026-06-17 | Insumos: **transferencia en lote** desde la selección múltiple del tab *Instancias* (botón "🔄 Transferir en lote", gating `can('transferencia')===1`). Modal `modal-transf-lote` reutiliza el patrón de la transferencia individual (checkboxes Sede/Responsable, destino, realizado por, observación) + guía/fotos opcionales (infra compartida con la salida en lote vía `loteFotoPrefix`). `saveTransfLote` crea UN movimiento `TRANSFERENCIA` con `instancias[]` + `instanciasSnapshot[]` (valores previos) + `camposTransferidos`; aplica el destino a cada instancia y la saca de su paquete. Reversión por instancia (restaura sede/responsable previos; no re-inserta en paquete, igual que la individual) + cascada + borrado de fotos. `renderMovimientos`/`editarMovimiento` adaptados al lote. La transferencia individual (`saveTransferencia`, `instanciaId` + `diffsData`) sigue intacta |
| 2026-06-17 | Insumos: **carga masiva Excel** (`.xlsx`, SheetJS vía CDN). Botón "📊 Carga masiva" en el sidebar (gate `can('crearItem')`). Modal `modal-carga-masiva`: descargar plantilla, exportar base actual, importar con preview (＋nuevos · ↻actualizar · ⚠error) antes de escribir. Archivo de 3 hojas (Catalogo / Instancias / Valores validos). Upsert: catálogo por `CODIGO`, instancias por `ID` (autogenera el id de instancia si falta). Carga **silenciosa** (`origen:'IMPORT_XLSX'`, sin movimientos); nombre de instancia derivado del catálogo (+ propaga a hijas como Etapa 1); NO toca `paqueteId`. Escritura por batch (450). Cierra el plan de 4 etapas de mejoras a insumos |
| 2026-06-17 | Insumos: carga masiva **más intuitiva** — la plantilla/exportación gana una 1ª hoja **"Instrucciones"** (explica cada columna, obligatoriedad, valores válidos y reglas) + anchos de columna; las filas de ejemplo se marcan con `ID=EJEMPLO` y el importador las **ignora** automáticamente. Nuevo botón **"📖 Manual (PDF)"** (jsPDF) con la guía paso a paso descargable. Constantes de guía compartidas `CM_GUIA_CAT`/`CM_GUIA_INST`/`CM_REGLAS` |
| 2026-06-18 | Insumos: la plantilla/export de carga masiva ahora trae **menús desplegables** (Excel data validation) en CATEGORIA/UNIDAD/TIPO_CANTIDAD/ESTADO (inline) y SEDE/ITEM_ID/RESPONSABLE (hoja oculta `_listas`). Como SheetJS Community no escribe validación, se inyecta el XML `<dataValidations>` post-proceso con JSZip (`_cmXlsxBlob`/`_cmValidacionesXml`/`_cmSheetFile`/`_cmListasSheet`); descargas ahora **async** (Blob). Probado round-trip con SheetJS+JSZip reales (genera, inyecta, re-lee; orden OOXML válido) |
| 2026-06-18 | Insumos: carga masiva — **ejemplo guiado** (constante `CM_EJEMPLO`: agregar un ítem "TALADRO" + 2 unidades hijas vía `ITEM_ID=CODIGO`) embebido en la hoja "Instrucciones" de la plantilla/export y en el manual PDF |
| 2026-06-18 | Insumos: carga masiva **más amigable** — se quita la columna **ID del Catálogo** (la llave visible es CODIGO) y se **oculta** la columna ID en Instancias (interna, evita duplicar al reimportar). Se elimina la columna **PLANO** de la carga masiva y del backup, y el **campo `plano` se borra de Firestore** en las 52 instancias que lo tenían (no se mostraba en ninguna pantalla; script `migrar_db/borrar_plano.js`). Las hojas de datos van vacías (los ejemplos pasan a la hoja Instrucciones); se retira el mecanismo de filas `ID=EJEMPLO`. Dropdowns reposicionados (catálogo: CATEGORIA→B, UNIDAD→C, TIPO_CANTIDAD→H) |
| 2026-06-18 | **Nueva app Observaciones (`observaciones.html`)** — "Manta de Observaciones" de mantenimiento Ripley, sobre el scaffold de `proveedores.html`. Colección `manta_observaciones` (log: 1 doc = 1 observación) + `manta_observaciones_fotos` (base64 aparte) + maestro `manta_equipos` (8 tiendas + 56 equipos, sembrado del Excel con `migrar_db/seed_manta_equipos.js`). Desplegables dependientes tienda→equipo, filtros tienda/estado/fecha, estados PENDIENTE/EN_PROCESO/OK, foto con lightbox, export Excel (sábana completa con relleno "Sin observaciones.") y PDF (jsPDF). `isObsAdmin()` (SUPERVISOR solo lectura). Card en `index.html` antes de Configuración; `observaciones` en `ALL_APPS` de proveedores/comprobantes; 3 colecciones `manta_*` al backup. **Parte A** del encargo del bot WhatsApp; **Parte B (bot) pendiente** — backend acordado en Vercel Functions (sin Blaze) |
| 2026-06-19 | **Bot WhatsApp Fase 4 (motor conversacional)** — `api/_lib/conversacion.js`: máquina de estados sobre **`wa_sesiones`** (RECOLECTANDO→CONFIRMANDO, TTL 30 min, doc id = wa_id). Repregunta lo mínimo (tienda/equipo no resueltos contra `manta_equipos`, o **un** detalle sugerido por la guía editable **`manta_guia`**); siempre permite "guardar así"; **confirma antes de escribir** y el técnico fija/corrige el estado; comandos cancelar/nueva/ayuda. Nuevos módulos `sesiones.js`, `guia.js`, `escritura.js`; `gemini.js` ahora devuelve `faltaDetalle`/`pregunta` guiados por `manta_guia`. Al confirmar, escribe en `manta_observaciones` (origen WHATSAPP). `manejarMensaje` con `analizar`/`guardar` inyectables → testeable sin Meta ni Gemini. Guía sembrada con `migrar_db/seed_manta_guia.js` (7 temas). Probado contra Firestore real (`migrar_db/test_fase4.mjs`, 17/17). Pendiente Fase 5: foto desde WhatsApp + aviso a supervisores |
| 2026-06-19 | **Dominio corporativo `app.multiaire.com.pe` EN VIVO** — DNS resuelto en ChileCL (CNAME→cname.vercel-dns.com, serial `2026062002`); conectado al proyecto Vercel `app-mantenimiento` (producción/main) vía `vercel domains add`; SSL OK (HTTP 200, sirve el Panel de Apps); agregado a **Authorized domains** de Firebase Auth (solo append; script `migrar_db/add_authorized_domain.js`). Subdominio por CNAME (no se delegan NS — el correo cPanel sigue en ChileCL). Pendiente de la migración corporativa: pasar la PROPIEDAD de Vercel/GCP/GitHub a `plataforma@multiaire.com.pe` (transfers que requieren login de esa cuenta) |
| 2026-06-20 | **Bot fix (prueba EN VIVO con Gemini real):** `gemini.js` fallaba intermitente al parsear el JSON — gemini-2.5-flash es modelo con *thinking* y el razonamiento truncaba/malformaba el JSON. Fix: `thinkingConfig:{thinkingBudget:0}` (sin thinking → JSON estructurado fiable y más rápido) + `maxOutputTokens` 1024→2048 + parseo defensivo (extrae el primer `{...}`). Verificado en vivo contra el inventario real (`migrar_db/test_bot_live.mjs`): extrae bien sede/equipo y resuelve el código (MA-COM-EXT-002, etc.). Nota: el free tier de Gemini puede dar 429 con muchas llamadas seguidas (el bot lo maneja con gracia) |
| 2026-06-20 | **Observaciones v2 — lado APP (`observaciones.html`) alineado a `inventario`** — desplegables del modal pasan de tienda→equipo (`manta_equipos`) a **sede→tipo→equipo** desde `inventario` (557 equipos; el filtro de tipo ayuda con sedes de hasta 96). `loadAll` carga `inventario`; `saveObs` guarda `sede`/`eqId`/`tipo` (+ `tienda`=cliente+sede). Export Excel = sábana completa del inventario (CLIENTE/SEDE/TIPO/EQUIPO/CÓDIGO/…); PDF = reporte de hallazgos por sede. Filtro de la toolbar poblado desde las observaciones. `manta_observaciones` estaba vacía → pivot limpio sin migración. Cierra el pivot completo (bot + app) |
| 2026-06-20 | **Observaciones v2 — bot alineado al inventario REAL** (`inventario`, 557 equipos · 12 sedes · 14 tipos, en vez de los 56 Roof Tops Ripley de `manta_equipos`). Nuevos `api/_lib/inventario.js` (carga+cachea `inventario`, contexto sedes/tipos) y `equipos.js` (`resolverEquipo`: sede fuzzy + equipo por eq_id exacto / scoring tipo+número / candidatos agrupados por tipo si hay muchos — ATOCONGO tiene 96). `gemini.js` extrae `sede`+`equipo` con contexto. `conversacion.js` usa el nuevo resolver (mensajes sede/equipo/código). `escritura.js`+`manta_observaciones` añaden `sede`/`eqId`/`tipo` (+ `tienda`=cliente+sede compat). `manta.js`/`manta_equipos` quedan **deprecados** (el bot ya no los usa). Probado contra inventario real (`migrar_db/test_obs_v2.mjs`, 15/15). **Pendiente:** lado app observaciones.html (desplegables sede→equipo desde inventario + export) |
| 2026-06-20 | **Bot WhatsApp Fase 5 (foto + avisos a supervisores)** — `media.js` descarga la foto vía Graph API; `fotos.js` la guarda pendiente en **`wa_sesiones_fotos`** (doc por nº, tope ~900KB) hasta el "SÍ"; al confirmar, `escritura.js` la escribe en `manta_observaciones_fotos` (dataURL, igual que la app) + `tieneFoto:true`. `avisos.js` notifica 1:1 a `maestros_personal` con flag **`recibeAvisos`** (+ teléfono, no a quien reportó); `enviarPlantilla` (Graph API template) para plantilla "utility" de Meta (`WHATSAPP_TEMPLATE_AVISO`), o texto libre si no hay plantilla; el aviso no rompe el flujo si falla. El handler descarga la imagen y su `guardar` = escribir + notificar. La confirmación/guardado muestran 📷. Probado contra Firestore real (`migrar_db/test_fase5.mjs`, 17/17). Falta para prod: deploy + env vars + setup Meta (número + plantilla) |
| 2026-06-19 | **Observaciones: editor de la guía del bot (`manta_guia`)** + backup — menú de usuario → 🤖 Guía del bot (ADMIN/SUPER_ADMIN): modal lista de temas + modal alta/edición (título, palabras clave, checklist, orden, activo); CRUD sobre `manta_guia` (`guiaList`, `openGuiaPanel`/`saveGuia`/`deleteGuia`). `manta_guia` añadida al backup export/import de `configuracion.html` (`manta_guia.csv`, 8 columnas, `palabrasClave`/`checklist` unidos por `\|`; 22ª colección). Cierra el "editable por admin" de la Fase 4 |
| 2026-06-20 | **Bot fix (auditoría pre-producción): foto huérfana no se adjunta a una observación nueva** — la foto pendiente (`wa_sesiones_fotos`) solo se borraba al confirmar/cancelar; si la sesión expiraba en silencio (TTL 30 min) la foto quedaba y se adjuntaba a la **siguiente** observación (otro equipo). Fix en `conversacion.js`: al inicio del turno, si no hay sesión viva (`!ses`), se descarta cualquier foto pendiente huérfana **antes** de guardar la del turno actual — sin riesgo de perder la foto dentro de una conversación viva (ahí `getSesion` sí devuelve sesión). Regresión cubierta en `migrar_db/test_fase5.mjs` (caso 6). De paso se **restauraron** `test_fase4.mjs` y `test_fase5.mjs`, que el pivot Obs v2 había dejado obsoletos (stubs con esquema `{tienda}`/"roof top" → ahora `{sede}` + eq_id real del `inventario`). Suite del bot en verde: fase4 17/17 · fase5 22/22 · obs_v2 15/15 |
| 2026-06-20 | **Merge develop→main → App Observaciones + bot WhatsApp + Obs v2 EN PRODUCCIÓN** (`8ee7e65`). `observaciones.html` activa en producción (desplegables sede→tipo→equipo desde `inventario`, fotos, export Excel/PDF, editor de `manta_guia`). El backend del bot (`api/whatsapp.js` + `_lib`, Fases 1-5) queda **desplegado pero inerte** hasta cablear Meta (sin env vars el webhook solo responde 401/403). También a prod: flag `recibeAvisos` en el editor de personal + colecciones `manta_*`/`wa_*` en backup. Verificado: `app.multiaire.com.pe/observaciones.html` y `multiaire-peru-app.vercel.app/observaciones.html` → 200, card en el index. **Pendiente go-live del bot:** marcar ≥1 supervisor con `recibeAvisos`, setup Meta, env vars en Vercel |
| 2026-06-20 | **Bot WhatsApp EN VIVO en número real** (+51 972 416 669) — setup Meta completo: app "MultiAire Bot", WABA de producción `4327823754136219`, número registrado en Cloud API, token permanente (Usuario del sistema, no caduca), webhook por API. Env vars en Vercel (Preview/develop). Probado end-to-end por WhatsApp: identifica al técnico, Gemini estructura, resuelve el equipo, conversa y guarda en `manta_observaciones`. (Detalle operativo en memoria [[project_migracion_corporativa]].) |
| 2026-06-20 | **Bot: identificación de equipo por UBICACIÓN** (`equipos.js`) — los técnicos nombran el equipo por su sitio ("el extractor del comedor", "gran volumen 1"), no por "extractor 1/2/3". El `matchEquipo` ahora: (1) **detecta el TIPO** mencionado y restringe el pool a ese tipo (arregla que "extractor …" devolviera un Split); (2) puntúa las palabras de **`area`** (ubicación del inventario, poblada en 545/557) con **+2** (distintivas) vs +1 del nombre; (3) el número del nombre/eq_id pesa +3 (canónico) y el del área +1 (desempata, p.ej. "gran volumen 1"→GRAN VOLUMEN 01). `conversacion.js` muestra **📍 Ubicación** en la confirmación y el `area` en la lista de candidatos; `escritura.js` guarda `area` en la observación (`sesiones.js` borrador añade `area`). Probado contra inventario real (11/11 casos ubicación/tipo/código) + obs_v2 15/15 + fase4 17/17 + fase5 22/22 |
| 2026-06-20 | **Bot: reintento ante rate limit de Gemini** (`gemini.js`) — el free tier de Gemini da **429** (y a veces 5xx transitorios) cuando el técnico manda mensajes muy seguidos; el bot respondía "🤔 No pude procesar eso, ¿puedes repetir?" y obligaba a repetir (síntoma: el MISMO texto fallaba y al reenviarlo funcionaba). Ahora `estructurarObservacion` **reintenta** ante 429/5xx con backoff (1.5s, 3s; 3 intentos) → se recupera solo sin pedir repetir. (Para alto volumen, considerar habilitar billing de Gemini para límites mayores.) |
| 2026-06-20 | **Bot: límite diario de Gemini + robustez de foto** — diagnóstico en vivo: la 1ª prueba con foto falló por **429 cuota DIARIA del free tier** (`gemini-2.5-flash` = solo **~20 req/día**), agotada por las pruebas del día (no era la imagen ni un bug). Arreglos: (1) `GEMINI_MODEL=gemini-2.5-flash-lite` en Vercel (cuota diaria separada y más alta; verificado que resuelve texto **e** imagen igual de bien); (2) `conversacion.js`: si el análisis **con imagen** falla, **reintenta solo con el texto** (la foto igual se adjunta — el caption es la señal principal, Gemini "verla" es opcional). **Recomendación para producción real: habilitar billing en Gemini** (pago por uso, ~fracciones de centavo por observación) → quita el tope diario. |
| 2026-06-20 | **Gemini en tier PAGADO + modelo `gemini-2.5-flash`** — el usuario habilitó **billing** en el proyecto `multiaire-fee43` (AI Studio → Configurar facturación; prepago/Cloud Prepay en PEN — Firebase pasa a Blaze pero a su uso sigue ~$0). Tier pagado activo → sin tope diario. `GEMINI_MODEL=gemini-2.5-flash` en Vercel (calidad/precio con margen; verificado texto+imagen sin 429). |
| 2026-06-20 | **Bot: agregar foto a la última observación** (la olvidó al registrar) — tras guardar, el bot recuerda la obs ~30 min (colección **`wa_ultima_obs`**, doc por nº, TTL). Si el técnico manda una **foto sin texto** justo después → pregunta *"¿La adjunto a tu última observación (X)?"* → **SÍ** la adjunta; o escribe *"agregar foto a la última"* → *"mándame la foto"* y la pega. Si en cambio describe un equipo nuevo, arranca una observación nueva (con esa foto). `escritura.js agregarFotoAObservacion(obsId, foto)` escribe en `manta_observaciones_fotos` + `tieneFoto:true`; `conversacion.js` añade el estado `ADJUNTAR_FOTO` + `manejarAdjuntarFoto`; `sesiones.js` guarda/lee `wa_ultima_obs`; `resumenGuardado` sugiere mandar la foto si se guardó sin ella. Probado contra Firestore real (`migrar_db/test_foto_ultima.mjs`, 8/8) + suite fase4 17 · fase5 22 · obs_v2 15 |
| 2026-06-20 | **Aviso a supervisores incluye la UBICACIÓN** (`avisos.js`) — no mostraba el `area`. `textoAviso` (texto libre) añade "📍 Ubicación: {area}", y la **plantilla** de Meta `nueva_observacion` pasa a **5 variables**: `{{1}}`sede · `{{2}}`equipo · `{{3}}`ubicación · `{{4}}`estado · `{{5}}`detalle (params saneados: sin saltos de línea + placeholder "—" si vacío, que Meta rechaza). **Pendiente:** aprobar la plantilla de 5 vars en Meta + `WHATSAPP_TEMPLATE_AVISO=nueva_observacion` en Vercel |
| 2026-06-20 | **Observaciones: columna UBICACIÓN en la app** (`observaciones.html`) — la app no mostraba el `area`. Nueva columna **Ubicación** en la tabla (entre Equipo y Observación; helper `obsArea(o)` que la deriva del `inventario` por `eqId` si el doc no la trae), hint **📍 Ubicación** en el modal al elegir equipo (`onObsEquipoChange`), `saveObs` persiste `area`, y el **Excel** suma columna UBICACIÓN. *(Nota: el backup CSV de `manta_observaciones` en configuracion.html sigue sin exportar los campos de Obs v2 — sede/eqId/tipo/area —, gap preexistente desde el pivot; pendiente de completar.)* |
| 2026-06-20 | **Observaciones MULTI-CLIENTE** (proyectado al crecimiento: Tottus el próximo año, etc.) — el panel estaba atado a Ripley. El modelo de datos YA era multi-cliente (`inventario.cliente`, `maestros_tiendas.cliente`); se destrabó la UI y el bot. **App (`observaciones.html`):** título genérico ("Manta de observaciones", sin "— Ripley"); selector **Cliente → Sede → Tipo → Equipo** (con UN solo cliente el campo Cliente se autoselecciona y se oculta → UX idéntica a hoy; al entrar otro cliente aparece). `fillClienteSelect`/`applyClienteVisibility`/`onObsClienteChange`; fills filtran por `(cliente, sede)`; `saveObs` guarda `cliente`. **Bot:** `equipos.js` recorta el nombre de cualquier cliente (no solo "RIPLEY") con `sinClientes`, detecta el cliente nombrado (`clienteMencionado`) y `matchEquipo` filtra por `(cliente, sede)` → desambigua sedes compartidas (ej. "tottus plaza norte" vs "ripley plaza norte"); `inventario.js` expone `clientes`; `escritura.js` guarda `cliente`. Probado: con TOTTUS temporal en PLAZA NORTE, "tottus…"→TOTTUS, sin cliente→pregunta. Suite verde (obs_v2 15 · fase4 17 · fase5 22 · foto_ultima 8). **Agregar un cliente = solo cargar su inventario + tiendas; cero cambio de código.** |
| 2026-06-20 | **Observaciones: ver detalle al click + filtro de cliente** (`observaciones.html`) — (1) **click en una fila** abre un modal de **detalle solo-lectura** (`modal-obs-ver`/`openObsVer`): tienda, cliente (si hay >1), equipo, 📍 ubicación, observación completa (caja legible, `white-space:pre-wrap`), estado, fecha, técnico, origen y foto (clic→lightbox), con botón **✎ Editar** para admins (→`openEditObs`). Antes era doble-click directo a editar. La foto-thumb y el botón de acción usan `event.stopPropagation()` para no disparar el detalle. (2) **filtro de Cliente** en la toolbar (`obs-f-cliente`/`populateClienteFilter`/`obsClienteDe`) — oculto con un solo cliente, visible con ≥2 (multi-cliente); en `obsFilter` + `clearObsFilters`. |
| 2026-06-20 | **Observaciones: hora de registro en el detalle + columna REGISTRADA en Excel** (`observaciones.html`) — el modal de detalle (`openObsVer`) muestra **🕐 Registrada** = `createdAt` (timestamp real de guardado) formateado en horario de Lima (`toLocaleString('es-PE',{timeZone:'America/Lima',…})`); el **Excel** suma la columna **REGISTRADA** (helper `fmtReg`) junto a FECHA. *(Backfill: el commit `e3fdc19` no había actualizado el changelog.)* |
| 2026-06-20 | **Observaciones: el filtro de Cliente cascadea al de sedes** (`observaciones.html`) — al cambiar el filtro de Cliente de la toolbar (`onClienteFilterChange`) se **repuebla el filtro de sedes** solo con las del cliente elegido (`populateTiendaFilter(cliente)`), evitando mostrar sedes de otros clientes; con un solo cliente el selector de cliente sigue oculto. *(Backfill: el commit `11c6cc4` no había actualizado el changelog.)* |
| 2026-06-20 | **Backup: `manta_observaciones` exporta/importa los campos de Obs v2** (`configuracion.html`) — el backup CSV omitía `cliente`/`sede`/`eqId`/`tipo`/`area` (gap arrastrado desde el pivot a `inventario`): un export→import perdía sede, código de inventario, tipo, ubicación y cliente de cada observación. Se añaden las columnas **CLIENTE/SEDE/EQ_ID/TIPO/AREA** al export (`mObsHeaders`/`mObsRows`) y al import (`batchWrite('manta_observaciones')`) → round-trip completo, alineado con el Excel de la app (que ya las traía). Retrocompatible: los backups viejos sin esas columnas importan con esos campos vacíos. |
| 2026-06-20 | **Bot: endurecimiento (health-check)** — auditoría del handler + `_lib` (suite verde: obs_v2 15 · fase4 17 · fase5 22 · foto_ultima 8 · avisos 11). Tres fixes de robustez de bajo riesgo: (1) **`maxDuration:30`** del webhook en `vercel.json` (evita que la función se corte y Meta reintente cuando foto+Gemini+avisos en serie tardan varios seg); (2) **`avisos.js`**: si la plantilla de Meta falla (typo en el nombre, pausada, error transitorio) **cae a texto libre** — le llega al supervisor que esté dentro de su ventana de 24 h (antes el aviso se perdía); senders `enviarPlantilla`/`enviarTexto` inyectables para pruebas; (3) **`inventario.js`**: caché con **TTL 5 min** (`_cacheAt`) — un equipo nuevo en `inventario` se ve sin esperar un cold-start de la función. Nuevo `migrar_db/test_avisos.mjs` (11/11: fallback, plantilla-OK, sin-plantilla, no-avisar-a-quien-reportó). |
| 2026-06-20 | **Bot: endurecimiento Tier 2/3** (continuación del health-check). (#4) **`fotos.js`**: la foto pendiente (`wa_sesiones_fotos`) gana **TTL 30 min** (`expiraEn`, validado al leer en `getFotoPendiente`) — defensa por si no se limpió; y **`conversacion.js`** avisa al técnico si la foto excede el tope (~900KB b64) y sigue con la observación sin ella. (#5) **Idempotencia sin pérdida**: `idempotencia.js` añade `liberarMensaje(id)`; `whatsapp.js` envuelve `procesarMensaje` y, si falla **antes de cualquier escritura**, **libera la marca** (`wa_mensajes`) y responde **500** para que Meta reintente (no perder el mensaje); guarda `escrituraIniciada` para NO liberar tras empezar a escribir (evita observación duplicada). (#6) **Diagnósticos**: `avisos.js` loguea cuando hay 0 supervisores con `recibeAvisos`; `firestore.js` envuelve el parseo de `FIREBASE_SERVICE_ACCOUNT` en try/catch con mensaje claro. (Tier 3) **`equipos.js`**: normaliza palabra-número (`uno`→1, …`doce`, excluye `un`/`una`) antes de puntuar → "extractor uno" empareja "Extractor 01". Nuevo `migrar_db/test_tier2.mjs` (9/9). Suite del bot: 82 verde (obs_v2 15 · fase4 17 · fase5 22 · foto_ultima 8 · avisos 11 · tier2 9). **Diferido (Tier 3):** race-condition en `wa_sesiones` (transacción) y multi-cliente misma-sede (para Tottus). |
| 2026-06-20 | **Bot: diferidos cerrados (race-condition + multi-cliente misma-sede).** (1) **`sesiones.js` race-condition**: `guardarSesion` pasa a una **transacción que fusiona el historial** (union por valor) con el doc actual → si dos mensajes del mismo número se procesan casi a la vez, ninguno pierde su texto; el resto del estado es last-write-wins pero se re-deriva del historial en el siguiente turno. Además `getSesion` **borra la sesión expirada al leerla** (antes quedaba en Firestore; con la fusión, una conversación nueva habría heredado su historial). Residual conocido (cosmético, raro): dos mensajes simultáneos pueden generar dos respuestas. (2) **Multi-cliente, misma sede compartida** (`equipos.js`/`conversacion.js`): si una sede existe para **>1 cliente** y el técnico no dijo cuál, `resolverEquipo` devuelve `motivo:'cliente'` y el bot **pregunta el cliente** (en vez de emparejar en silencio con el equipo equivocado); `resolverEquipo` acepta `textoCompleto` para detectar el cliente aunque se diga en una repregunta. Con un solo cliente (hoy: RIPLEY) la rama nunca dispara. Nuevo `migrar_db/test_tier3.mjs` (7/7, con TOTTUS temporal). Suite del bot: **89 verde** (… · tier2 9 · tier3 7). |
| 2026-06-20 | **CI: deploy durable + `workflow_dispatch`.** El secret `VERCEL_TOKEN` (era un token de sesión del CLI que **caduca** → los deploys por Action se romperían al vencer) se reemplazó por un **token de Vercel sin expiración** (creado en el dashboard, scope de la cuenta dueña del proyecto). Además, los workflows `vercel-develop.yml`/`vercel-production.yml` ganan el trigger **`workflow_dispatch`** → se puede **redeployar a mano** (`gh workflow run`) sin un push — útil, p.ej., tras cambiar env vars (como activar la plantilla de Meta). (`workflow_dispatch` queda disponible una vez el workflow está en la rama por defecto.) |
| 2026-06-20 | **Responsive: fixes tras auditoría de los 11 módulos.** Auditados todos contra el estándar `@media(max-width:768px)`; conformes proveedores/comprobantes/observaciones/insumos (incl. cámara y sidebar con toggle ☰). Arreglos: (1) **`inventario_multiaire.html`** — NO tenía breakpoint móvil → agregado `@media(max-width:768px)` completo: `.app` apila (sidebar arriba con `max-height:280px`, tabla abajo a todo el ancho con `.tw{overflow:auto;max-height:65vh}` y thead sticky), header/stats compactos, toolbar y search full-width, modal a 1 columna con botones apilados. (2) **`configuracion.html`** — breakpoint **600→768px** + completado: header compacto (oculta `.logo span`/`.user-name`), `.section-body` padding 20→14px, botones del header full-width, `.import-mode` en columna; y **captura de cámara** en el editor de fotos del personal (botón 📸 `.cam-only-mobile` + `<input capture="environment">`, junto al 📷 de subir). (3) **`itinerario.html`** — header compacto + `.iti-actions` apila los botones (`width:100%`) para que no desborden. Además se eliminó `import_whatsapp.html` (archivo huérfano gitignored, 404, sin enlazar — nunca se desplegó). |
| 2026-06-20 | **Responsive: fix overflow del header de inventario + pulido de las 3 restantes.** Bug reportado (captura del usuario): en `inventario_multiaire.html` el header se **desbordaba horizontalmente** en móvil (el **nombre completo del usuario** `max-width:120px` + las **3 stats** Equipos/Sedes/Tipos eran más anchos que la pantalla → toda la página se corría a la derecha y la info se salía). Fix: en el `@media(max-width:768px)` se **ocultan `.user-name` y `.stats`** (el conteo ya sale en la toolbar; queda solo el avatar) y se reduce padding/gap del header. *(No se usó `overflow-x:hidden` en body: rompe el `position:sticky` del header.)* Pulido del mismo patrón en las otras: **`asistencia_multiaire.html`** y **`mantenimiento_multiaire.html`** (header compacto: oculta `.logo span`/`.user-name`, reduce padding; + `.main`/`.content`/`.tab-view` padding 24/16→12px); **`index.html`** breakpoint **600→768px** (consistencia: un tablet a 601-768px ya no ve el layout de escritorio). Con esto los **11 módulos** quedan parejos en responsive. |
| 2026-06-20 | **Responsive (detalle): el pill del usuario queda círculo en móvil.** Al ocultar `.user-name` en móvil, el `.user-pill` mantenía su `padding:4px 10px 4px 4px` (de cuando estaba el nombre) → se veía como un **óvalo con hueco a la derecha** junto a "← Inicio". Fix: en el `@media(max-width:768px)` se fuerza `.user-pill{padding:3px;border-radius:50%}` (círculo limpio alrededor del avatar de 26px) en `inventario`, `asistencia`, `mantenimiento` y `configuracion`. (Los scaffold-based —proveedores/comprobantes/observaciones— ya lo redondeaban.) |
| 2026-06-20 | **Responsive: fix overflow del header de mantenimiento.** Era el header más cargado (☰, logo, pill de conexión, 📤 WhatsApp, 📄 PDF, avatar, ← Inicio) y se **desbordaba** en móvil (la pill "Conectado · 557 equipos" se enrollaba a 3 líneas y el avatar quedaba cortado). Fix en el `@media(max-width:768px)`: se **oculta `.hpill`** (estado de conexión, secundario) y los botones de export pasan a **icon-only** (📤 / 📄, etiquetas envueltas en `<span class="hdr-lbl">` que se oculta; igual "Guardando..."), + `header{gap:6px;padding:0 8px}`. itinerario/asistencia NO tenían el problema (pill corta "✓ Conectado" y sin botones de export). |
| 2026-06-20 | **Responsive: header homogéneo en los 8 módulos.** `itinerario.html` se había quedado mostrando el **nombre completo del usuario** y el pill ovalado en móvil (en el primer pase solo recibió `.logo span`). Se le agregó `.user-name{display:none}` + `.user-pill{padding:3px;border-radius:50%}`. Verificado por grep que **los 8 módulos** (inventario, asistencia, mantenimiento, itinerario, configuracion, proveedores, comprobantes, observaciones) ahora ocultan el nombre y dejan el avatar como círculo en móvil → header consistente en toda la plataforma. |
| 2026-06-20 | **Responsive: avatar grande + botones cuadrados en el header móvil (estilo `insumos`, pedido del usuario).** `insumos.html` era el único que en móvil **agranda el avatar a 32px** (J centrado, círculo limpio) y usa **botones cuadrados** en el header; las otras 8 apps tenían avatar 26px + back-link píldora. Se alinearon TODAS al estilo de insumos: en su `@media(max-width:768px)`, `.user-avatar/-init/-img{width:32px;height:32px}` + `.user-pill{padding:2px;border-radius:50%}` (avatar grande centrado) y `.back-link{border-radius:8px}` (cuadrado; en `inventario` el ← Inicio es un `<a>` inline → `header a{border-radius:8px!important}`). Aplica a inventario/asistencia/mantenimiento/itinerario/configuracion/proveedores/comprobantes/observaciones. Header móvil 100% homogéneo en toda la plataforma. |
| 2026-06-20 | **Responsive (fix de cascada): avatar de mantenimiento no se centraba.** En `mantenimiento_multiaire.html` el bloque `@media(max-width:768px)` (línea ~205) está **antes** de las reglas base `.user-pill`/`.user-avatar` (línea ~246) → por orden de fuente, la base (misma especificidad, más abajo) **ganaba**, así que el avatar seguía en 26px y el pill conservaba `padding:4px 10px 4px 4px` (asimétrico) → avatar chico y empujado a la izquierda. Fix: las reglas del `@media` se prefijan con **`header`** (`header .user-pill`/`header .user-avatar*`/`header .back-link`) → mayor especificidad, ganan sin importar el orden. (Verificado por grep que **solo** mantenimiento tenía el `@media` antes de la base; los otros 7 lo tienen después y ya aplicaban.) |
| 2026-06-20 | **Responsive: tabla de asistencia desalineada + scroll lateral en itinerario.** (1) **`asistencia_multiaire.html`**: la tabla "Hoy" tiene **7 columnas**; en móvil se ocultan Cargo(2) y Comentario(7) → quedan **5 visibles**, pero la grilla estaba en **4 columnas** (`2fr 1fr 1fr 1fr`) → las celdas se desalineaban ("raras"). Fix: grilla móvil a **5 columnas** (`1.6fr .8fr .8fr .7fr 1.1fr`) + padding lateral reducido. (2) **`itinerario.html`**: había **scroll lateral** en móvil; como el módulo NO tiene tablas (que sí lo necesitarían), se agrega `overflow-x:hidden` a `.main` y `.sidebar` en el `@media` (ambos ya tienen `overflow-y:auto`, así que no rompe el scroll vertical ni el header sticky —que va fuera de `.app`—; no se usó en `body` por eso mismo). |
| 2026-06-20 | **Responsive (varios) + Rendición de Caja: modelo y prompt.** (1) **`observaciones.html`**: la fila de filtros en móvil no era clara → el botón de limpiar pasa de "✕" a "**✕ Limpiar**" y el filtro de fecha gana `min-width:150px` (muestra "dd/mm/aaaa"). (2) **`configuracion.html`**: el editor de personal (`.maestro-item`) **desbordaba** en móvil → `flex-wrap:wrap` + el nombre y los inputs de cargo/teléfono se vuelven flexibles (`.maestro-cargo{width:auto!important}` para vencer el `width:130px` inline del teléfono). (3) **`asistencia_multiaire.html`**: las **tablas secundarias** (Historial 8 col, E/S 6 col, Resumen 4 col) se aplastaban/superponían en móvil → `.table-wrap{overflow-x:auto}` + `min-width` por tabla (720/520/440px) → **scroll horizontal** legible (la tabla "Hoy" sigue reflowada a 5 col). (4) **`comprobantes.html` (Rendición de Caja)**: modelo Gemini por defecto **`gemini-2.5-flash-lite` → `gemini-2.5-flash`** (mejor reconocimiento; tier pagado, sin tope diario) + se agregó **2.5 Pro** como opción; y el **prompt** se reforzó para extraer la **RAZÓN SOCIAL LEGAL** (registrada en SUNAT, junto al RUC, termina en S.A.C./S.A./E.I.R.L.…), **NO la marca/nombre comercial** (antes devolvía la marca). *(La API key sigue siendo la que el usuario ingresa en la app —localStorage `cs_gemini_key`—; no se hardcodea en el front por seguridad: para tier pagado, ingresar la key del proyecto con billing.)* |
| 2026-06-20 | **Rendición de Caja: Gemini por función serverless (reusa la `GEMINI_API_KEY` del bot).** Antes el front llamaba a Gemini con una API key que el usuario pegaba (`localStorage`). Ahora hay **`api/comprobante.js`** que llama a Gemini con la **`GEMINI_API_KEY` del servidor** (la misma del bot de observaciones, tier pagado) — la key **nunca llega al navegador**. Protegido: verifica el **ID token de Firebase** del usuario logueado (`admin.auth().verifyIdToken`, reusa `api/_lib/firestore.js`); valida modelo (flash/pro/flash-lite, `thinkingBudget:0` salvo en Pro); responde el JSON parseado. `comprobantes.html`: `callGeminiForImage` ahora hace POST a `/api/comprobante` con `Authorization: Bearer <idToken>`; se **quitó el input de API key** (nota "usa la clave del servidor") y el gate por key (botón analizar depende solo de que haya imágenes). `vercel.json`: `maxDuration:30` para la función. **Pendiente para PRODUCCIÓN:** `GEMINI_API_KEY` solo está en el entorno **Preview/develop** (donde corre el bot); para que la función ande en `app.multiaire.com.pe` (main) hay que agregar `GEMINI_API_KEY` también al entorno **Production**. **Además: filtro de fecha de observaciones** ahora lleva ícono 📅 + "Fecha:" (la caja vacía no se identificaba en móvil). |
| 2026-06-21 | **Link corto del bot WhatsApp: `app.multiaire.com.pe/bot`.** Redirect en `vercel.json` (`redirects: [{source:/bot → wa.me/51972416669?text=Nueva observación 🛠️, permanent:false}]`) → un link propio/branded para compartir el bot con los técnicos (en vez del `wa.me/...?text=...` largo y feo). El número real es +51 972 416 669; perfil del bot ya con foto (logo MultiAire) e Información; nombre visible `MultiAire Perú Observaciones` enviado a revisión de Meta. `permanent:false` (307) para poder cambiar el mensaje/número sin caché. |
| 2026-06-21 | **Bot: plantilla de avisos `nueva_observacion` ACTIVADA.** Meta aprobó la plantilla "utility" de 5 variables ({{1}}sede {{2}}equipo {{3}}ubicación {{4}}estado {{5}}detalle). Se puso **`WHATSAPP_TEMPLATE_AVISO=nueva_observacion`** en Vercel (Preview/develop) + redeploy (`gh workflow run vercel-develop.yml`) → los avisos a supervisores pasan de texto libre a la **plantilla** (le llega al supervisor aunque esté fuera de la ventana de 24 h). `avisos.js` ya mandaba los 5 params en orden con saneo (sin saltos de línea, placeholder "—") y **cae a texto libre** si la plantilla falla. **Pendiente de verificar EN VIVO:** (a) el **idioma** con que Meta la aprobó — el código usa `es` (override `WHATSAPP_TEMPLATE_IDIOMA`); si fue `es_LA`/`es_PE`/`es_MX` hay que setear esa var o el envío falla (error 132001) y cae a texto libre; (b) **método de pago** en la cuenta de WhatsApp Business — necesario para entregar plantillas fuera de la ventana de 24 h. |
| 2026-06-21 | **Nota (sin fix): login en producción con Brave → `auth/popup-blocked`.** Al entrar a `app.multiaire.com.pe/observaciones.html` con Brave, el login con Google falla con `auth/popup-blocked` — **Brave bloquea el popup** (`signInWithPopup`). **NO es el dominio**: `app.multiaire.com.pe` está autorizado en Firebase Auth (si fuera dominio sería `auth/unauthorized-domain`). Workaround: permitir popups del sitio en Brave, o usar Safari/Chrome. **Fix opcional DIFERIDO** (decisión del usuario): migrar el login a `signInWithRedirect` (más robusto para Brave y el navegador in-app de WhatsApp en móvil) — cada página define su `window.__fb` con `signInWithPopup`. |
| 2026-06-21 | **Bot: avisos por plantilla a supervisores COMPLETOS y EN PRODUCCIÓN (verificado en vivo).** Cerrado el ciclo de la plantilla `nueva_observacion`: idioma confirmado `es` ("Spanish" en WhatsApp Manager, coincide con el default del código → sin `WHATSAPP_TEMPLATE_IDIOMA`); **método de pago agregado** en la cuenta de WhatsApp Business (tarjeta Visa, info fiscal MULTIAIRE PERU S.A.C. / RUC `20613186396`, divisa PEN) → habilita entregar plantillas fuera de la ventana de 24 h. **Prueba end-to-end OK:** el aviso llegó al supervisor con formato de plantilla. **Decisión de costos del usuario:** avisos **instantáneos** (sin batch/digest ni email) — el costo es de centavos/mes (~S/0.05–0.11 por aviso "en frío"; gratis dentro de la ventana de 24 h). Freno disponible (gratis, reversible): quitar `WHATSAPP_TEMPLATE_AVISO` → el bot vuelve a texto libre. Recordatorio: el bot NO avisa a quien reporta. |
| 2026-06-21 | **Merge develop→main → changelog en producción** (`8b8a73d`). Merge `--no-ff` (convención del repo). Solo documentación: `CLAUDE.md` (+3 líneas con la activación/verificación en vivo de la plantilla `nueva_observacion` y la nota del login Brave). Lo funcional (responsive de los 11 módulos, Rendición de Caja serverless, link corto del bot) ya estaba en producción desde el merge previo `0208f16`; la config de hoy (env var `WHATSAPP_TEMPLATE_AVISO` en Vercel + método de pago en Meta) vive fuera del repo. Deploy de producción OK, `app.multiaire.com.pe` → 200. |
| 2026-06-21 | **Header: botones cuadrados en TODOS los anchos (no solo móvil) + fix scroll lateral del sidebar en tablet/iPad.** (1) El `.back-link` (← Inicio) tenía `border-radius:20px` (píldora) en base y solo se cuadraba en el `@media(max-width:768px)`; el usuario quería el estilo cuadrado (como `insumos`, que usa `btn btn-sm`) **en todo ancho**. Se pasó `.back-link{border-radius:20px→8px}` en base en los 7 módulos que lo usan (asistencia, comprobantes, configuracion, mantenimiento, itinerario, proveedores, observaciones) + el `<a>` inline de `inventario` (insumos ya era cuadrado). (2) **Scroll lateral del sidebar en iPad**: `.sidebar{overflow-y:auto}` hace que `overflow-x` compute a `auto` → el sidebar se volvía contenedor de scroll horizontal (cualquier sub-pixel de desborde daba barra/arrastre lateral en touch). Fix: `overflow-x:hidden` explícito en el `.sidebar` de los 5 módulos con layout flex (asistencia, inventario, itinerario, mantenimiento, insumos); además `min-width:0` en `.main` de asistencia e itinerario (los que no tienen `overflow:hidden`) para que el contenido no empuje la `.app` más ancha que el viewport. (No se usó `overflow-x:hidden` en `body` — rompe el `position:sticky` del header.) |
| 2026-06-21 | **Asistencia: pulido del sidebar en iPad** (input de fecha que sobresalía + ancho + botones parejos). (1) **`<input type=date>` nativo de iOS**: ignoraba `width:100%` y se dimensionaba a su contenido (sobresalía del sidebar). Fix: `.sb-input[type=date],[type=time]{-webkit-appearance:none;appearance:none;text-align:left}` → respeta el 100% y queda alineado a la izquierda. (2) **Sidebar más angosto**: `.sidebar` 300→270px (da más aire a la tabla; queda en la familia de 240–280 de los otros módulos). (3) **Botones de estado especial parejos**: el contenedor pasó de `flex;flex-wrap` (anchos dispares Desc.médico/Permiso/Falta) a `grid;grid-template-columns:repeat(3,1fr)` → 3 botones de igual ancho. |
| 2026-06-21 | **Auth: `authDomain` propio `app.multiaire.com.pe` (login de Google profesional, sin verificación de marca).** Antes el popup de Google mostraba `multiaire-fee43.firebaseapp.com` (el `authDomain` por defecto) porque la **marca OAuth está sin verificar** y Google cae al identificador técnico. En vez de pasar por la verificación de marca (pide política de privacidad + dominio verificado en Search Console + revisión de Google), se replicó el patrón de **clima-frio**: `authDomain` = el propio dominio + **proxy del auth handler en Vercel**. (1) `firebase-config.js`: `authDomain` `multiaire-fee43.firebaseapp.com`→**`app.multiaire.com.pe`** (config compartido; todas las páginas lo cargan; `index.html` no usa Firebase). (2) `vercel.json` **rewrites**: `/__/auth/:path*` y `/__/firebase/:path*` → `https://multiaire-fee43.firebaseapp.com/...` (Vercel hace de fachada del handler). `app.multiaire.com.pe` ya está en Authorized domains de Firebase. **OJO (despliegue):** con un solo `authDomain` para todo, el login solo funciona cuando **producción** (`app.multiaire.com.pe`) tiene los rewrites → no se puede probar el login completo solo en develop; verificado `/__/auth/handler`→200 en develop y luego en prod tras el merge. **(3) Paso que faltaba (salió `Error 400 redirect_uri_mismatch` al probar):** con el dominio propio el redirect_uri es `https://app.multiaire.com.pe/__/auth/handler` → hay que **registrarlo en el cliente de OAuth** (GCP → APIs y servicios → Credenciales → "Web client (auto created by Google Service)" → URIs de redireccionamiento autorizados) + agregar `https://app.multiaire.com.pe` (sin ruta) en Orígenes de JavaScript. Hecho. (Propagación de Google: 5 min a algunas horas.) Ahora el popup muestra "continuar en app.multiaire.com.pe". |
| 2026-06-21 | **Itinerario: trazabilidad de versiones (bug — solo se veía la última versión).** Cada versión se guarda como doc aparte (`bd_itinerarios` id `ITI-…_vN`, bloques con campo `version`), pero la **lectura** siempre tomaba la más nueva: la lista "Recientes" mostraba todas las versiones (`v1`/`v2`/`v3`) pero el clic llamaba `loadItiById(id, fecha)` **sin la versión** → `loadFecha` ordenaba desc por versión y tomaba `matching[0]` (la última). Por eso al hacer clic en "v1" se veía la "v3". Fix: (1) nueva función **`loadItiVersion(id, version)`** que filtra por `id + version` exactos (itinerario y bloques) y renderiza esa versión; el clic en Recientes ahora pasa `${iti.version}`. (2) **Selector de versiones en el header del detalle** (chips `v1 · v2 · v3`, la actual marcada) para saltar entre versiones desde cualquier vista — aparece solo cuando hay >1 versión del mismo `id`. Global **`allItis`** (todas las versiones) que refrescan `loadFecha` y `loadItiVersion`. El selector de fecha sigue mostrando la última versión por defecto (sensato); desde ahí se navega a versiones previas con los chips. Solo lectura/render — no cambia cómo se guarda. |
| 2026-06-22 | **Bot: la repregunta de detalle ahora es consciente del TIPO de equipo** (`gemini.js`). Reportado en vivo: a *"ripley atocongo cortina de aire 1 no enfría"* el bot preguntó *"¿El compresor arranca o hace ruido anormal?"* — pero una **cortina de aire es ventilación y NO tiene compresor**. La repregunta la genera Gemini desde la guía genérica (`manta_guia`: filtros/refrigerante/compresor/…) y no consideraba el tipo. Fix de **prompt**: se instruye que la pregunta sea pertinente al tipo — cortinas de aire/extractores/inyectores/UMA son ventilación (preguntar por motor/ventilador/flujo/ruido, nunca compresor/gas); splits/chillers/fan coils/paquetes sí pueden tener compresor/gas; si la guía no encaja, pregunta genérica o `faltaDetalle=false`. Solo prompt (no toca lógica ni el guardado); `node --check` OK. |
| 2026-06-25 | **Importación de asistencia mayo–junio desde WhatsApp** (`asistencia_registros`, período 2026-05-25→2026-06-24). **292 registros nuevos** cargados (330 en total con los 38 previos): 184 de 8 técnicos (PER001/002/003/005/008/009/010/011), 27 de **Jefferson (PER017563) = Descanso Médico** L–S, y 81 de los 3 administrativos (Andrés/Leslie/Marchena, **L–V 8:30–18:00 · Sáb 8:30–13:00**). **PER006 (Segundo Inuma) y PER007 (Luis Chávez) NO se tocaron** (carga manual previa del usuario + Permiso del 15/06 intactos). **Luis Ampuero (PER004) ya está `activo:NO`** → excluido; Adrián Olaya y Daniel multiaire fuera (no son personal de la plataforma). **Reglas de cálculo** (cerradas con el usuario): redondeo a la media hora más cercana *half-up* (min 15→:30, 45→:00 sig.); por día entrada=1ª foto de llegada/inicio/ingreso, salida=última de salida; **marca faltante** (solo entrada o solo salida) → se completa con la jornada base (9.5h L–V / 4.5h Sáb) para que el día quede en **HE 0** (ni suma ni resta); días laborables sin ninguna foto → **NO** se registran (no se marca falta; se entregó reporte de ausencias). HE las calcula la app (`heManual:false`, `horasExtra:null`). Clasificación de fotos hecha con **agentes en paralelo** leyendo cada caption (typos extremos *Sañida/Slaida/Sñaida/Salisa*, "Inicio"=entrada, fotos nocturnas sin texto = ruido ignorado); 0 horas inventadas (validadas contra el chat); método verificado contra la carga manual de PER006/PER007 (reproduce idéntico tras redondeo). Scripts en `~/Documents/migrar_db/cargar_asistencia_mayjun.js` (upsert idempotente, guardas que abortan fuera de ventana y protegen PER006/PER007). |
| 2026-06-25 | **Asistencia jun: ajustes de vacaciones/ausencias indicados por el usuario** (no hay estado "Vacaciones" en el sistema → se usa **estado `P` (Permiso) con `observacion:"Vacaciones"`**). (1) **Segundo Inuma (PER006) y Luis Chávez (PER007)**: vacaciones del **15/06 al 27/06** (L–S, 12 días c/u; el Permiso del 15/06 que ya existía se conserva — mismo `registradoPor` — y solo se le ajusta la obs a "Vacaciones"). (2) **Orlando Mera (PER002)**: la **semana del 1–6/06** (ausencia) = vacaciones (estado P); fue solo esa semana (desde el 08/06 vuelve a marcar). (3) **Hernán Zavaleta (PER009)**: sus 4 días sin foto (02,11,17,24/06, todos L–V) → **jornada estándar 8:30–18:00** (estado A, HE 0, obs "Jornada estándar (sin registro de fotos)"). Scripts `~/Documents/migrar_db/registrar_vacaciones_jun.js` y `ajustes_jun.js` (upsert idempotente). (4) **Completados con jornada estándar** (8:30–18:00 L–V / 8:30–13:00 Sáb, estado A, HE 0; `ajustes2_jun.js`): **Orlando** (25/05, 15/06), **Rafael Santos** (04, 05, 19, 20/06) y **Mattiw** (18/06) → los tres quedan con sus 27 días laborables completos. (5) **José Vivar (PER011)** completado (`ajustes3_jun.js`): 17/06 → **Falta (estado F)**; los otros 5 días (27/05, 29/05, 30/05, 06/06, 13/06) → jornada estándar (sábados a media jornada) → 27 días completos. (6) **Jesús Martínez (PER010)** completado (`ajustes4_jun.js`): sus 9 días sin foto → jornada normal (8:30–18:00 L–V; 13/06 y 20/06 sáb → 8:30–13:00) → 27 días completos. **COBERTURA TOTAL:** los 14 colaboradores con sus 27 días laborables de la ventana 25/05–24/06 (378 registros = 14×27): A=326, DM=27 (Jefferson), P=24 (vacaciones), F=1 (Vivar 17/06); + las vacaciones de PER006/PER007 hasta el 27/06. |
| 2026-06-25 | **Sistema de roles centralizado (`auth-guard.js`) en los 9 módulos.** Nuevo helper compartido **`window.MA`** (`auth-guard.js`, cargado tras `firebase-config.js` en cada página) que unifica la política de permisos, antes dispersa en `if`s por archivo. API: `MA.setUser(maUser)`, `MA.can(accion, app)`, `MA.canEnter(app)`, `MA.isAdmin()`, `MA.isSuperAdmin()`. Diseño **fail-closed** (acción desconocida → denegada). **Modelo de 4 roles** (acordado con el usuario): **SUPER_ADMIN** (hardcodeado por email) = todo; **ADMIN** = crea/edita/aprueba/exporta lo operativo pero NO borra/backup/importación masiva/gestión de usuarios/configuración (solo super); **SUPERVISOR** = opera (crea/edita/aprueba/exporta) SOLO en las apps de su `apps[]`; **TECNICO** = solo lectura. Acceso **híbrido**: el rol da el nivel base y `apps[]` controla a qué módulos entra cada usuario (gate de entrada en `initApp`/auth de cada página). El panel "Gestión de accesos" pasa a ser **solo SUPER_ADMIN** (antes ADMIN). **insumos** conserva su tabla `PERMS` (modelo de dominio con flujo solicitud→aprobación); solo se alinearon bordes (entrada por `apps[]`, eliminar/importación masiva→super, exports→`exportar`, panel→super). **configuracion** es **solo super admin** (no carga datos si no es super). **comprobantes** (herramienta de captura sin persistencia) exige `MA.can('editar','comprobantes')` para entrar. Auditado de punta a punta con Codex (flujo Council, `/auditar`) por tandas: atrapó un *fail-open* de ADMIN, escritura abierta en comprobantes, `delItem`/`delSel` de inventario sin gate, un `sync()` que cargaba datos antes del gate de acceso, un bug runtime preexistente (`movData_extra` en TDZ en `saveSolicitud`) y ~25 funciones de escritura/export de insumos sin guard interno — todos corregidos. **NOTA pendiente (Carril B):** no hay `firestore.rules`; el helper es control de UX, no seguridad server-side. |
| 2026-06-25 | **Roles — pulido de UX tras prueba en vivo (TECNICO).** Dos arreglos detectados probando con un técnico real (apps `insumos`/`inventario`): (1) **Pantalla "Sin acceso" uniforme** — al entrar a una app sin acceso, el mensaje se escribía en un `auth-overlay` que arrancaba `display:none` → la página quedaba "conectando" sin avisar. Nueva **`MA.showNoAccess(seccion)`** en `auth-guard.js`: overlay full-screen (z-index 99999) "🔒 Sin acceso a \<sección\>" + botón "← Volver al inicio" (sección escapada para `innerHTML`); los **9 módulos** la usan en su gate de entrada. (2) **inventario**: el botón "+ Nuevo equipo" y los de exportar se mostraban a un no-editor (solo la *acción* estaba gateada) → `openNew()`/`openExport()` ganan guard `MA.can('editar'/'exportar','inventario')` y los botones (`btn-new-eq`/`btn-pdf-eq`/`btn-lbl-eq`) se ocultan en `maVerify` según permiso (el super admin los sigue viendo). Auditado con Codex (Council). *(Pendiente análogo: en `mantenimiento`/`itinerario` los botones de acción aún se muestran a no-editores; el dato real solo afecta si se asigna esos módulos a un técnico — la acción ya está gateada.)* |
| 2026-06-25 | **Roles — pulido de UX en mantenimiento/itinerario/inventario + limpieza de textos legacy.** Se completó el patrón "TECNICO ve datos pero sin botones de acción" en los 3 módulos restantes, con **clases en el `<body>`** (añadidas en `maVerify` según `MA.can(...)`) + CSS: **mantenimiento** `.mant-readonly` (oculta `.mant-edit-btn` —editar equipos— y desactiva `.tarea-cb`), export condicionado; `openSelEquipos()` con guard. **itinerario** `.iti-readonly`/`.iti-noexport`/`.iti-nodel` (ocultan `.iti-edit` —Guardar/Crear/Editar/Agregar bloque—, `.iti-export` —PNG/PDF/WhatsApp—, `.iti-del` —Eliminar—); `nuevoItinerario`/`editarItinerario` con guard `editar`, `openDeleteModal` con `borrar`. **inventario** `.inv-readonly`/`.inv-noexport`/`.inv-nodel`/`.inv-nobulk` (ocultan el botón Nuevo, los de exportar, el Eliminar masivo y —para quien no exporta ni borra— toda la selección masiva: botones Todo/Ninguno, `#selbar` y los checkboxes de header y fila). **Limpieza de stack legacy:** inventario ya migró de Google Sheets a Firestore (`gasGet`/`gasPost` usan `collection('inventario')`); el botón "Sincronizar Sheets" pasó a **"↻ Recargar"** y se corrigieron todos los textos visibles que decían "Google Sheets" (botón "Guardar en Sheets"→"Guardar equipo", mensajes de carga/error en inventario y mantenimiento). Auditado con Codex (Council). |
| 2026-06-25 | **Carril B — `firestore.rules` con el modelo de roles SERVER-SIDE (desplegado).** Hasta ahora el control de acceso era solo UX (`auth-guard.js`); las reglas de Firestore estaban en modo inseguro: `usuarios` con **lectura pública** y **escritura para cualquier autenticado** (→ cualquier cuenta Google podía auto-asignarse `SUPER_ADMIN` usando el `firebase-config.js` público), y el resto read/write para cualquier autenticado. Nuevas `firestore.rules` (en el repo + `firebase.json` + `.firebaserc`): solo personal con doc en `usuarios` accede; **`usuarios`/maestros/config/`manta_guia`/borrados = solo SUPER_ADMIN**; datos operativos crear/editar por **rol + `apps[]`** (admin/super siempre, supervisor en sus apps, técnico solo lee); colecciones del bot `wa_*` no escribibles desde el cliente (el bot usa Admin SDK, que ignora las reglas); catch-all `read:authorized`/`write:false` (en Firestore los `match` se combinan con OR — un catch-all permisivo anularía las reglas granulares). El **super admin por email** (`marchenaangulojoseluis@…`) tiene acceso aunque su doc aún no exista. Validadas con el **emulador** (`@firebase/rules-unit-testing`, 25/25), **auditadas por Codex** (APROBADO) y **probadas contra Firestore REAL** con custom tokens (6/6: admin entra y lista usuarios, técnico solo lee, cuenta ajena no accede ni escala). Desplegadas vía Admin SDK; rollback guardado. **Caveat:** el flujo de "solicitud" del técnico en insumos queda bloqueado server-side (no se usa hoy; ajustar reglas si se implementa). *(Las reglas aplican al proyecto Firestore compartido por develop y producción — no hay reglas "solo de develop".)* |
| 2026-06-26 | **Roles — garantía de que SIEMPRE exista un super admin + dos supers del sistema.** Para que nunca pueda haber cero super admins: `auth-guard.js` define **`SUPER_ADMIN_EMAILS`** = `marchenaangulojoseluis@gmail.com` + **`plataforma@multiaire.com.pe`** (dos supers "del sistema": SUPER_ADMIN por código pase lo que pase en su doc, e intocables). Nueva **`MA.puedeQuitarSuper(email, users)`** que bloquea degradar/eliminar a un super del sistema o si dejaría 0 supers efectivos; aplicada en los 9 paneles de Gestión de accesos (`changeUserRol`/`maChangeRol` al degradar y `deleteUser`/`maDeleteUser`/`deleteAccessUser`); las vías legacy (`changeUserRol`/`removeUserAccess`/`maSaveUsers`) protegen a los system supers con `MA.isSystemSuper`. `firestore.rules`: `isSuperEmail()` incluye ambos emails (super server-side aunque el doc diga otra cosa) — reglas redesplegadas. Auditado con Codex (Council, APROBADO). Validado en vivo en develop con un técnico real. |
| 2026-06-26 | **Nueva app Personal (`personal.html`) — Expediente del Colaborador 360°, Fase 1.** Módulo dedicado (scaffold de `proveedores.html`) que reemplaza el editor de personal plano de `configuracion.html`, sobre la misma colección `maestros_personal` (sin colección nueva en F1). Amplía el doc con identidad (tipoDoc/numDoc/nacimiento/género/nacionalidad/estado civil/foto), **contacto multi-país** (`telefonos[]` con catálogo de países —PE +51 default, CL +56— + `correos[]` + dirección + contacto de emergencia), laboral (área/estado laboral/ingreso-cese/contrato/sede base/**jefeId** jerarquía/recibeAvisos) y operativo (rol/**competencias[]**/licencia/vehículo). **Bot-safe:** mantiene el campo legacy `telefono` = e164 del marcado WhatsApp → `identidad.js` (últimos 9 dígitos) intacto; lectura migra el `telefono` plano viejo a `telefonos[]`. Escritura `{merge:true}` (no pisa otras fases). Roles `isPersAdmin()` (ADMIN/SUPER editan, SUPERVISOR por `apps[]`, TECNICO lectura; borrar solo super). `personal` agregado a `ALL_APPS` de los 8 módulos + card 👥 en index (antes de Configuración). Export Excel. Sintaxis JS validada (`node --check`). **Activa en develop, sin merge a main.** **`configuracion.html` reconciliado en el mismo lote:** `saveMaestros` deja de hacer delete-all+recrear (ahora borra solo los colaboradores eliminados + `set {merge:true}` → preserva el expediente) y el backup CSV de `maestros_personal` gana columna **`JSON`** (doc completo) en export+import → restauración fiel; backups viejos compatibles. **Pendiente (fases siguientes del diseño):** `personal_confidencial`(sueldo, solo ADMIN)+rules, timeline `personal_eventos`, legajo en Drive + certificaciones + alertas, catálogos `cat_*`, `identidad.js`→E.164, bot de asistencia geolocalizada. Diseño completo en memoria `project_expediente_personal`. |
| 2026-06-26 | **Personal — fixes de la auditoría Codex (Council) antes de subir.** El veredicto fue RECHAZADO; corregidos: (1) **Roles alineados UI↔reglas** — decisión del usuario: editar personal = **ADMIN+SUPER** (no supervisor). `personal.html` `isPersAdmin()` pasa a `MA.isAdmin()`; **`firestore.rules`** `maestros_personal` → `create,update: if isAdmin(); delete: if isSuper()` (antes solo super) — **requiere redeploy de reglas** (pendiente, ver nota). (2) **Bot-safe** — el `telefono` legacy se deriva del teléfono marcado **principal** (radio WhatsApp) y no del primer `tipo:whatsapp`; aviso al guardar un activo sin teléfono. (3) **Editor viejo** — el campo teléfono de `configuracion.html` pasa a **solo-lectura** (gestionar en Personal) → no desincroniza `telefono`↔`telefonos[]`. (4) **Backup import** — `batchWrite` acepta `{merge:true}` y la importación de `maestros_personal` lo usa → en modo Fusionar con backups viejos (sin `JSON`) ya **no pisa** los campos del expediente; JSON inválido se loguea. (5) **`nextPersId()`** basado en timestamp (estilo PER017563 existente) + dedup → sin colisión por correlativo. (6) **Migración multi-país** — `migrarTelLegacy` detecta el prefijo por el más largo conocido (51/56/593…), ya no fuerza Perú. Quedan como **deuda transversal de toda la plataforma (no de este diff):** XSS por `innerHTML`/`onclick` y el patrón de super-del-sistema sin doc en `maVerify` (idénticos en los 9 módulos). Sintaxis revalidada (`node --check`). **Redeploy de `firestore.rules` pendiente** (afecta el proyecto Firestore compartido develop+prod) — hacerlo junto al go-live; mientras, en develop solo el super puede guardar personal. |
| 2026-06-26 | **`firestore.rules` desplegado a prod** (ruleset `d72e8a7e`) vía Admin SDK (`securityRules().releaseFirestoreRulesetFromSource`, service account de `migrar_db` — el firebase CLI tenía el login caducado). `maestros_personal` ahora permite escribir a ADMIN (borrar solo super). Backup del ruleset previo (`fe5cfda5`) en `/tmp/firestore.rules.live-before-deploy` + rollback del repo en `/tmp/firestore.rules.rollback`. Verificado: reglas vivas == `firestore.rules` del repo. |
| 2026-06-26 | **Editor de personal RETIRADO de `configuracion.html`** (decisión del usuario: una sola fuente de verdad = `personal.html`). Se quitó el tab+panel "Personal" (Tiendas pasa a ser el tab activo por defecto), la carga de `maestros_personal` en `loadMaestros`, el bloque `b1` de `saveMaestros` (ya no escribe personal — guarda solo tiendas/grupos/feriados) y las ~105 líneas de funciones del editor (`renderPersonal`/`addPersonal`/`setPersonalFoto`/`compressSquare`/`updatePersonal`/`togglePersonal`/`toggleAviso`/`deletePersonal`/`normalizeTel`). **El backup/restore de `maestros_personal` se conserva intacto** en configuracion (export con columna JSON + import + `expected`). 0 referencias colgantes, sintaxis revalidada. (Helper `cargoBadgeHtml` y el `personal:[]` del init se eliminaron en el pulido posterior.) |
| 2026-06-26 | **Personal — régimen de asistencia por colaborador** (`personal.html`, módulo Laboral). Campo **`regimenAsistencia`** con 3 valores: **`MARCA`** (marca entrada/salida — técnicos/ayudantes, vía bot/app), **`FIJO`** (horario fijo, no marca → se completa solo) y **`NO_MARCA`** (sin control — gerencia/externos). Para `FIJO`, **horario configurable por persona**: `horarioFijo:{entrada,salida}` en decimal (8.5=08:30, como `asistencia_registros`); inputs `<input type=time>` precargados a la jornada estándar 8:30–18:00, editables (p.ej. José Marchena 11:00–18:30, que NO encaja en la estándar). Helpers `hhmmToDec`/`decToHHMM`; el bloque de horario solo se muestra para `FIJO` (`togglePersHorarioFijo`). **Esto saca de un script de Python el hardcodeo de horarios fijos** (antes: Leslie/Andrés/Marchena fijos a mano en `wsp_import`): ahora el régimen es DATO del expediente que leerán el módulo de Asistencia y el bot geolocalizado — a `MARCA` les pide marcación, a `FIJO` los completa con su horario, a `NO_MARCA` los excluye del control/ausencias. Default `MARCA`. Sintaxis validada. **Pendiente:** generar automáticamente los registros diarios de los `FIJO` (hoy entran por importación) y exponer el régimen en el Excel del directorio + la futura columna del módulo Asistencia. |
| 2026-06-26 | **Personal — responsive móvil del modal del expediente** (`personal.html`), **verificado visualmente a 390px** (harness del modal renderizado en Chrome). Bug: los `.form-group` con ancho fijo inline (`flex:0 0 Npx` — fila de Asistencia, Identidad, Laboral) y los selects de los repetidores **no se estiraban** al apilar (el `flex-basis` gana sobre `width:100%`). Fix en `@media(max-width:768px)`: `.form-group{flex:1 1 auto!important;min-width:0}`, `.rep-row .rep-pais/.rep-num/.rep-tipo/.rep-email/.rep-ctipo{flex:1 1 100%!important;width:100%}`, `.modal{padding:16px}`, `.fieldset{padding:12px}`, y **ocultar columnas secundarias** (Documento·Área·Correo, `nth-child(4/6/8)`) de la tabla en móvil (+ scroll-x). Confirmado: Identidad/Contacto (teléfonos y correos apilados)/Laboral (incl. Asistencia + inputs `<time>`)/Operativo apilan a ancho completo y alto natural. |
| 2026-06-26 | **Personal F2 — sueldo confidencial** (`personal.html` + `firestore.rules` + backup). Nueva colección **`personal_confidencial`** (doc id = `personalId`): `sueldo`, `moneda` (PEN/USD), `periodicidad` (mensual/quincenal/semanal/por hora/por día/RxH), `banco`/`cuentaNumero`/`cci`, `notas`. **UI:** fieldset 🔒 "Confidencial · solo ADMIN" que **se muestra/carga/guarda SOLO si `MA.isAdmin()`** (supervisor/técnico no lo ven). `loadConfidencial`/`saveConfidencial` leen/escriben la colección **aparte** con `{merge:true}` (no tocan `maestros_personal`); se guarda tras el set de `maestros_personal` en ambas ramas (alta/edición). **`firestore.rules`:** `personal_confidencial` → `read, write: if isAdmin()`. **OJO — lo atrapó la auditoría Codex (RECHAZADO en 1ª ronda):** una regla granular `read:if isAdmin()` NO basta, porque el **catch-all** `match /{document=**}` con `read:if authorized()` la **sobre-permite por OR** (cualquier autorizado leería el sueldo; Firestore no tiene "deny"). Fix: el **catch-all pasa a FAIL-CLOSED** (`read:false, write:false`) y se declaran explícitas las `wa_*` del bot (`read:authorized, write:false`) que antes dependían de él (verificado que ningún módulo cliente lee otra colección no listada). De paso se retiró `'insumos_stock'` (colección legacy inexistente) de `resetearBaseDatos()` en `insumos.html`, que con fail-closed habría abortado el reseteo (hace `.get()` de cada colección). **Backup:** al export/import de `configuracion.html` (columna `JSON` con el doc completo + columnas legibles SUELDO/MONEDA/… + `expected`, 23ª colección; doc id por `_docId`). Sintaxis validada. **`firestore.rules` desplegado a prod** (ruleset `61234d3a`, con el catch-all fail-closed). |
| 2026-06-26 | **Personal F3 — timeline de eventos** (`personal.html` + `firestore.rules` + backup). Nueva colección **`personal_eventos`** (N docs por persona, doc id auto, con `personalId`): `tipo` (catálogo `TIPOS_EVENTO`: INGRESO/ASCENSO/CAMBIO_CARGO/AUMENTO/FELICITACION/AMONESTACION/SANCION/VACACIONES/LICENCIA/DESCANSO_MEDICO/CAPACITACION/ACCIDENTE/CAMBIO_DATOS/BAJA/OTRO), `fecha`, `titulo`, `detalle`, `createdAt/By`. **UI:** fieldset 📅 Historial en la ficha (solo al **EDITAR** un colaborador existente) que lista eventos (más reciente primero) + sub-modal `modal-evento` "Nuevo evento" (`openNuevoEvento`/`saveEvento`); `loadEventos(id)` hace query por `personalId`. **Append-only:** no se editan; agregar = `isPersAdmin` (ADMIN); borrar solo super (`deleteEvento`, `MA.can('borrar')`). **`firestore.rules`:** `personal_eventos` → `read: isAdmin` (sensible, incluye sanciones), `create,update: isAdmin`, `delete: isSuper`. **Backup:** al export/import de `configuracion.html` (ID/PERSONAL_ID/TIPO/FECHA/TITULO/DETALLE/JSON + `expected`, 24ª colección; doc id por `_docId`). Sintaxis validada. **`firestore.rules` desplegado a prod** (ruleset `dddb063a`). |
| 2026-06-26 | **Personal F4 — legajo + certificaciones** (`personal.html` + `firestore.rules` + backup). Nueva colección **`personal_documentos`** (N docs/persona, doc id auto, `personalId`) que **unifica** documentos y certificaciones vía `categoria` (DOCUMENTO/CERTIFICACION): `tipo` (datalist por categoría — contrato/DNI/EMO/SCTR/altura/eléctrico/licencia…), `nombre`, `entidad`, `fecha`, **`vence`** (opcional), **`url`** (enlace a Drive — el archivo vive en Drive, la app guarda el link; NO hay subida automática desde el cliente Firebase: se sube a Drive vía web o `rclone cfdrive` y se pega el enlace), `createdAt/By`. **Alertas de vencimiento:** `estadoVencimiento(vence)` → badge VIGENTE / POR VENCER (≤30 días) / VENCIDO por documento en la ficha. **UI:** fieldset 📎 "Legajo y certificaciones" (solo ADMIN, al editar) + sub-modal `modal-doc` (`openNuevoDoc`/`saveDoc`); `loadDocs(id)` query por `personalId`. Agregar=ADMIN, borrar=super (`deleteDoc`). **`firestore.rules`:** `personal_documentos` read/create/update=isAdmin, delete=isSuper. **Backup:** export/import (ID/PERSONAL_ID/CATEGORIA/TIPO/NOMBRE/ENTIDAD/FECHA/VENCE/URL/JSON + `expected`, 25ª colección). Sintaxis validada. **Pendiente:** redeploy de `firestore.rules` (regla de `personal_documentos`); y a futuro, **alertas proactivas** (indicador de vencimientos en el directorio + aviso por WhatsApp, requieren un cron/backend). |
| 2026-06-26 | **Bot: `identidad.js` a E.164 (multi-país) — F5 del expediente** (`api/_lib/identidad.js`). `identificarTecnico` hace ahora **2 pasadas**: (1) match **EXACTO por E.164** contra `telefono` legacy y cada `telefonos[].e164` (preciso; distingue países aunque compartan los últimos 9 dígitos); (2) **fallback por últimos 9 dígitos** (preserva el comportamiento histórico). Lógica extraída a **`matchTecnico(docs, waId)` pura/testeable** (sin Firestore) + `clavesDe`/`soloDigitos` exportadas; lee `telefonos[]` del nuevo expediente además del `telefono` legacy. **Compatible:** los datos actuales (telefono peruano sin `telefonos[]`) se identifican igual. Verificado con test unitario (8/8: peruano legacy/sin-prefijo, chileno e164 que NO confunde, exacto>fallback, inactivo, sin-match, vacío). **En develop; el bot productivo (`main`) NO se toca hasta un merge.** La otra parte de F5 (**catálogos `cat_*` editables**) queda **diferida**: bajo valor a esta escala (≈15 personas); los `datalist` sembrados de valores existentes + texto libre ya bastan. |
| 2026-07-01 | **Bot de ASISTENCIA geolocalizada por WhatsApp — NÚCLEO construido (develop, sin merge a main).** Segundo bot en el MISMO número (+51 972 416 669), conviviendo con el de observaciones vía un **router** (`api/_lib/router.js`): un mensaje de *ubicación* → asistencia; sesión de asistencia viva → asistencia; sesión de observación viva → observaciones; intención de marcaje por texto (regex estricto, ancla al inicio o "marcar/registrar/fichar + entrada/salida") → asistencia; resto → observaciones. `api/whatsapp.js` ahora **capta el mensaje `location`** (antes lo rechazaba) y enruta. **Flujo de marcaje** (`api/_lib/asistencia.js`, máquina de estados sobre **`wa_asistencia_sesiones`**, TTL 20 min; selfie pendiente en **`wa_asistencia_fotos`**): identifica al técnico (reusa `identidad.js`) → solo régimen **`MARCA`** marca (a `FIJO`/`NO_MARCA` les responde que no marcan) → **autodetecta entrada/salida** por el registro del día → lee su **plan del día** en `bd_bloques` (`api/_lib/plan_dia.js`: usa la versión de itinerario más nueva por fecha, excluye bloques `CANCELADO`, soporta `personal[]` + legacy `personal1..4`) → pide **ubicación** y **selfie** (en cualquier orden) → **geofencing haversine** (`api/_lib/geo.js`, radio por sede, default 250 m) contra la sede del plan más cercana → escribe en `asistencia_registros` (`api/_lib/asistencia_registros.js`, **upsert idéntico a la app**: query `where fecha` → find `colabId`, entrada crea el doc con su `id`, salida hace `set({...existing,horaSalida})`; `heManual:false`/`horasExtra:null` → la app calcula la HE) con **evidencia** (`marcajeEntrada`/`marcajeSalida`: coords, distancia, radio, dentroRadio, fueraDePlan, hora, ts) + **selfie** en `asistencia_registros_fotos` (dataURL, colección aparte). **Decisiones (usuario 2026-07-01):** fuera de radio → **registra con bandera** (no rechaza); sin plan del día → deja marcar con bandera **fuera de plan** (elige la sede más cercana con coords, o pregunta si el GPS no ubica). Fechas/horas en **Lima UTC-5** (`api/_lib/fecha.js`; hora del marcaje redondeada a la ½h, convención del proyecto). Módulos con **deps inyectables** (incluido el almacén de sesiones) → suite **`migrar_db/test_asistencia.mjs` 42/42** sin tocar Firestore. Suites de observaciones intactas (fase4 17/17). |
| 2026-07-01 | **Asistencia — geo en `maestros_tiendas` + editor + backup** (`configuracion.html`). Nuevos campos **`latitud`/`longitud`/`radio`** (metros) por sede. Editor de **Tiendas** ganó una 2ª fila 📍 Geo: campo "lat, long" que **pega de Google Maps** (`updateTiendaGeo` separa el par), input de radio, badge ⚠️ *sin ubicación* y link 🗺️ *ver* para verificar en el mapa. **Fix crítico:** `saveMaestros` hacía delete-all + recrear cada tienda con solo `{id,cliente,tienda,sede,activo,obs}` → **borraba las coordenadas**; ahora persiste también `latitud/longitud/radio`. **Backup:** `maestros_tiendas.csv` suma columnas `LATITUD/LONGITUD/RADIO/OBS`; `asistencia.csv` suma `ORIGEN` + `EVIDENCIA` (JSON con `marcajeEntrada/Salida`, que no tenían columna); nueva **`asistencia_registros_fotos.csv`** (selfies base64, doc id = id del registro) en export/import + `expected` (26ª colección). Borrador de coordenadas de las 13 sedes sembrado con `migrar_db/seed_geo_tiendas.js` (idempotente; imprime link de Maps por sede; **a verificar/ajustar por el usuario** — Comas/SJL/Primavera/Oficina marcadas confianza BAJA). |
| 2026-07-01 | **Asistencia — vista de evidencia** (`asistencia_multiaire.html`). El modal de edición de un registro muestra la **evidencia del bot** (`renderEvidencia`): por marcaje (entrada/salida) la **selfie** (miniatura → lightbox `selfie-lightbox`), 🏪 sede, hora, badge geo (✓ en la sede · N m / ⚠️ fuera de radio · N m / sin coordenadas) y ⚠️ *fuera de plan*, con link 🗺️ a la ubicación; las selfies se cargan de `asistencia_registros_fotos` por id al abrir. La fila del día muestra un 📱 cuando el registro tiene evidencia de marcaje (o `origen==='WHATSAPP'`). |
| 2026-07-01 | **Asistencia — auditoría Codex (Council) + endurecimiento.** El bot pasó por 3 rondas de auditoría independiente (RECHAZADO → APROBADO CON OBSERVACIONES). Fixes aplicados: (1) `construirRegistro` ENTRADA fuerza estado A + obs limpia + HE auto (no hereda de un DM/P/F previo); (2) prioridad del **router** = sesión asistencia → sesión obs → ubicación → texto (una ubicación ya no corta una observación en curso); (3) `RE_ASISTENCIA` reescrito para no dar falsos positivos ("salida de aire", "entrada principal", "marca Carrier") y sí captar "salí/ya salí" — usa lookahead `(?![a-z])` en vez de `\b` por los acentos ("llegué"); (4) `plan_dia` soporta `personal[]` de **objetos** `{id,nombre}` (formato real, itinerario.html:955) + 5º asignado; (5) `renderEvidencia` sin XSS (valida selfie `data:image/...`, coacciona coords a `Number`); (6) editar un registro del bot preserva la evidencia (`merge` si sigue en estado A; overwrite si pasa a DM/P/F); (7) `fecha.js` capa la hora a 23.5 (no `24`). **Anti-duplicados:** app **y** bot crean registros nuevos con el **mismo doc id determinístico `AST-<colabId>-<fecha>`** → imposible duplicar un registro del mismo colab+fecha (cierra la carrera de concurrencia). `origen` a nivel de registro es preciso en casos mixtos (entrada app + salida bot). Editor de tiendas escapa sus inputs (`escAttr`). Suite `migrar_db/test_asistencia.mjs` **54/54**. |
| 2026-07-02 | **Asistencia — geo CERRADA + bot a PRODUCCIÓN (merge develop→main).** Las coordenadas de las 13 sedes en `maestros_tiendas` quedaron **ajustadas y testeadas en campo por el equipo** (se cargaron directo por el editor 📍 Geo de `configuracion.html`; la **fuente de verdad es Firestore**, no los scripts) con radios **intencionales de 1000 m** por tienda y **500 m** en la oficina (con radio amplio la bandera *fuera de radio* casi no salta en falso; el default 250 m de `geo.js` ya no aplica a ninguna sede). `migrar_db/update_geo_tiendas.js` (que tenía coords de OSM ya superadas) quedó **neutralizado como snapshot 2026-07-02** de los valores vivos —correrlo re-escribe lo mismo, inocuo— y `migrar_db/check_geo_tiendas.js` (solo lectura) verifica Firestore vs snapshot: **13/13 OK**. Con la geo cerrada y el núcleo auditado (Codex APROBADO, suite 54/54) se hizo **merge a main con orden explícita del usuario** → el bot de asistencia queda en producción en el mismo webhook del de observaciones. Pendiente: **prueba en vivo del marcaje** (técnico régimen `MARCA` con plan del día). |
| 2026-07-02 | **Setup colaborativo:** Adrián (`Adro3004`) invitado al repo con permiso push; nueva sección **Trabajo colaborativo** en CLAUDE.md (ramas, territorio por módulos, tablero, reglas de datos); **Tablero de equipo** creado en el vault compartido "Cerebro MultiAire" (Drive). Alta SUPER_ADMIN de Adrián completada en `usuarios` (adrian30_04@hotmail.com — su hotmail es cuenta Google, login verificado; acceso a las 9 apps). |
| 2026-07-02 | **Login en el índice + tarjetas por rol + máscara de URL** (`index.html` + `auth-guard.js`). (1) El panel de apps (antes 100% público) exige **login antes de mostrar las tarjetas** — mismo scaffold que los módulos (overlay `auth-overlay` + `signInWithPopup` + verificación contra `usuarios` + `MA.setUser`); con la sesión de Firebase persistente en el dominio, **un solo login** sirve para índice y módulos. Header del índice gana **user-pill** (avatar + menú con rol y Cerrar sesión → `location.reload()`). Robustez del auth: `permission-denied` al leer `usuarios` (o "logueado en Google pero sin doc") → overlay con **"Acceso denegado"** (en los módulos quedaba página en blanco); al denegar se hace **`signOut()`** (sin esto el denegado quedaba en bucle auto-login→denegado en cada recarga) y el provider lleva **`prompt:'select_account'`** (permite probar con otra cuenta Google); si el popup es bloqueado (`auth/popup-blocked`, típico de **Brave**) cae a **`signInWithRedirect`**, con **`getRedirectResult().catch`** para que un redirect fallido muestre su error en vez de volver al login en silencio; y si el CDN de Firebase no carga, el overlay muestra el error en vez de quedarse "conectando" (try/catch alrededor del init). (2) **Tarjetas según el gate REAL de cada módulo** (`data-app` por card + `applyCardAccess()` con `CARD_GATES`): entrada por `apps[]` (`MA.canEnter`), salvo los gates propios más estrictos — Configuración `MA.can('config')` (solo SUPER_ADMIN) y Rendición de Caja `MA.can('editar','comprobantes')` (TECNICO no entra). **Fail-closed**: card sin `data-app` conocido no se muestra; usuario sin ninguna app visible ve el aviso **`#grid-empty`** ("aún no tienes apps asignadas"). `card-meta` muestra el slug (`inventario`) en vez del archivo `.html`. (3) **Máscara de URL** en `auth-guard.js` (decisión de UX del usuario, avisado del trade-off): `history.replaceState(null,'','/')` en toda página → la barra muestra **siempre solo el dominio** (`app.multiaire.com.pe`), sin `/pagina.html`. Cosmético, NO seguridad. **Solo aplica en la raíz del dominio** (guard: `pathname` debe ser `/` o `/*.html`; en subrutas tipo GitHub Pages — `/repo/` y también `/repo` sin slash — `replaceState('/')` cambiaría la base de los href relativos y rompería la navegación). Asumido: F5, Adelante, duplicar pestaña y restaurar sesión dentro de un módulo vuelven al índice; los deep-links SÍ abren (la máscara reescribe después de cargar), solo no se pueden copiar de la barra; no aplica en `localhost`; revertir = borrar el bloque al final de `auth-guard.js`. Sintaxis validada (`node --check`). |
| 2026-07-03 | **Índice: branding con el logo oficial + limpieza de contenido genérico** (`index.html`, pedido del usuario). *(La parte del header fue superada el mismo día por la iteración siguiente.)* (1) Se agrega **`logo-multiaire.png`** al repo (logo oficial, mismo de la web pública `multiaire-web`): en el **header** va sobre una **placa blanca** (`.logo-plate`; el wordmark azul no se lee sobre el header azul) + texto "Panel de Gestión" (oculto en móvil); la **tarjeta de login** lo lleva arriba (52px) en lugar del punto naranja + texto. (2) **Eliminados** el **hero** de bienvenida ("Bienvenido al sistema de gestión") y la tarjeta **"Información del sistema"** completa — en particular el **link al repo de GitHub, que no debe exponerse a los usuarios**. (3) La **versión/entorno** dinámicos por hostname se mudan al **footer** (`#footer-ver`: prod `v3.3.0`, develop `v3.3.0-dev · Develop`, local `· Local`). CSS huérfano retirado (`.hero*`, `.infobar`, `.info-*`, `.logo-dot`, `.auth-logo-dot/-text`). Sintaxis validada. |
| 2026-07-03 | **Índice: iteración del branding (feedback del usuario tras ver el PR #2 en develop)** — el header deja la placa con logo y queda solo el título **"Panel de Gestión" CENTRADO** (negrita, blanco, 17→19px; `.logo{position:absolute;left:50%}` — el header sticky es el contenedor); el **logo de MultiAire baja a donde estaba el hero** (bloque `.brand` arriba de "Apps disponibles", 72px desktop / 56px móvil, fondo claro donde el wordmark azul sí contrasta). El badge "Sistema activo" se oculta desde **900px** hacia abajo (hallazgo del Council: el título centrado es absoluto y en 769-900px un nombre largo en el pill + badge podían pisarlo) y el título baja a 16px en móvil. La tarjeta de login no cambia (logo 52px). |
| 2026-07-03 | **Índice: logo de MultiAire centrado sobre las tarjetas** (`index.html`, pedido del usuario) — el `.brand img` (que es `display:block`) quedaba alineado a la izquierda; gana `margin:0 auto` y el logo queda centrado horizontalmente sobre el panel de apps, en desktop y móvil. |
| 2026-07-03 | **Headers de los 10 módulos: solo el nombre del módulo** (pedido del usuario) — el `.logo` deja de ser "MultiAire / Módulo" (con puntito naranja) y pasa a ser **solo el nombre del módulo** con la negrita blanca que ya tenía el `.logo` (p.ej. "Inventario de Equipos", "Rendición de Caja"). Se retiran del header el `.logo-dot` (y su `@keyframes blink` donde quedaba huérfano — `index.html` conserva el suyo, lo usa `.live-dot`), la regla base `.logo span{…}` y el `.logo span{display:none}` de los media queries móviles → **en celular ahora se ve el nombre del módulo** (antes el media query ocultaba el span y quedaba solo "MultiAire"), con las reducciones de tamaño ya existentes (13–14px donde las había). Las tarjetas de login de los módulos no cambian. |
| 2026-07-03 | **Índice: fix — el avatar del usuario caía a la IZQUIERDA en celular** (`index.html`, reportado por el usuario con capturas de producción). Causa: el `margin-left:auto` que empuja el bloque derecho del header vivía en `.header-badge`; al ocultarse el badge desde 900px (fix del Council del PR #3), nada empujaba al `#user-pill` (el título centrado es absoluto, no ocupa flujo) → el pill quedaba pegado a la izquierda y su menú desplegable (`right:0` respecto al pill) se abría hacia fuera de la pantalla, cortado. Fix: `.user-pill{margin-left:auto}` dentro del mismo `@media(max-width:900px)` — solo en móvil, porque en desktop el badge conserva su auto-margin (dos auto-margins repartirían el espacio libre y descolocarían el badge). |
| 2026-07-03 | **Inventario: sede con búsqueda + filtros en cascada** (`inventario_multiaire.html`, pedido del usuario). (1) El filtro **Sede** del sidebar y el campo **Sede** del formulario de alta/edición dejan de ser `<select>` y pasan a **input con datalist** (se escribe "chorrillos" y matchea la sede): el filtro busca por **substring case-insensitive** sobre `r.sede`, y el formulario exige que el texto corresponda a una **sede real** (unión de `maestros_tiendas` activas + sedes ya usadas en `inventario`, normalizando a la grafía canónica) — si no, toast de error. Se pierde el agrupado visual por cliente (`optgroup`, hoy solo RIPLEY; el datalist no agrupa). (2) **Filtros en cascada** en `af()`: cada control (Tipo/Área/Marca y las sugerencias de Sede) ofrece **solo los valores coherentes con los demás filtros activos** (helper `pass(r,skip)` — las opciones de un campo se calculan filtrando por todos menos él mismo, patrón facetado independiente del orden); `fs()` ahora **preserva la selección actual** aunque haya quedado fuera de las opciones (antes el rebuild la reseteaba a "" en silencio). `buildSedeSelect`/`byCliente` retirados; global `sedesForm` con las sedes de maestros (fallback: sedes de los datos). Sintaxis validada (scripts inline por `node --check`). |
| 2026-07-03 | **Inventario: eq_id único race-safe (preparación multi-usuario)** (`inventario_multiaire.html`). El código `MA-SEDE-TIPO-###` de un equipo nuevo se generaba **contando** los datos locales → dos riesgos: (a) con huecos por eliminaciones, conteo+1 **repetía un código vivo** (quedan 001 y 003 → conteo 2 → generaba 003) — bug incluso mono-usuario; (b) dos usuarios creando misma sede+tipo a la vez generaban el mismo código y el `setDoc` del segundo **pisaba al primero en silencio**. Fix: correlativo por **máximo del prefijo** en vez de conteo + **`crearEquipoUnico()`**: transacción **create-only** (`runTransaction`: `tx.get` → si `snap.exists` lanza `eqid-exists` → `tx.set`) con **hasta 20 reintentos** avanzando el correlativo; si se agotan, error pidiendo recargar. El toast de alta muestra el código final. `gasPost` queda solo con `update`/`delete` (la acción `save` migró a la transacción). Pendiente conocido multi-usuario (aceptado): sin tiempo real (hay que refrescar para ver lo ajeno) y last-write-wins al editar la misma ficha. |
| 2026-07-03 | **Inventario: dropdown de sedes agrupado por cliente** (`inventario_multiaire.html`, feedback del usuario con capturas: el datalist nativo mostraba la lista plana y quería "como antes pero buscando"). Se reemplaza el datalist por un **dropdown propio** (`sddInit`/`sddGrupos`/`sddGroups` + CSS `.sdd*`): al enfocar o escribir en Sede (filtro del sidebar y formulario) se abre un panel con las sedes **bajo el encabezado de su cliente** (como el `optgroup` original) filtradas por el texto tipeado (**sin distinguir tildes**, NFD). Clic en una sede la fija (en el filtro dispara `af()`); Escape/Enter/clic fuera cierra; `pointerdown` con `preventDefault` para no robar el foco. Fuentes de datos: filtro = datos en cascada (`pass(r,'sede')` agrupado por `r.cliente`); formulario = `maestros_tiendas` activas por `t.cliente` (fallback: datos). En móvil `.sdd{max-height:170px}` (el sidebar colapsado mide 280px). El matching del filtro sigue siendo substring: escribir sin elegir también filtra. |
| 2026-07-03 | **Proveedores: historial de compras (provisional)** (`proveedores.html`, pedido del usuario — spec detallada de mejora del módulo). Nueva colección **`compras`** (top-level, no anidada en `proveedores`) con su propia capa de datos desacoplada en **`compras.js`** (`window.COMPRAS`, recibe `db` explícito, sin leer globals de `proveedores.html` — mismo criterio que `auth-guard.js` — para poder reutilizarse si en el futuro existe un módulo Compras independiente; **no decidido todavía**, el usuario evaluará viabilidad). La columna **Notas** de la tabla se reemplaza por **Acciones** (👁 Vista rápida · 📋 Detalles · 🗑 Eliminar, este último solo `MA.can('borrar')`). |
| 2026-07-03 | **Proveedores: 👁 Vista rápida** — popover flotante con 4 indicadores (productos diferentes, compras realizadas, última compra, monto total comprado), cargado on-demand vía `COMPRAS.porProveedor`/`COMPRAS.indicadores` y cacheado en memoria por proveedor. |
| 2026-07-03 | **Proveedores: 📋 Detalles** (`modal-prov-detalle`) — reemplaza la vista de solo-lectura que antes vivía en el modal de edición (`openEditProv` ahora solo se abre para quien puede editar, vía el nuevo botón **✎ Editar** de este panel): información general del proveedor, los mismos 4 indicadores como stat tiles, tabla de **productos más comprados** (top 5 por frecuencia, `COMPRAS.topProductos`) y botones **🕘 Ver historial completo** / **➕ Registrar compra** (gateados a `isProvAdmin()`). Doble clic en la fila abre este panel (antes abría el modal de edición). |
| 2026-07-03 | **Proveedores: ➕ Registrar compra** (`modal-registrar-compra`) — proveedor precargado, ítems dinámicos con tipo **Artículo del catálogo** (buscador contra `insumos_catalogo` vía `COMPRAS.catalogo()`, exige seleccionar una coincidencia real — no texto libre) o **Servicio** (descripción libre), cantidad/precio unitario/subtotal, fecha y N° de comprobante opcional. Los ítems ARTICULO guardan `catalogoId` como preparación para una futura relación con Inventario (no activa todavía — no se toca `insumos.html`); los SERVICIO nunca la llevan. |
| 2026-07-03 | **Backup**: colección `compras` agregada a export/import de `configuracion.html` (`compras.csv`, doc completo en JSON por fila — mismo criterio que `insumos_movimientos`, por el array `items[]` de esquema variable). |
| 2026-07-03 | **`firestore.rules`: regla para `compras`** (mismo permiso que `proveedores` — `read: authorized()`, `create/update: canWrite('proveedores')`, `delete: isSuper()`) para que el catch-all fail-closed no bloquee la colección nueva. **`firestore.rules` desplegado a prod** (`firebase deploy --only firestore:rules`, proyecto `multiaire-fee43` — afecta develop+prod por igual, comparten el mismo Firestore). |
| 2026-07-03 | **Proveedores: ajustes tras auditoría con Codex CLI** (`compras.js` + `proveedores.html`, pedido del usuario). (1) **Acciones** de la tabla pasa de 3 a 2 botones (👁 Vista rápida, 📋 Detalles) — se saca el 🗑 Eliminar de la fila; sigue disponible en el modal de edición y en el panel Detalles. (2) **Productos más comprados** suma columna **Precio mínimo** (`COMPRAS.topProductos` ahora trackea `precioMinimo` por ítem, no solo el último precio). (3) **Fix de integridad de datos** (hallazgo Codex, P2): `removeProveedor` ahora borra en cascada las compras del proveedor eliminado vía `COMPRAS.eliminarPorProveedor` (antes quedaban huérfanas en Firestore, inaccesibles pero no borradas); el confirm avisa cuántas compras se eliminan. **Hallazgo Codex pendiente (no arreglado, no exclusivo de esta función):** restaurar un backup anterior a `compras` en modo "Reemplazar todo" no limpia la colección si el ZIP no trae `compras.csv` — comportamiento preexistente de `configuracion.html` para cualquier colección nueva. |
| 2026-07-04 | **Proveedores: responsive del modal de Detalles/compras en celular** (`proveedores.html`, reportado por el usuario tras el testeo en vivo: "el modal es muy grande y hay que scrollear para llegar a los botones"). En el `@media(max-width:768px)`: (1) **`.modal-foot` sticky** (`position:sticky;bottom:0` + fondo) → los botones quedan **siempre visibles** al pie mientras el contenido scrollea detrás; **`flex-wrap:wrap`** porque a 390px los 4 botones del Detalles no caben y "🗑 Eliminar" se cortaba por la izquierda (el overflow de un flex `justify-end` ocurre al inicio y ni scrollWidth lo delata). (2) Fix del **hueco vertical bajo "Fecha"** en Registrar compra: con `.form-row` en columna, el `flex:0 0 160px` del form-group se vuelve **alto** de 160px → `.form-row .form-group{flex:0 0 auto}`. (3) Modal compacto (`padding:16px 14px`, stat tiles más chicos) y la mini-table de "Productos más comprados" envuelta en **`.table-scroll`** (scroll horizontal propio en vez de arrastrar el modal entero). Verificado inyectando el CSS sobre develop en un iframe de 390px antes del merge. |
| 2026-07-04 | **Índice: el footer subía al header durante la carga + Proveedores: scroll horizontal en Registrar compra (iPhone)** (capturas del usuario). (1) `index.html`: `#main-page` arrancaba `display:none` hasta resolver el auth → sin el main en el flujo, el footer quedaba pegado al header con todo el vacío debajo (el sticky-footer `main{flex:1}` no tenía a quién empujar). Fix: **`visibility:hidden` en vez de `display:none`** (ocupa su alto desde el primer paint; el JS ahora setea `visibility='visible'`). (2) `proveedores.html`: el **`input[type=date]` de iOS ignora `width:100%`** y se dimensiona a su contenido → desbordaba el modal (scroll horizontal); mismo fix que asistencia 2026-06-21: `-webkit-appearance:none;appearance:none;text-align:left`. (3) El **hueco bajo "Fecha" persistía** pese al fix del PR #13 porque `flex:0 0 160px` era **estilo inline** (le gana al override del media query): movido a la clase **`.fg-fecha`** — ahora el `.form-row .form-group{flex:0 0 auto}` móvil sí gana por especificidad. (4) Candado: `.modal{overflow-x:hidden}` en móvil. |
| 2026-07-04 | **Módulos sin footer** (pedido del usuario: "en el index normal, pero en los demás es innecesario"). Se retira el `<footer>` (MultiAire © · módulo · reloj) de los 4 módulos del scaffold que lo tenían — `proveedores.html`, `comprobantes.html`, `observaciones.html`, `personal.html` (los otros 6 nunca tuvieron) — junto con su CSS (`footer{}`, `.footer-txt`, `.footer-time`, el `footer{}` del media móvil) y el **bloque CLOCK** (IIFE `tickClock` de 1s): quitarlo era obligatorio, no opcional — corre en el MISMO `<script>` que inicializa Firebase y con `#clock` ausente lanzaría TypeError matando el auth de la página. `index.html` conserva su footer (ahí vive la versión/entorno `#footer-ver`). |
| 2026-07-04 | **Proveedores: 2 hallazgos Media del Council en la auditoría de integración develop→main** (previa a la subida a producción). (1) **"Ver historial completo" sin gate**: la propia spec (changelog 2026-07-03) lo declara gateado a `isProvAdmin()` pero solo Editar/Registrar lo implementaban → botón con `id=det-btn-historial`, visible solo si `isProvAdmin()` (como sus hermanos) + guarda interna en `openHistorial()`. (2) **Orden del borrado en cascada**: `removeProveedor` borraba el proveedor ANTES que sus compras — si la cascada fallaba quedaban compras huérfanas (justo lo que el PR #12 quería cerrar); ahora borra **compras primero** y el proveedor al final (si la cascada falla, el proveedor sigue vivo y se reintenta). |
| 2026-07-04 | **Mantenimiento — Etapa 1: actividades por tipo Y por equipo** (plan del usuario, cuaderno "Mtto." puntos 1-2; etapas 2-4 pendientes: registro+fotos+vistas, bot de 3 flujos, testeo). Modelo por HERENCIA: `tareas_config` sigue siendo la plantilla por tipo, y la nueva colección **`mtto_actividades_equipo`** (doc id = eq_id) guarda solo diferencias `{agregadas:[{nombre,minutos}], quitadas:[nombre]}` → lista efectiva = (plantilla − quitadas) + agregadas, resuelta por **`actividadesDe(eq)`** (devuelve los arrays paralelos tareas/minutos que ya usaba todo el módulo — estado por índice intacto). Los 7 puntos que leían `TAREAS[tipo]`/`MINUTOS_TAREA[tipo]` por equipo (renderMain/renderEquipo/refreshEquipoCard/updateStats/msg WhatsApp/getPct/export PDF) pasan al resolver. `loadTareas` carga plantillas+overrides en paralelo (cache sessionStorage renombrado a `ma_tareas2`, invalidado al guardar). **Editor** (botón "⚙️ Actividades de mtto." en el sidebar, oculto a técnicos): selector tipo+equipo — modo PLANTILLA (solo SUPER_ADMIN, regla de `tareas_config`: editar minutos, quitar, agregar) y modo EQUIPO (canWrite mantenimiento: desmarcar heredadas / agregar propias; sin diferencias ⇒ se borra el override y vuelve a heredar). `firestore.rules`: `mtto_actividades_equipo` read authorized / **write (incl. delete) canWrite('mantenimiento')** — borrar el override es parte de la edición normal. Backup: colección sumada a export/import de configuracion.html (patrón JSON-doc como `compras`). ⚠️ Requiere **redeploy de firestore.rules**. |
| 2026-07-04 | **Mantenimiento — Etapa 2: fotos por actividad + historial por equipo + vista Global** (cuaderno "Mtto." puntos 3, 4 y 7). (1) **Fotos de evidencia por actividad**: botón 📷 en cada fila de tarea del checklist (badge con conteo) → modal con grilla de fotos, lightbox, agregar (input `capture`, múltiples, comprimidas a 900px JPEG .72) y borrar; nueva colección **`mantenimiento_fotos`** — **1 doc por foto** `{clave, eq_id, tareaIdx, tarea, foto, createdAt, createdBy}` (esquiva el límite de 1MB/doc; queries de igualdad pura, sin índice compuesto). Los badges se recargan por período (`loadFotosCounts`) y se re-pintan tras cada `renderMain`. (2) **Historial de intervenciones** (tap en el nombre del equipo): recorre los docs `SEDE|PERIODO|AÑO` de `mantenimiento`, muestra por período el semáforo, X/N actividades y conteo de fotos, ordenado desc; nota visible: el total usa la lista vigente del equipo. (3) **Tab 🌐 Global**: tarjetas por sede del período actual (equipos seleccionados, % completado, 🟢🟡🔴) leyendo todos los docs del período + `inventario` para resolver la lista efectiva de cada equipo. Regla `mantenimiento_fotos` (read authorized / write canWrite mantenimiento) + backup export/import en configuracion.html (columnas, patrón insumos-fotos). ⚠️ Requiere **redeploy de firestore.rules**. |
| 2026-07-04 | **Bot — Etapa 3: tres flujos + menú + registro de actividades por WhatsApp** (cuaderno "Mtto." puntos 2 y 5). (1) **Router ampliado** (`router.js`): prioridad sesión asistencia → **sesión mtto** → sesión obs → ubicación → "1"/"2"/"3" sin sesión (elección de menú: 1=mtto, 2=hint observación, 3=hint asistencia, sin estado) → **saludo en frío → MENÚ** de 3 opciones → RE_ASISTENCIA → **RE_MTTO** (`mantenimientos?|mtto|preventiv[oa]s?` con lookahead) → observaciones (default intacto). (2) **Nuevo flujo `mtto.js` + `wa_mtto_sesiones`** (TTL 30 min): resuelve el equipo con el MISMO motor de observaciones (`resolverEquipo`, repregunta sede/cliente/equipo), presenta la **lista EFECTIVA de actividades** del equipo (tareas_config − quitadas + agregadas de `mtto_actividades_equipo`, caché 5 min → los cambios del editor de la app se reflejan sin redeploy), el técnico marca por números ("1, 3" / "todas"), **confirma → se guarda de inmediato** (transacción merge sobre `mantenimiento/{SEDE|PERIODO|AÑO}`: seleccionados + tareaStatus por índice, updatedBy "BOT · nombre"; período bimestral calculado en hora Lima) y recién después la fase de **FOTOS: múltiples por actividad** (cada imagen → doc de `mantenimiento_fotos` con dataURL; SIGUIENTE avanza de actividad, FIN termina, CANCELAR en cualquier punto). Abandonar las fotos NO pierde el registro. Sin cambios de rules (el bot usa Admin SDK). Suite `migrar_db/test_mtto.mjs` **33/33** (regex/saludo/período Lima incl. cruce de año/selección/merge/router con sesiones reales). |
| 2026-07-04 | **Bot mtto: 2 fixes de la prueba en vivo de José** (capturas reales, registro AA Roof Top 05). (1) **"Foto 1" repetido**: los mensajes de un álbum de WhatsApp llegan como invocaciones serverless **en paralelo** → el contador en la sesión perdía la carrera (las 3 leyeron fotos=0). Las 3 fotos SÍ se guardaban (docs independientes); solo el contador mentía. Fix: **`contarFotos()`** cuenta los docs reales de `mantenimiento_fotos` (con `.select()` para no bajar los base64) — inmune a la concurrencia; el resumen final también. (2) **Hint de observaciones colado en pleno flujo de fotos**: si `descargarMedia` fallaba (transitorio de la Graph API), la imagen sin contenido caía al aviso genérico "Mándame una observación…". Fix: imagen con `image.id` pero sin descarga → "📷 No pude descargar esa foto, ¿me la reenvías?" (aplica a los 3 flujos). Suite 42/42. |
| 2026-07-04 | **Mantenimiento: tab 🕘 Historial (bitácora de registros) + fuera "Última actualización"** (pedido del usuario: "ver qué registró cada técnico a cierta hora y por días"). Nueva colección **`mtto_log`** (evento por acción): la escriben el **bot** (al confirmar un registro: autor, sede, equipo, actividades, período) y la **app** (cada marcado/desmarcado de checkbox — `logRegistro()` fire-and-forget: nunca frustra el marcado; incluye `done:false` para desmarcados). Tab **Historial** junto a Global: últimos 300 eventos ordenados desc, **agrupados por día** (fecha en hora Lima), cada fila hora · autor · equipo (sede) · actividades · badge BOT/APP · "(desmarcó)"; filtros por sede y origen. Query `orderBy ts desc limit 300` (sin índice compuesto; filtros en cliente). Rules: `mtto_log` create=canWrite('mantenimiento'), update/delete=isSuper (bitácora inmutable para el cliente); el bot escribe por Admin SDK. Backup en configuracion.html (JSON-doc). Se RETIRA la leyenda "Última actualización" del sidebar (pedido del usuario: redundante con la bitácora; `renderLastUpdated` queda como no-op y los campos updatedAt/updatedBy se siguen guardando). La bitácora arranca vacía: registra desde este deploy. ⚠️ Requiere redeploy de firestore.rules. |
| 2026-07-04 | **Bot mtto: reconoce las ACTIVIDADES definidas como intención + escapes de la sesión de obs** (capturas del usuario: "lavado de serpentín de condensador de rooftop 5…" se iba a observaciones, y la sesión de obs resultante se tragaba el "hola" y el "quiero registrar…" — quedaba atrapado). (1) **`esActividadConocida()`**: cache 5 min de TODOS los nombres de actividades (plantillas `tareas_config` + `agregadas` de los overrides), match por substring normalizado; solo nombres específicos (≥2 palabras y ≥10 chars — "Realizado" daría falsos positivos). El router la usa como intención de mtto. (2) **`esIntencionMtto` amplía**: "registrar … actividad(es)" cuenta aunque no diga mantenimiento/mtto. (3) **Escapes con sesión de OBS viva**: saludo → limpia la sesión y muestra el MENÚ; intención mtto/actividad conocida → limpia y va a mtto (antes la sesión se tragaba todo). (4) **Pre-marcado**: al resolver el equipo, si el mensaje original menciona actividades de SU lista, se marcan solas y salta directo a CONFIRMA ("🔎 Detecté N actividad(es)…"). (5) En CONFIRMA, **NO ya no cancela**: vuelve a la selección por números (para abortar está CANCELAR). Suite `test_mtto.mjs` **51/51**; asistencia 54 y obs_v2 15 sin regresión. |
| 2026-07-04 | **Bot: el hint de "tipo no soportado" respeta la conversación viva** (captura del usuario: en plena fase de fotos, un mensaje raro del álbum soltaba el genérico "Mándame una observación… o marca entrada/salida" — confuso para el técnico). Ahora ese branch consulta `decidirFlujo` (las sesiones mandan): con sesión mtto → "Envía una foto, SIGUIENTE o FIN"; con asistencia → pide ubicación/selfie; solo sin sesión cae al genérico. Se loguea el `msg.type` no soportado para diagnóstico. |
| 2026-07-04 | **Mantenimiento: los registros del BOT no aparecían en el checklist** (feedback del usuario: "no se está marcando la actividad como hecha"). El bot SÍ escribía `tareaStatus` en Firestore, pero el load del período usaba un **caché de sessionStorage sin vencimiento**: si existía, NO releía de Firestore (ni con F5 — sessionStorage sobrevive recargas). Fix: **leer SIEMPRE de Firestore** al cargar sede/período (el doc es chico); el caché queda solo como respaldo si la lectura falla (offline). Con esto, lo registrado por WhatsApp (o por otro usuario) aparece al entrar o recargar. |
| 2026-07-04 | **Bot mtto: flujo de fotos fluido** (capturas de la prueba en vivo). (1) En fase FOTOS (el registro YA está guardado), una **nueva intención de registro cierra el anterior y arranca el nuevo** ("🏁 Cerré el registro de X (N fotos)" + flujo nuevo), y un **saludo cierra y muestra el menú** — antes la sesión vieja se tragaba todo pidiendo fotos. El fallback de FOTOS ahora dice qué registro está en curso. (2) **Foto enviada en ACTIVIDADES/CONFIRMA**: mensaje claro ("primero confirma con SÍ — las fotos vienen al toque") en vez de ignorarla con el fallback genérico. (3) El mensaje raro que WhatsApp manda junto a un álbum, en pleno flujo mtto, ahora se ignora **en silencio** (ni "🤔" — feedback del usuario: solo mete ruido entre los "Foto N guardada"). Suite 57/57. |
| 2026-07-04 | **Bot mtto: los conteos de fotos son POR SESIÓN** (reporte del usuario: el resumen final decía "12 foto(s)" cuando subió 3 — sumaba las fotos históricas del equipo en el período). La sesión guarda su `inicio` (ISO) y `contarFotos` filtra `createdAt >= inicio` (client-side sobre un `.select('createdAt')` — sin índice compuesto): tanto el "Foto N guardada" como el "🏁 Registro completo — N foto(s)" cuentan solo lo de esta sesión. Sesiones creadas antes del deploy (sin `inicio`) cuentan todo, transitorio. |
| 2026-07-04 | **Bot: palabras de control sueltas → MENÚ** (captura del usuario: un "fin" con la sesión ya cerrada caía a observaciones y abría un borrador absurdo preguntando la sede). `esControlSuelto()` ("fin", "sí", "no", "siguiente", "listo", "cancelar", "ya", "gracias"…): sin conversación viva responden el menú de 3 opciones en vez de ir al default de observaciones. Con sesión viva nada cambia (las sesiones se evalúan primero). Suite 59/59. |
| 2026-07-04 | **Insumos: foto de guía + foto de entrega en Entrada, Salida y Transferencia individuales** (`insumos.html`, pedido del usuario). Hasta ahora ese adjunto solo existía en Salida/Baja en lote y Transferencia en lote; se extendió a los 3 modales "de a uno" reutilizando tal cual la infraestructura ya construida (`compressFit`, `loteFotos`/`loteFotoPrefix` — ahora también toma los valores `fent`/`fsal`/`ft` — y `refreshLoteFotoPreview`), sin tocar el esquema de `insumos_movimientos_fotos` ni `firestore.rules`. Cada modal, al abrirse, resetea `loteFotos` y fija su prefijo; al guardar, si hay N° de guía o alguna foto, el movimiento gana `guiaNumero`/`tieneFotos` y se escribe el doc en `insumos_movimientos_fotos` (doc id = id del movimiento) — mismo patrón que `saveSalidaLote`/`saveTransfLote`. `renderMovimientos` y la reversión/cascada ya leían `m.guiaNumero`/`movFotos[m.id]` de forma genérica (sin filtrar por tipo), así que no requirieron cambios. Sin cambios de backup (la colección ya se respalda desde 2026-06-17). Sintaxis validada (`node --check` sobre el script inline). |
| 2026-07-04 | **Insumos: foto de referencia por ítem de catálogo, heredada dinámicamente por sus instancias** (`insumos.html` + `firestore.rules` + backup, pedido del usuario). Nueva colección **`insumos_catalogo_fotos`** (doc id = id del catálogo, mismo shape que `insumos_instancias_fotos`). Se agrega el bloque "Foto de referencia" al modal de ítem (`fi-foto-wrap`). El fallback es **dinámico, no una copia**: `fotoDeInstancia(inst)` centraliza la lectura (`instFotos[inst.id] || catFotos[inst.itemId]`) y la usan `instThumb`, el lightbox, la vista previa del modal de instancia (etiqueta "📎 Del catálogo" cuando la foto es heredada) y los exports de material (CSV + reporte PDF/PNG vía `_matPrepare`). Subir una foto a una instancia la independiza; quitarla la devuelve a heredar la del catálogo sin acción aparte. `deleteInsumo` borra en cascada la foto de referencia del catálogo eliminado. `firestore.rules`: regla nueva para `insumos_catalogo_fotos` (mismo permiso que `insumos_catalogo`) — **requiere redeploy de reglas**. Backup: `insumos_catalogo_fotos.csv` agregado a export/import de `configuracion.html` (mismo patrón minimal ID+FOTO que `insumos_instancias_fotos.csv`). Sintaxis validada (`node --check` sobre los 3 archivos). |
| 2026-07-04 | **Insumos: la foto de referencia también se puede adjuntar al crear un ítem nuevo** (`insumos.html`, feedback del usuario: antes solo se podía subir editando un ítem ya guardado). Como un ítem nuevo aún no tiene `docId` en el momento de elegir la foto, esta se queda en memoria (`pendingCatFoto`, mismo criterio que `loteFotos` para guía/entrega) hasta que `saveInsumo()` crea el ítem — recién ahí se sube a `insumos_catalogo_fotos/{docId}`; si el guardado falla (p.ej. código duplicado) la foto en memoria sobrevive para el siguiente intento. `setCatalogoFoto`/`removeCatalogoFoto`/`refreshCatFotoPreview` distinguen ítem-ya-guardado (sube/borra de inmediato, como antes) de ítem-nuevo-sin-guardar (memoria). `pendingCatFoto` se resetea al abrir "Nuevo ítem" para no arrastrar una foto de un intento cancelado. Gate de permisos alineado al de `saveInsumo` (`crearItem` o `editarItem`). Sin cambios de esquema ni de `firestore.rules`. Sintaxis validada. |
| 2026-07-04 | **Fix: "Stock mínimo" en 0 no se veía al reabrir un ítem de catálogo** (`insumos.html`, reporte del usuario: "coloco 0, guardo, y parece que no se guardó — me salen muchos avisos"). `openModalInsumo` precargaba el campo con `i.stockMin||''` — como `0` es falsy en JS, un ítem con `stockMin:0` **ya guardado correctamente** volvía a mostrar el campo vacío al editarlo, dando la impresión de que el 0 no se había guardado (el guardado en sí, `Number(valor)||0`, siempre estuvo bien). Fix: `i.stockMin??''` (nullish coalescing) — ahora `0` se muestra como "0" y solo un ítem sin `stockMin` definido queda en blanco. *(Nota para el usuario: el aviso "⛔ sin stock" del banner superior NO depende de Stock mínimo — sale cuando un ítem tiene 0 instancias activas registradas; poner 0 en Stock mínimo solo apaga el aviso "⚠️ stock bajo". Si el banner que te aparece es "sin stock", hay que registrar una Entrada con las unidades reales, no ajustar este campo.)* |
| 2026-07-04 | **Revert del fix anterior — decisión del usuario** (`insumos.html`): vuelve a `i.stockMin||''`, es decir, un ítem con `stockMin:0` muestra el campo **vacío** al reabrirlo, como antes del fix de hoy. Motivo: vacío y 0 son funcionalmente idénticos para las alertas (ninguno de los dos activa "stock bajo" — ver nota de la entrada anterior), y el usuario prefiere que el campo quede vacío para que siga visible el placeholder de ejemplo ("Ej: 5 — deja 0 para no alertar"); si el campo mostrara "0" el placeholder nunca se vería. Sin pérdida de información: el guardado (`Number(valor)||0`) siempre trató vacío y 0 como el mismo valor. |
| 2026-07-04 | **Insumos: banners de "sin stock"/"stock bajo" ahora desplegables** (`insumos.html`, pedido del usuario: "no debería haber tanta información en pantalla"). Antes cada banner metía el nombre de **todos** los ítems afectados en una sola línea — con varios ítems se volvía una pared de texto que empujaba el resto de la pantalla. Ahora el banner colapsado solo muestra el conteo ("⛔ 12 sin stock ▸ ver"); un clic (`toggleAlertGroup('sin'|'bajo')`) despliega la lista completa debajo en una caja aparte, y otro clic la vuelve a ocultar. El estado expandido/colapsado (`alertsExpanded`) es una variable de módulo que persiste entre los `renderAlerts()` que dispara cada `renderAll()`, para que no se cierre solo al hacer cualquier otra acción en la app. Sin cambios de datos ni de permisos. |
| 2026-07-04 | **Fix: cerrar un modal (Entrada/Salida/Transferencia/Baja/etc.) con clic afuera dejaba el modal roto para la próxima apertura** (`insumos.html`, reporte del usuario). Causa: **dos mecanismos de cierre conflictivos** sobre el mismo `.modal-overlay` — el `onclick` inline de cada modal (`if(event.target===this)closeModal(id)`, que limpia `classList` y el estilo inline) **y** un listener genérico al final del script que ponía `style.display='none'` **directo en el elemento**. Ese estilo inline queda pegado y le gana a la clase CSS `.modal-overlay.open{display:flex}` la próxima vez que `openModalX()` intenta reabrir el modal (solo agrega la clase, nunca limpia el inline) — el modal deja de responder aunque el código de apertura corra sin error. Fix: el listener genérico ahora llama a `closeModal(o.id)` en vez de tocar el estilo a mano — es idempotente (no rompe nada que ya se llame dos veces desde los modales que además tienen su propio `onclick`) y deja el mismo estado limpio siempre. Afecta a **todos** los modales del archivo, no solo los de hoy. *(Hallazgo aparte, no corregido en este fix: `modal-scanner` no tiene `onclick` inline propio — cerrarlo con clic afuera nunca llama a `closeScanner()`/`destroyScanner()`, así que la cámara del escáner QR queda encendida de fondo. Preexistente, mismo comportamiento antes y después de este cambio.)* |
| 2026-07-05 | **Insumos: hallazgos del Council en la auditoría de integración pre-main** (paquete #28–#34 de Adrián). (Crítico) las fotos de catálogo/instancia/movimientos iban crudas a `src` → **`fotoSrc()`** valida `data:image/*;base64` con charset base64 puro (sin comillas posibles) en los 6 puntos de render. (Alto) el `onclick` de la vista previa de foto de catálogo interpolaba `editingInsumoId` en un string JS → ahora la expresión se evalúa al click con los globals. (Alto, **cierra el backlog transversal**) "Reemplazar todo" ahora **VACÍA las colecciones nuevas ausentes del ZIP** (`compras`, `mtto_actividades_equipo`, `mantenimiento_fotos`, `mtto_log`, `insumos_catalogo_fotos`; batch troceado a 450) — un backup viejo ya no deja datos fantasma. (Medio) `deleteInsumo` borra la foto de catálogo **incondicionalmente** (el mapa local podía no tenerla cargada); nombres escapados (`escB`) en los banners desplegables de stock. |
| 2026-07-05 | **Itinerario ⇄ Mantenimiento: asignar actividades de mtto preventivo al plan del día** (pedido del usuario: "jalar cuadros de actividades"). Nueva colección **`mtto_plan`** — 1 doc por actividad asignada, **id determinístico `EQ|PERIODO|AÑO|IDX`** (re-asignar la mueve, no duplica): {eq_id, nombreEq, tipo, tareaIdx, tarea, sede, periodo, anio, fecha, itiId, nBloque, asignadoPor, ts}. **En itinerario.html**: cada bloque-card gana "🔧 Asignar" → panel con las actividades **PENDIENTES de la sede del bloque** (lista efectiva del equipo − hechas del período − ya planificadas; agrupadas por equipo); **click/tap asigna** al bloque (celular incluido) y en desktop los cards son **arrastrables a cualquier bloque** (HTML5 DnD, la sede destino se toma del DOM del bloque); chips "🔧 tarea · equipo" en cada bloque con ✕ para quitar; los chips se cargan al pintar el día (`cargarPlanDia`, hook post-render). El período se calcula de la FECHA del itinerario (`periodoDe`), no de hoy. **En mantenimiento_multiaire.html**: la fila de la tarea muestra badge **"🗓 dd/mm"** si está planificada (query `mtto_plan` por sede+período, `loadPlanBadges`/`refreshPlanBadges`, mismo patrón que las fotos). Rules: `mtto_plan` read authorized / write canWrite('itinerario'). Backup export/import + colección sumada a la lista NUEVAS del "Reemplazar todo". ⚠️ Requiere redeploy de firestore.rules. |
| 2026-07-05 | **Itinerario/plan: fix de sede con cliente + asignación POR TÉCNICO** (capturas del usuario: el panel decía "sin pendientes" en RIPLEY PLAZA NORTE, y aclaró que la actividad se asigna a un técnico específico). (1) **Fix del match de sede**: la tienda del bloque viene con el cliente adelante ("RIPLEY PLAZA NORTE") y el inventario guarda solo la sede ("PLAZA NORTE") → match exacto o el **sufijo más largo que sea una sede real** del inventario (no confunde "MAC PLAZA NORTE" con "PLAZA NORTE"). (2) **Asignación por técnico**: al asignar, si el bloque tiene un solo técnico va directo a él; con varios, **mini-selector "👷 ¿Para qué técnico?"** (personal del bloque, formato objetos o legacy p1–p4); sin personal, queda a nivel de bloque. `mtto_plan` gana `tecnicoId`/`tecnicoNombre`; el chip muestra "🔧 tarea · equipo 👷 TÉCNICO". |
| 2026-07-05 | **Itinerario: tanda de la auditoría COMPLETA del módulo + actividades en los exports** (pedido del usuario pre-main). (P0) **XSS**: helper `escI()` aplicado a todos los renders con datos de Firestore/inputs — bloque (tienda/grupo/personal/detalle), `obsGenerales` (vista, textarea del editor y print layout) y el layout de PNG/PDF completo. (P0) **`mtto_plan` consciente de versión**: guarda `itiVersion` y los chips solo pintan los de la versión abierta (una v2 con bloques reordenados ya no muestra actividades en el bloque equivocado; docs legacy sin versión se muestran para no ocultar lo asignado en develop). (P1) **borrar versión / todas las versiones limpia `mtto_plan`** (sin asignaciones huérfanas). (P1) **validación al guardar**: bloque sin tienda o con horario fin ≤ inicio ya no se persiste (antes el bot lo ignoraba en silencio). (P2) guard `MA.can('editar','itinerario')` dentro de `_finalizarAsignacion`. **Exports**: WhatsApp suma "🔧 Actividades:" por bloque, y PNG/PDF (buildPrintLayout) una sección "Actividades de mantenimiento" — ambos leen los chips del DOM. |
| 2026-07-05 | **Itinerario: multi-selección en el panel de actividades** (pedido del usuario: "seleccionar varias a la vez, no una por una"). El tap sobre un cuadro ahora **marca/desmarca** (✓ visual, set `_paSel`) y el pie del panel gana el botón sticky **"🗓 Asignar seleccionadas (N)"**: con un técnico en el bloque asigna todas directo; con varios, el selector 👷 aparece **una sola vez** para el lote. Escritura en **`writeBatch`** (atómica), un solo toast, cards removidos y contador reseteado. El **drag & drop individual se mantiene** en desktop (arrastrar un cuadro a otro bloque sigue asignando de a uno con su propio selector). |
| 2026-07-05 | **Itinerario: actividades AGRUPADAS POR TÉCNICO** (pedido del usuario: salían en orden de asignación). Los chips del bloque se agrupan bajo un encabezado "👷 TÉCNICO" (grupo "Sin técnico asignado" al final; dentro de cada grupo orden por equipo y tarea; el chip lleva `data-tec` y ya no repite el nombre). Los **exports** agrupan igual: WhatsApp ("👷 TÉCNICO" + viñetas) y PNG/PDF (subtítulo por técnico en la sección de actividades). |
| 2026-07-05 | **Itinerario: agrupación en DOS NIVELES — técnico → equipo (nombre · 📍 área)** (pedido del usuario). `mtto_plan` guarda ahora el **`area`** del equipo (tomada del inventario al asignar; docs previos sin área se muestran sin ella). Chips del bloque: "👷 TÉCNICO" → "❄️ Equipo · 📍 Área" → sus tareas (chips solo con la tarea, `data-tec`/`data-eq` para los exports). El **panel de pendientes** también muestra el área en el encabezado de cada equipo. **Exports** con la misma jerarquía: WhatsApp (👷 → ❄️ → viñetas) y PNG/PDF (subtítulos anidados con sangría). |
| 2026-07-05 | **Itinerario: el panel de pendientes muestra SOLO los equipos seleccionados del período** (pedido del usuario: salían los 40 equipos/169 actividades de Plaza Norte cuando el período solo tiene 10 Rooftops seleccionados). `pendientesDeSede` filtra por el mapa `seleccionados` del doc `mantenimiento/SEDE|PERIODO|AÑO` — el itinerario planifica **lo que Mantenimiento decidió trabajar** (y las listas efectivas ya respetan los aumentos/disminuciones por tipo o por equipo). Mensajes de vacío diferenciados: sin selección del período → indica ir a Mantenimiento → ＋ Seleccionar equipos; con selección pero sin pendientes → todo hecho/planificado. |
| 2026-07-05 | **Itinerario: las actividades de versiones viejas vuelven a ser seleccionables en la versión nueva** (hallazgo del usuario: asignó a 2 técnicos en v1, creó v2 y esas actividades ni se veían ni se podían re-seleccionar — quedaban bloqueadas como "planificadas" a nivel de período mientras los chips solo muestran la versión abierta). Regla nueva de bloqueo en `pendientesDeSede`: asignada en **otro itinerario/día** del período → bloquea (no planificar doble); asignada en **esta misma versión** → bloquea; asignada en una **versión vieja de ESTE itinerario** → re-seleccionable (re-asignarla sobreescribe el doc por el id determinístico y la MUEVE a la versión actual, sin duplicar). **Semántica documentada (Council): el plan es ESTADO VIVO, no historial por versión** — una actividad re-asignada en v2 deja de verse al abrir v1 (el doc se movió), y borrar la v2 devuelve sus actividades a pendientes (el registro previo de v1 fue sobreescrito y no se restaura). La trazabilidad de EJECUCIÓN vive en la bitácora mtto_log, no en el plan. |
| 2026-07-05 | **Plan ⇄ Ejecución: lo EJECUTADO sale del itinerario en automático** (pedido del usuario: "si planifiqué X para mañana y el técnico la hizo hoy, debe quitarse del itinerario sola"). Sincronización en las 3 puntas, todas por **id determinístico** `EQ|PERIODO|AÑO|IDX`: (1) el **bot**, al confirmar un registro, borra de `mtto_plan` las actividades marcadas; (2) la **app de Mantenimiento**, al marcar un checkbox (o el solo-marcar de equipo completo), borra las asignaciones correspondientes (`limpiarPlanDe`, fire-and-forget); (3) el **itinerario**, al cargar un día, hace un **barrido**: cruza el plan contra los `tareaStatus` de las sedes involucradas y retira (borra + no pinta) lo ya hecho — red de seguridad para lo ejecutado antes de este deploy o por caminos no cubiertos. Regla actualizada: **delete** de `mtto_plan` también con `canWrite('mantenimiento')` (quien registra la ejecución limpia el plan); create/update siguen solo itinerario. ⚠️ Redeploy de rules. |
| 2026-07-05 | **Itinerario: el campo Tienda pasa a "Zona de trabajo" con opción "Otro…"** (pedido del usuario). El selector del bloque mantiene el **maestro de tiendas** alineado como siempre y suma **"✏️ Otra zona (escribir)…"**: habilita un input libre para zonas ocasionales fuera del maestro (se guarda en MAYÚSCULAS, `idTienda:''`, `cliente:''`; al editar un bloque con zona tipeada el selector vuelve preseleccionado en Otro con su texto). Validación: "Otro" sin texto no se guarda. Efectos conocidos y aceptados: una zona libre no matchea inventario (el panel de actividades avisa que no hay selección del período) ni el geofencing del bot (sede fuera de maestros ⇒ `fueraDePlan`, comportamiento ya existente). |
| 2026-07-05 | **Itinerario: veto al drop cruzado de sedes** (bloqueante del Council en la integración pre-main): arrastrar una actividad a un bloque de **otra zona** guardaba `sede` de origen con `nBloque` de destino (datos incoherentes). Ahora el drop solo se acepta en bloques cuya tienda coincide con la del panel de origen (`_paTienda`); si no, toast explicativo. La asignación normal (panel abierto → mismo bloque) no cambia. |
| 2026-07-05 | **Itinerario: fix del selector de técnico — el bloque se captura POR VALOR** (encontrado en el testing responsive/funcional con harness de iframes: un doc de `mtto_plan` quedó con `nBloque:null`, invisible en chips y en el plan). El callback diferido del selector 👷 leía la global `_panelBloque` **al momento del click**: si el panel de actividades se cerraba entre abrir el selector y elegir técnico, la global ya era null y el doc se escribía huérfano. Fix: `asignarSeleccionadas` captura `nB = _panelBloque` en el closure (mismo criterio en el flujo individual, que ya era local) + **guarda defensiva** en `_finalizarMulti`/`_finalizarAsignacion`: `nBloque == null` → toast "vuelve a abrir 🔧 Asignar", jamás escribir nulo. Doc huérfano de la prueba borrado de Firestore. |
| 2026-07-05 | **Itinerario: desbordes en iPhone REAL** (capturas del usuario; el harness de iframes no los reproduce — usa el motor de escritorio, no iOS). (1) Los **`input type=time/date`** del editor se dimensionan a su contenido en iOS e ignoran `width:100%` (sobresalían del card del bloque) → `appearance:none` base, mismo fix ya probado en asistencia (2026-06-21) y proveedores. (2) El **`.bloque-header` no envolvía**: título + 2 badges fijaban un ancho mínimo > 390 que estiraba el card entero — por eso los tags de personal se cortaban en el borde → `flex-wrap:wrap` en móvil + `min-width:0` en el card + `max-width:100%` en el tag. (3) El header del día pasa a **columna en móvil** (título arriba, botones PNG/PDF/… debajo a todo el ancho, ya venían apilados). Candados: `.modal{overflow-x:hidden}` y `min-width:0` en `.bloque-form-item`. **Lección**: los desbordes específicos de iOS (rendering de fuentes + inputs nativos) requieren verificación en dispositivo real, el harness de iframes solo cubre layout genérico. |
| 2026-07-05 | **Personal: tab "🔔 Alertas del bot" — matriz de notificaciones de WhatsApp** (cuaderno del usuario: "Tab para Alertas de chat"). Matriz colaborador × 3 eventos: quién recibe el aviso cuando el bot registra una **observación**, un **marcaje de asistencia** o una **actividad de mtto**. Nuevo campo **`avisos:{obs,asistencia,mtto}`** en `maestros_personal` (toggle con `set merge` + toast; `recibeAvisos` legacy se mantiene sincronizado = `avisos.obs`); el check "📣 Recibe avisos del bot" **se retira del expediente** (vivía en Laboral y solo cubría observaciones). Filas con 📵 si el colaborador no tiene teléfono WhatsApp; checkboxes solo ADMIN (data-attrs + listener delegado). **Bot**: `avisos.js` gana `tieneAviso(p,tipo)`/`destinatariosAviso(tipo)` (fallback legacy: `recibeAvisos` = obs) y **`notificarPorTipo`** (texto libre); **hooks nuevos**: `asistencia.js` avisa al confirmar un marcaje (tipo, sede, hora, geo, fuera de plan/radio) y `mtto.js` al confirmar un registro (equipo, sede, período, actividades ✅) — ambos en try/catch, nunca frustran la acción, y **no se avisa a quien hizo la acción**. ⚠️ Asistencia/mtto van por TEXTO (sin plantilla Meta aún): llegan dentro de la ventana de 24h del destinatario; para entrega en frío garantizada habría que aprobar plantillas como `nueva_observacion`. Sin colección nueva (el backup ya exporta el doc completo en la columna JSON). Suites 11+59+54 verdes. |
| 2026-07-05 | **Avisos de asistencia y mtto por PLANTILLA de Meta** (pedido del usuario: sin plantilla solo llegaban dentro de la ventana de 24h). `notificarPorTipo` ahora intenta la **plantilla** del tipo (env `WHATSAPP_TEMPLATE_ASISTENCIA`=`marcaje_asistencia` / `WHATSAPP_TEMPLATE_MTTO`=`registro_mtto`, ambas de 5 variables, params saneados sin saltos + "—" + tope 600) y **cae a texto libre** si falla o no existe — mismo patrón probado de `nueva_observacion`. Los hooks pasan `params`: asistencia [tipo, nombre, sede(+fuera de plan), hora, geo] y mtto [autor, equipo, sede, período, actividades unidas por " · "]. Env vars ya creadas en Vercel (Production + Preview). Script **`migrar_db/crear_plantillas_meta.mjs`** envía las 2 plantillas UTILITY a revisión de Meta por Graph API (requiere el WHATSAPP_TOKEN — correr con el .env de producción). Suites 11+59+54 verdes. Mientras Meta no apruebe, el fallback a texto sigue operando solo. |
| 2026-07-05 | **`api/admin_plantillas.js`: creación de las plantillas de Meta desde el servidor** (el `WHATSAPP_TOKEN` es variable *sensitive* de Vercel — no se puede descargar, así que la operación corre donde el token vive). Endpoint POST gateado a **SUPER_ADMIN** (verifica el ID token de Firebase y el email contra la lista del sistema, patrón `comprobante.js`): `accion:'crear'` envía `marcaje_asistencia` y `registro_mtto` (UTILITY, es, 5 vars) a revisión de Meta vía Graph API; `accion:'estado'` lista las plantillas del WABA con su status. Queda como herramienta admin permanente (gateada). `migrar_db/crear_plantillas_meta.mjs` queda como alternativa manual si algún día se tiene el token a mano. |
| 2026-07-05 | **Personal: la matriz de Alertas del bot perdía su última columna en celular** (reporte del usuario: "no hay scroll para ver la última columna"). No era falta de scroll: el media móvil ocultaba las columnas 4/6/8 de **todas** las tablas (regla del directorio de 8 columnas) y la matriz tiene 4 → "🔧 Actividades" quedaba `display:none`. Las reglas nth-child se scopean a `#view-directorio`; la matriz muestra sus 4 columnas con el scroll-x del `.table-scroll` ya existente. |
| 2026-07-05 | **Insumos: hallazgos del Council sobre el escáner en lote (#56) pre-main.** (1) `addToScanLote` trackeaba también ids que YA estaban seleccionados manualmente → "Deshacer" los borraba (contradecía su propia spec); ahora solo trackea lo nuevo. (2) **Cerrar el escáner con la sesión sin confirmar deshace la sesión** (antes lo escaneado quedaba seleccionado en silencio — riesgo de salida accidental después); la selección manual previa se conserva. (3) XSS: nombre/id y el **código escaneado** (input externo — un QR malicioso podía inyectar HTML) pasan por `escB`. Nota de diseño documentada: los botones del escáner ahora inician SIEMPRE el flujo de lote (intencional en #56: una guía = un movimiento; un solo ítem = lote de 1, mismo resultado). |
| 2026-07-05 | **Máscara de URL: recargar dentro de un módulo ya NO te bota al índice** (reporte del usuario — era la consecuencia asumida de la máscara del 2026-07-02). `auth-guard.js`: cada módulo guarda su página en **`sessionStorage.ma_pagina`** (por pestaña) antes de enmascarar; el índice, al cargar, si la pestaña recordaba un módulo hace `location.replace` de vuelta (un F5 en `/` = recarga del módulo). **Navegar a propósito sigue normal**: un clic en cualquier link interno (← Inicio, tarjetas) borra el recuerdo en fase captura, así que ir al índice funciona igual que siempre. Por pestaña (sessionStorage): pestañas nuevas abren el índice como antes; duplicar pestaña hereda el módulo (deseable). Sin cambios de seguridad — sigue siendo cosmético sobre el mismo gate de auth. |
| 2026-07-05 | **Itinerario: arrastre de actividades incumplidas a días siguientes** (pregunta del usuario: planifiqué A/B/C hoy, solo se hizo A y B — ¿puedo asignar C mañana? Antes NO sin quitar el chip a mano). Regla de bloqueo refinada en `pendientesDeSede`: una asignación de **fecha ANTERIOR al día que se planifica y aún viva** (no ejecutada — el sweep ya borra las hechas) **deja de bloquear**: la actividad reaparece en el panel con la etiqueta **"⏪ del dd/mm"** (tooltip explicativo) y al asignarla se **MUEVE** al nuevo día por el id determinístico (desaparece del chip del día viejo, sin duplicar). Las asignadas en días vigentes/futuros siguen bloqueando (no planificar doble). |
| 2026-07-05 | **Itinerario: cuándo una asignación cuenta como VENCIDA para el arrastre — regla de las 19:00 hora Perú** (afinación del usuario al #64: con solo comparar fechas, la actividad de hoy era arrastrable a mañana desde la mañana misma). Ahora `yaVencio()`: días pasados completos → vencida a toda hora; el **día en curso recién desde las 19:00 de Lima** (`Intl.DateTimeFormat` con `America/Lima` — robusto aunque el dispositivo esté en otra zona). Si el técnico la cumple antes de medianoche —incluso ya movida a mañana— el registro la borra por su id único y queda como completada (comportamiento existente, confirmado). |
| 2026-07-06 | **Insumos: ARCHIVAR la base anterior + arrancar limpio (José + Adrián van a re-registrar todo físicamente).** La data de insumos "quedó mal hecha"; se decidió **congelarla como base anterior (sin borrarla, consultable)** y dejar la base viva vacía para re-cargarla de cero recorriendo el almacén e imprimiendo etiquetas QR (flujo de registro/etiqueta ya existente). **Archivado (script `migrar_db/archivar_insumos.js`, NO destructivo):** copia las 7 colecciones `insumos_*` (catálogo/instancias/movimientos/paquetes + las 3 de fotos) a **`insumos_*_legacy`** (mismo doc id, todos los campos, batch 450) y vuelca un **dump JSON íntegro** a `migrar_db/insumos_dump_<fecha>/` (respaldo en frío de fidelidad total); verifica origen==legacy antes de dar OK. **`firestore.rules`:** 7 reglas nuevas para las `*_legacy` → `read: authorized()`, **`write: false`** (archivo **inmutable**: la operación normal ADMIN/SUPERVISOR nunca lo toca; el Admin SDK del script ignora reglas). Necesarias por el catch-all fail-closed. ⚠️ **requiere redeploy de firestore.rules.** **Vista en la app:** pestaña **"📦 Archivo"** en `insumos.html` (solo lectura, carga perezosa de `insumos_catalogo_legacy`/`insumos_instancias_legacy`/`insumos_movimientos_legacy`; selector Catálogo/Instancias/Movimientos + buscador; tablas planas sin botones de edición). **Vaciado de la base viva (script `migrar_db/vaciar_insumos.js`, GATED):** guard que se niega a borrar si las `*_legacy` no cubren a las vivas + flag `--confirmar`; **no se corre hasta que el usuario lo autorice** (el día que arranquen el re-registro). **Backup:** el respaldo del archivo es el **dump JSON en disco + su presencia inmutable en Firestore** (NO se agrega al backup de `configuracion.html`: las `*_legacy` son `write:false` → un restore no podría reescribirlas, y meterlas en la lista `NUEVAS` de "Reemplazar todo" arriesgaría vaciarlas con un ZIP viejo). Firestore es UNO compartido dev/prod → archivar/vaciar afecta prod; las `insumos_*` solo las usan `insumos.html`/`configuracion.html` (vaciar no rompe otros módulos). Sintaxis validada (`node --check`). **Ejecutado y en producción (merge a main `82ec32c`):** archivado (623 docs → `*_legacy`, verificado) + vaciado de la base viva (623 docs, base activa en 0) + dump JSON respaldado también en `~/Documents/insumos_archivo_legacy_2026-07-06.zip`. |
| 2026-07-06 | **Insumos: la pestaña "📦 Archivo" pasa a ser SOLO del super admin** (pedido del usuario). Gate de UX + regla server-side (patrón del proyecto). (1) `insumos.html`: el botón `#tab-btn-archivo` arranca `display:none` y solo se muestra si `MA.isSuperAdmin()` (en `maVerify`, mismo criterio que "🗑 Resetear BD"); guardas defensivas en `switchTab('archivo')` (toast + return) y en `loadArchivoOnce()` (return) por si se invoca con el botón oculto. (2) `firestore.rules`: las 7 `insumos_*_legacy` pasan de `read: authorized()` a **`read: isSuper()`** (seguridad real, no solo visual) — redesplegado a prod (ruleset `eef152a8`). Adrián es SUPER_ADMIN en la app, así que él y José siguen viendo el archivo; ADMIN/SUPERVISOR/TECNICO ya no. Sintaxis validada (`node --check`). |
| 2026-07-06 | **Insumos: fix — el botón "← Inicio" no dejaba volver al índice** (reporte del usuario). `insumos.html` era el ÚNICO módulo cuyo botón Inicio era un `<button onclick="location.href='index.html'">`; los otros 9 usan `<a href="index.html">`. La máscara de URL (`auth-guard.js`) solo limpia el recuerdo `sessionStorage.ma_pagina` cuando el clic sale de un `a[href]` (`closest('a[href]')`, changelog 2026-07-05) → con el `<button>` el recuerdo NO se limpiaba, así que al llegar al índice este hacía `location.replace(ma_pagina)` y **rebotaba de vuelta a insumos**. Fix: el botón pasa a `<a href="index.html" class="btn btn-sm" …text-decoration:none>` (idéntico a los otros 9 módulos) → el handler de auth-guard limpia el recuerdo y la navegación al índice funciona. Solo cambio de etiqueta HTML, sin tocar JS. |
| 2026-07-07 | **Insumos: 3 mejoras de UX (pedido del usuario, pre re-registro con Adrián).** Ninguna cambia el modelo de datos ni las reglas. (1) **Paquete — dropdown de instancias**: `buscarInstanciasPkg()` mostraba resultados solo al escribir; ahora al **enfocar** el campo (q vacío) despliega las instancias disponibles (no BAJA, sin paquete/de este paquete, no ya elegidas), **priorizando la sede del paquete**, hasta 25 con scroll; sigue filtrando al tipear. Handler de clic-afuera para cerrar el dropdown. (2) **Crear ítem con stock inicial**: el modal "Nuevo ítem" gana un bloque **📦 Stock inicial (opcional)** = Cantidad + Sede (sin preseleccionar) + Responsable, visible **solo al crear** (oculto al editar). `saveInsumo()` valida la sede antes de escribir y, tras crear el ítem, llama al nuevo helper **`crearInstanciasIniciales()`** (misma lógica que `saveEntrada`: prefijo del código + correlativo + `set` con guard de id existente + movimiento **ENTRADA** con motivo "Stock inicial al crear el ítem") → crea las N unidades con su código/QR sin ir a Entrada aparte. Cantidad 0/vacío = comportamiento actual. (3) **Lote — añadir instancias no escaneadas**: los modales de salida/baja (`lote-inst-list`) y transferencia (`tlote-inst-list`) ganan un **buscador por código o nombre** (patrón `buscarInstanciasPkg`) para sumar instancias que no se escanearon; el render de la lista se extrajo a **`renderLoteInst(which)`** (config `LOTE_UI`), con **✕ por fila** para quitar; `addInstLote`/`removeInstLote` operan sobre `selectedInstQRIds` (misma fuente que `saveSalidaLote`/`saveTransfLote`, que **no cambian**); cerrar el modal (`cerrarLoteModal`) re-sincroniza el tab Instancias. Todo el render nuevo pasa por `escB()`. Sintaxis validada (`node --check`). |
| 2026-07-07 | **Bot mtto: fix — el registro se quedaba "pegado" pidiendo la sede en bucle** (reporte del usuario con captura; reproducido bit-a-bit con el inventario real, `migrar_db/repro_mtto_platanorte.mjs`). Causa raíz: al elegir la opción **"1"** del menú, `manejarMtto` guardaba ese "1" como **`textoOriginal`** de la sesión. Efecto doble: (a) el "1" metía un **número falso** → "1 lavado de filtros de rooftop **3**…" se leía con los números **1 y 3** → falso empate **AA Roof Top 01 vs 03** (pedía desambiguar un equipo que ya era claro); (b) al responder "rooftop 03", el bot **concatenaba solo `textoOriginal`("1") + la última respuesta** = "1 rooftop 03" → **se perdía la sede** ya dicha ("plaza norte") → preguntaba la sede; y "fin"/"hola" se leían como "1 fin"/"1 hola" → seguían sin sede → **bucle** (solo "cancelar" salía). Fix en `mtto.js`: (1) **no arrastrar el dígito del menú ni un saludo** como `textoOriginal` (`(t==='1'\|\|esSaludo)?'':texto`) → sin el número falso, el 1er mensaje resuelve directo; (2) en fase EQUIPO **acumular** lo dicho en `textoOriginal` (la 1ª frase trae la sede, la respuesta trae el equipo) → responder de a poco ya no pierde la sede; (3) a mitad del flujo EQUIPO, un **saludo, una palabra de control** (`fin`/`listo`/`ya`/`gracias`…, vía `esControlSuelto`) **o un dígito de menú** (`1`/`2`/`3`) **reinician al menú** (no quedar atrapado ni re-contaminar con un dígito) — cubre los 2 hallazgos del Council ("fin" no escapaba; "1" repetido re-metía el número falso). Suite del bot en verde (`test_mtto` **65/65** con 6 casos de regresión nuevos; asistencia 54 · obs_v2 15 · fase4 17 · fase5 22 · foto_ultima 8 · avisos 11). Council: **APROBADO**. **En develop; el bot productivo (main) NO se toca hasta merge.** |
| 2026-07-07 | **Inventario: fix — filtrar por sede "PLAZA NORTE" arrastraba "MAC PLAZA NORTE"** (reporte del usuario). El filtro de sede (`af()` en `inventario_multiaire.html`) siempre hacía match por **substring** (`r.sede.includes(sede)`), así que "PLAZA NORTE" colaba cualquier sede que la contuviera. Fix: se calcula `sedeExacta` = el texto del filtro coincide EXACTAMENTE con una sede existente (la elegiste del dropdown) → filtra por **igualdad**; si es parcial (estás escribiendo) → sigue por substring. Así elegir "PLAZA NORTE" muestra solo esa, pero escribir "plaza" sigue mostrando ambas (útil para explorar). Verificado con prueba de lógica (node). El resto de campos (equipo/área/marca) ya eran igualdad exacta. |
| 2026-07-07 | **`firestore.rules` fix — "Missing or insufficient permissions" al registrar salida/baja en lote (reporte de Leslie, ADMIN).** La UX (`insumos.html`, tabla `PERMS`) da `salida:1` a **ADMIN/SUPERVISOR**, pero las reglas tenían `insumos_instancias` → `allow delete: if isSuper()` → **solo SUPER_ADMIN podía borrar instancias**. Como una SALIDA/BAJA **borra las instancias** (`saveSalidaLote`, `batch.delete`; ídem salida individual y reversión de ENTRADA), cualquier ADMIN/SUPERVISOR recibía *Missing or insufficient permissions* al confirmar. Peor: el movimiento SALIDA se crea (`.add`) **antes** del borrado → cada reintento dejaba un **movimiento fantasma** (registro sin que las instancias salieran; Leslie generó 3 duplicados de LIMPIA CONTACTOS con LIC-007/008/009 aún vivas). Fix: `insumos_instancias` → `allow create, update, delete: if canWrite('insumos')` (incluye ADMIN vía `isAdmin()` y SUPERVISOR con la app) — borrar una instancia es **operación normal** del módulo (el bien se consumió/entregó), mismo criterio ya usado en `mtto_actividades_equipo`/`mantenimiento_fotos`/`mtto_plan`. La UX sigue gateando quién hace salida/baja (`can()`). **Desplegado a prod** (Admin SDK `deploy_rules.js`, ruleset `62ee0e4d`; respaldo del previo en `/tmp/firestore.rules.live-*`; afecta develop+prod por igual). Diagnóstico verificado leyendo Firestore (`migrar_db`): 3 movimientos fantasma + 0 instancias huérfanas. **Cierre:** tras el deploy, Leslie repitió la salida y **funcionó** (movimiento `s27M…`, 21:43, borró LIC-007/008/009); se borraron los **3 duplicados fantasma** de las 21:28 (con respaldo `migrar_db/backup_fantasmas_*.json`), conservando la salida real → queda 1 movimiento consistente. |
| 2026-07-07 | **Insumos: guard anti-doble-click en "Nuevo ítem / editar ítem"** (`saveInsumo`, reporte del usuario: "cuando creo ítem no debo poder guardar 2 veces"). Al crear, el docId es `'INS-'+Date.now()` → dos clicks separados por unos ms generaban **dos docId distintos = dos ítems duplicados** (y si había stock inicial, duplicaba las unidades). Fix: el botón Guardar gana `id="btn-save-insumo"`; `saveInsumo` añade el guard reentrante `if(btn&&btn.disabled)return;` (tras las validaciones, para que un `return` de validación no deje el botón trabado), deshabilita el botón antes del `try` ("⏳ Guardando...") y lo re-habilita en `finally` — mismo patrón que ya tenían `saveEntrada`/`saveSalida`/`saveSalidaLote` (era el único modal de guardado sin el guard). Sintaxis validada (`node --check` sobre el script inline). |
| 2026-07-07 | **Insumos: cobertura COMPLETA del guard anti-doble-click en todos los modales** (pedido del usuario: "verificar que ningún modal pueda hacer eso, para evitar duplicado de escrituras"). Auditados los **13** botones de guardado/confirmación de modales de `insumos.html`. Ya protegidos (9): `saveInsumo`/`saveEntrada`/`saveSalida`/`saveTransferencia`/`saveSalidaLote`/`saveTransfLote`/`saveSolicitud`/`confirmarAprobacionPaquete`/`saveTransfPaquete` (este último con la bandera de módulo `_savingPkgTransf` + `try/finally` que envuelve todo el cuerpo — sus `return` de validación disparan el finally, no traba el botón). **Faltaba el guard en**: `savePaquete` (id `btn-save-pkg`; crea/edita paquete + reasigna instancias), `updateInstancia` (id `btn-save-inst`; **duplicaba el movimiento `ACTUALIZACION`** con `.add()`), `guardarEdicionMovimiento` (id `btn-save-editmov`; doble update de instancia+movimiento) — a los 3 se les añadió `id` al botón + guard reentrante `if(btn&&btn.disabled)return;` (tras validaciones) + `disabled` antes del `try` + re-habilitar en `finally`. `confirmarImportInsumos` ya deshabilitaba+`finally` (e importa por docId determinístico con `merge:true`, idempotente); se le sumó el reentrante por uniformidad. Sintaxis validada (`node --check`). *(Fuera de alcance —no son modales de formulario, son botones de acción en tablas—: `revertirMovimiento` (no idempotente: restaura instancias) y los `delete*` (idempotentes); se pueden endurecer aparte si se quiere.)* |
| 2026-07-07 | **Insumos: auditoría profunda del tab Movimientos + tanda de fixes** (pedido del usuario: "revisa bien todo el módulo, el tab de movimientos, la lógica, encuentra vacíos, audita doble"). Hallazgos corregidos: **(A1 · atomicidad, la raíz del incidente de Leslie)** el movimiento y la instancia se escribían en operaciones separadas → un fallo parcial descuadraba stock↔historial (movimiento fantasma o instancia sin registro). `saveSalida`, `saveSalidaLote`, `saveEntrada` y `crearInstanciasIniciales` ahora escriben **movimiento + instancia(s) en un `db.batch()` atómico** (movRef pre-generado con `.doc()`; en lote el movimiento va en el primer bloque ≤449, todo-o-nada hasta 449 uds). **(A2)** `aprobarSolicitud` no tenía guard ni revalidación → doble-click duplicaba la salida/transferencia real; se añadió Set `_procesandoSolic` + `if(estado!=='PENDIENTE')return` + verificación de que la instancia exista; `confirmarRechazo` revalida estado. **(A3 · XSS almacenado)** `renderMovimientos` interpolaba `motivo`/`itemNombre`/`responsable`/`cambios`/`instancias`/`paqueteNombre`/`guiaNumero`/`motivoRechazo` sin escapar (el *motivo* es texto libre) → ahora todo pasa por `escB()`. **(M3)** `revertirMovimiento` (botón de tabla) gana guard `_revirtiendo`. **(B4)** `guardarEdicionMovimiento`/ACTUALIZACION verifica que la instancia exista antes del `update`. Sintaxis validada (`node --check`). **Pendientes documentados (NO tocados, requieren tu decisión):** **M1** — el flujo SOLICITUD (PERMS da `solicitarSalida:1` a TECNICO y muestra los botones, pero `firestore.rules` bloquean `insumos_movimientos.create` a quien no es `canWrite('insumos')` → el técnico recibiría *Missing or insufficient permissions*; decidir: ocultar el flujo en la UX o habilitar reglas para `SOLICITUD_*`); **M2** — la generación de id de instancia (`get`→`set`) sigue sin transacción (race teórica multi-usuario José+Adrián; el `while` con verificación + el batch atómico ya la mitigan, pero el fix 100% es `runTransaction` como en inventario, más invasivo); **B3** — `resetStockItem` borra instancias sin registrar movimiento (sin trazabilidad). **Auditoría Codex (Council):** ver commit/veredicto. |
| 2026-07-07 | **Insumos: ciclo Council (Codex) sobre la auditoría de movimientos + M1 resuelto.** El Council RECHAZÓ dos veces (bien: encontró hallazgos reales) y se corrigieron: **(1ª ronda)** el batch atómico con `qty≥500` excedía el límite de 500 ops de Firestore, y el `while` de reserva de ids podía crear **menos** instancias que las pedidas silenciosamente. **(2ª ronda)** aun troceado, en ENTRADA el movimiento del primer batch declaraba stock que un batch posterior podía no crear; y si `crearInstanciasIniciales` fallaba, el ítem quedaba creado bloqueando el reintento. **Fix definitivo:** se **valida ≤449 uds por operación** (`saveEntrada`/stock-inicial de `saveInsumo`/`saveSalidaLote`) → el batch es **siempre único y atómico**, sin descuadre (el inventario real son decenas de unidades; entradas mayores se dividen); la reserva de ids **aborta sin escribir** si no logra las qty (no crea parcial); y el stock inicial va en su **propio try/catch** (si falla, el ítem ya creado se conserva, se avisa "regístralo como Entrada", no bloquea). Además XSS: se escaparon también `cantInfo`/`unidad` y se saneó la clase `tCls`. **M1 (decisión del usuario: ocultar el flujo SOLICITUD):** flag **`FLUJO_SOLICITUDES=false`** en `can()` → devuelve 0 para `solicitarSalida`/`solicitarTransf`/`aprobar`, ocultando todos los botones (Solicitar salida/baja/transf/entrada/paquete, Solicitudes pendientes, ✓Aprobar/✗Rechazar). Elimina el *Missing or insufficient permissions* que recibía un técnico y hace irrelevante la concurrencia de aprobación. Reversible (poner `true` + ajustar reglas + hacer aprobar/rechazar transaccional). **M2** (id de instancia sin `runTransaction`) queda mitigado y documentado como mejora futura; **B3** (`resetStockItem` sin movimiento) documentado. Sintaxis validada; Council final: ver veredicto. |
| 2026-07-07 | **Insumos: 2ª ronda de hardening XSS — cierre COMPLETO del módulo** (continuación de la A3 del tab Movimientos; commit `925178d`). La 1ª ronda escapó los `renderX()` de tablas pero dejó vivos varios sinks; esta cierra todo el archivo tras **3 rondas de doble auditoría Codex** (6 pasadas; veredicto final **APROBADO**). Cambios: **(1)** helper central **`optHtml(value,label,extra)`** que escapa **todas** las `<option>` (sede/personal/catálogo/instancia/roles) + **`sedeOpts()`** blindado — antes un nombre de ítem/sede con `</option><img onerror>` inyectaba al abrir cualquier modal. **(2)** `escB`/`escJ` en **TODOS** los `inst.id`/`item.id`/`pkg.id`/`m.id` en handlers `on*` (escJ), atributos `data-id`/`value` (escB) y texto (escB) **en todo el archivo** — renderCatalogo/Instancias/StockSede/Movimientos/Paquetes, detalle de técnico, `instThumb`, flujo `apkg`, lote (`addInstLote`/`removeInstLote`), solicitud-paquete, y los paneles `info.innerHTML` de salida/transf/solicitud (motivo: aunque la app genera IDs seguros, un doc id de Firestore/backup/consola puede traer HTML). **(3)** **`renderUserPill`**: escapa nombre/email; `rol` saneado contra allowlist antes de la clase CSS. **(4)** **Allowlist `^[A-Za-z0-9_-]{1,40}$`** para el `ID` importado del XLSX (`_cmValidar`) → corta la inyección en el origen, además del escape en render. **(5)** **`fotoSrc()`** en el chokepoint **`openFotoSrc`** del lightbox y en **`_matPrepare`** (export PDF/PNG con fotos); `openInstFotoLightbox` delega en `openFotoSrc` → ninguna URL remota de un dato manipulado llega a `img.src`. **(6)** **`escJ` reforzada** con `\n`/`\r`/U+2028/U+2029. **(7)** **`nivelSafe(v)`** (`Number.isFinite`+clamp 0-100) en `sNivel`/`renderRangoBadge`/`renderRangoBadgeSede` → un `nivel` string manipulado no rompe el promedio (concatenación) ni la clasificación. **(8)** **`csvGuard()`** anti-inyección de fórmulas (`= + - @` TAB/CR/LF → prefija `'`) + quoting con `\r` en los 2 exports CSV (Phomemo y material). `maUser.picture` (avatar) queda con `escB` a propósito: es URL remota legítima de Google, `fotoSrc` la rompería. Codex validó: `escB`/`escJ`/`optHtml`/`fotoSrc`/`nivelSafe`/`csvGuard` correctos, sin regresión (`.value`/`dataset`/comparaciones de id intactas, sin `&amp;` visible). Sintaxis validada (`node --check` sobre el script inline). **En develop.** |
| 2026-07-07 | **Mantenimiento: hardening XSS — escape consistente de datos de Firestore** (commit `c240011`; auditoría a raíz de revisar la deuda transversal tras cerrar insumos). `mantenimiento_multiaire.html` **ya tenía** los helpers `_actEsc` (escape HTML) y `_jsq` (`_actEsc(JSON.stringify(x))` para handlers) pero los aplicaba **solo en el checklist principal y el historial**; el resto renderizaba datos crudos. Cerrado tras **3 rondas de auditoría Codex (Council)**, veredicto final **APROBADO**. Cambios: **(1)** `_actEsc` reforzado con la comilla simple `'` (seguro también en atributos con comilla simple; no rompe `_jsq`, cuyos atributos son de comilla doble). **(2)** Datos de equipo (`eq.nombre`/`eq_id`/`area`/`tipo`, de la colección `inventario`) escapados en el **selector de equipos**, **tarjetas del dashboard** (`eq-dash`), **barras "Top Pendientes"/"Por Sede"**, **tab Global** y **print-detail del PDF** (~15 sitios). **(3)** Handlers `onclick`/`onchange` con datos (`toggleTipo`/`toggleEqSel`/`ftBorrar`) → `_jsq`. **(4)** **Export PDF**: `currentSede` + `updByStr` en header, footer **y** `<title>` con `_actEsc` (el `document.write` del reporte era un sink). **(5)** **`renderUserPill`**: `maUser.picture` (atributo, como en insumos — es URL remota de Google, no se le aplica un allowlist que la rompería), `name`/`email`/iniciales con `_actEsc`. **(6)** **Panel de Gestión de accesos** (los 3 sub-render `maToggleRole`/`changeUserRolByEl`/`maChangeRol`): `u.email`/`e`/`rol`/`app.id`/`app.label` escapados; los handlers ya usaban el patrón seguro `data-email` + `this.dataset` (no interpolan en el `onclick`). **(7)** Los **5 `catch`** que volcaban `e.message` a `innerHTML` → `_actEsc(e.message)`. Los `toast`/`.textContent`/`confirm` y el texto de WhatsApp (`msg +=`) quedan sin tocar: no son sinks HTML. Codex validó sin regresión (acentos y emails legítimos intactos, el DOM decodifica las entidades). Sintaxis validada (`node --check`, 3 scripts inline). **En develop.** *(Nota colaborativa: mantenimiento puede ser territorio de Adrián — el arreglo fue por orden explícita del usuario; conviene anotarlo en el Tablero de equipo para no pisarnos.)* |
| 2026-07-08 | **Merge develop→main → hardening XSS de mantenimiento + insumos EN PRODUCCIÓN** (merge `a132251`, `--no-ff`, orden explícita del usuario). Subió los 5 commits pendientes de develop (todos Council APROBADO): los **3 de insumos** (2ª ronda de hardening XSS que cerró el módulo — `optHtml`/`escB`/`escJ`/`fotoSrc`/`nivelSafe`/`csvGuard` — + transacciones atómicas movimiento+instancia + reserva de ids race-safe ≤449/op + flujo SOLICITUD oculto) y los **2 de mantenimiento** (escape consistente de datos de Firestore, ver entrada previa). Antes del merge se verificó que develop era superset limpio de main (`git diff develop...origin/main` vacío = main no tenía nada que develop no tuviera; dry-run sin conflictos; los 55 commits de main-no-develop son todos merge commits, patrón normal del repo). Deploy de producción **OK** (workflow success); verificado **HTTP 200 + marcadores del hardening presentes** en `app.multiaire.com.pe/mantenimiento_multiaire.html` (idéntico a develop) y `/insumos.html`. `main == develop` en contenido tras el merge. |
| 2026-07-08 | **Mantenimiento: fix — el % del tab Global reflejaba mal el avance** (commit `56e7165`, reporte del usuario con captura: PLAZA NORTE con 22 equipos / 4 parciales mostraba **0%**). El tab **Global** (tarjetas de todas las sedes, `renderGlobal`) calculaba el `%` de cada sede como **equipos 100%-completos / total** (`100*g/tot`) → ignoraba todo el trabajo **parcial**, así que una sede sin equipos terminados del todo salía 0% aunque tuviera actividades hechas. Inconsistente con el **Dashboard** (tab de la sede actual, `renderDashboard`), que ya mostraba el **avance ponderado real**. Fix: `renderGlobal` ahora replica la fórmula del Dashboard — por equipo `eqPct = getPct` (ponderado por minutos, con fallback a peso igual) y luego ponderado por `PESO_TIPO` entre equipos (`pct = round(totalAvance/totalPeso)`) → con el escenario de PLAZA NORTE pasa de 0% a **9%**, idéntico al Dashboard de esa sede. El semáforo 🟢🟡🔴 (`g/y/rr`, conteo de equipos completos/parciales/en-cero) **se mantiene** — sigue siendo útil y correcto. **Council (Codex) APROBADO CON OBSERVACIONES**: el hallazgo Media (un equipo solo-marcar `st===true` **sin** actividades configuradas debía dar 0% como `getPct`, que retorna 0 si `!tareas` **antes** de mirar el estado, no 100%) se cerró envolviendo el cálculo en `if(nT)`. Verificado que la nueva fórmula replica `getPct` en los 5 casos (parcial por minutos / solo-marcar con tareas / edge solo-marcar sin tareas / objeto sin minutos / nada). Mi commit previo de hardening XSS (`c240011`) **no** tocó este cálculo (solo `_actEsc`); el bug era preexistente del tab Global (desde su creación 2026-07-04). Sintaxis validada (`node --check`, 3 scripts). **En develop.** **EN PRODUCCIÓN** (merge `5bc0173`); verificado en vivo (MEGAPLAZA 0% · PLAZA NORTE 6% · PRIMAVERA 2%, coincide con el Dashboard). |
| 2026-07-08 | **Mantenimiento: fix — el semáforo 🟢🟡🔴 contaba mal los parciales con marcas obsoletas** (commit `2d75091`, a raíz de que el usuario pidió revisar el conteo del semáforo tras el fix del %). En `renderGlobal` (tab Global) y en el **historial por equipo**, el conteo `done` usaba `Object.keys(st).filter(k=>st[k]===true)` / `Object.values(st).filter(...)` → contaba **todas** las marcas `true` de `tareaStatus`, incluidas las **obsoletas** de actividades ya quitadas del equipo (índices fuera del rango vigente `0..nT-1`, que quedan si se edita la lista de actividades después de marcar). Efectos: **falso 🟡** (equipo con 0 actividades vigentes hechas pero con marcas viejas contaba como parcial), **falso 🟢** (`done` inflado ≥ `nT` lo marcaba completo aunque sus actividades actuales no estuvieran hechas), y el historial mostraba cosas como "5/2 actividades" y `%` > 100%. Fix: `done = tareas.filter((_,i)=>st[i]===true).length` (solo índices vigentes), **igual que `getPct`/`getSemaforo`/checklist/Dashboard**, que ya contaban bien (el sidebar "PROGRESO DEL PERÍODO" vía `getSemaforo` **nunca** tuvo el bug). Ahora el semáforo del Global es consistente con el `%` de la tarjeta. Verificado con test (6 casos: marcas obsoletas → 🔴/🟡 correctos, normales sin regresión). **Council (Codex) APROBADO CON OBSERVACIONES**: la única observación es un edge **preexistente y degenerado** (tipo con plantilla **vacía** `[]` + equipo solo-marcar → verde en Dashboard, rojo en Global por la inconsistencia de `actividadesDe`, que normaliza vacío a `null` salvo en el camino sin override) que **no aplica a datos reales** (todos los tipos tienen actividades) y era así desde antes; **no se tocó**. Sintaxis validada (`node --check`). **En develop.** |
| 2026-07-08 | **Mtto grupal + KPIs — Etapa 1: asignación a CUADRILLA en el itinerario** (`itinerario.html`; plan en `~/.claude/plans/clever-meandering-charm.md`, memoria `project_mtto_grupal_kpis`). Primera de 4 etapas (0/2/3 pendientes). Hasta ahora una actividad de mtto preventivo se asignaba a **un** técnico; ahora se puede asignar a **varios** (cuadrilla co-responsable). Cambios en `itinerario.html` (NO toca `firestore.rules` ni producción — solo escribe en `mtto_plan`, colección ya existente): **(1)** `mtto_plan` pasa a **multi-técnico retrocompatible** — el doc gana `tecnicoIds[]`, `tecnicos[{id,nombre}]`, `modo('individual'|'grupo')` y `minutos`, **conservando** los escalares `tecnicoId`/`tecnicoNombre` (= primer técnico) para no romper docs/lecturas viejas; id determinístico `EQ|PERIODO|AÑO|IDX` e idempotencia intactos. **(2)** Helpers nuevos: `planTecnicos(p)` (lee técnicos de un doc: arrays nuevos → escalar viejo → `[]`; cae al escalar si el array viene sin nombres), `planTecLabel(tecs)` (`"A + B + C"`), `_tecInfo(tecs)` (normaliza el array elegido → campos del doc + retrocompat). **(3)** `pedirTecnicos(personal, subtítulo, cb)`: el `pt-overlay` pasa de "un botón por técnico, elige uno al click" a **checkboxes + botón "✓ Asignar" + atajo "Todos"** (1 marcado = individual, 2+ = cuadrilla); el `cb` recibe el **array** de técnicos. **(4)** `asignarSeleccionadas`/`asignarPlan` (drag&drop y multi-selección) usan el nuevo selector cuando el bloque tiene >1 técnico; `_finalizarMulti`/`_finalizarAsignacion` cambian de firma `tec`→`tecs` (array) y escriben los campos nuevos + escalares. **(5)** `pendientesDeSede` resuelve los **minutos** por actividad (arrays paralelos de `tareas_config`, mismo criterio que `actividadesDe` de mantenimiento: por índice vigente, default 5) y los lleva al plan (`p.minutos`) — insumo para los KPIs de carga (Etapa 3). **(6)** `renderPlanChips` agrupa por `planTecLabel(planTecnicos(p))`: cuadrilla **`👥 A + B + C`**, individual **`👷 Nombre`**; el `data-tec` del chip lleva la etiqueta de cuadrilla → los exports WhatsApp/PNG/PDF (que agrupan por `data-tec`) siguen funcionando sin tocarlos. Render escapado con `_pEsc`. Verificado: 14 tests de helpers (retrocompat escalar↔array, `_tecInfo`, minutos == `actividadesDe`) + sintaxis (`node --check`, 2 scripts). **Council (Codex) APROBADO CON OBSERVACIONES** — 2 hallazgos Baja aplicados: (a) `planTecnicos` cae al escalar si `tecnicos[]` viene sin nombres; (b) `_ptCerrar()` limpia `_ptCb`/`_ptPersonal` al cerrar el selector con clic-fuera (no dejar estado colgado). **En develop, sin merge a main (a verificar por el usuario). Etapas 0/2/3 pendientes.** *(Nota territorio: `itinerario.html` es de Adrián — tiene un `git stash` local pausado; registrado en el Tablero para reconciliar. Se hizo por orden explícita del usuario.)* |
| 2026-07-08 | **Mtto grupal + KPIs — fix de la auditoría de INTEGRACIÓN (Council RECHAZADO→resuelto) antes del merge a main** (`mantenimiento_multiaire.html` + `api/_lib/mtto.js`). La auditoría de integración de Codex previa al merge encontró (**Alta**) que la app atribuía la ejecución desde el **caché local `planDocs`** (cargado por `loadPlanBadges`): si se asignaba en el itinerario con la app ya abierta, o se marcaba antes de que cargara el caché, `plan` salía `null` → la ejecución se escribía **sin técnico** y **luego se borraba el `mtto_plan`** = pérdida silenciosa e irreversible de la atribución/KPI; y (**Media**) tanto app como bot borraban el plan aunque la escritura de la ejecución fallara. Fix: **`registrarEjecucion`** (app) pasa a **async** y lee el `mtto_plan` **autoritativo de Firestore** (fallback al caché solo si la lectura falla) y **borra el plan SOLO tras** registrar la ejecución OK (si falla, conserva el plan → recuperable; el barrido del itinerario es la red de seguridad); se retira `limpiarPlanDe` (su rol de borrado+caché lo asume `registrarEjecucion`; los toggles hacen un borrado optimista de `planBadges` para que el 🗓 desaparezca al instante). **Bot `guardarRegistro`**: escribe la ejecución por índice, borra del plan **solo las que quedaron registradas** y **loguea los fallos** con `eqId/periodo/anio/idx`. **2ª ronda** (la re-auditoría encontró 2 gaps más del mismo tema): (**Alta**) el **barrido `cargarPlanDia` del itinerario** borraba planes ejecutados sin verificar la ejecución → ahora, antes de borrar un plan hecho, **asegura que exista `mtto_ejecuciones/{p._id}`** (si falta, la escribe **desde el propio plan** —que trae la atribución— con `origen:'SYNC'`) y **solo entonces borra** el plan; si la escritura falla (p.ej. sin permiso mantenimiento) conserva el plan → esto convierte el barrido en la verdadera red de seguridad de atribución. (**Media**) guarda **anti-carrera marcar→desmarcar** en `registrarEjecucion`: tras la lectura async del plan, si la actividad ya no está marcada, aborta (no escribe la ejecución ni borra el plan). **3ª ronda** (la 3ª auditoría encontró que la idempotencia era solo de ID, no de contenido): un 2º `.set()` (app/bot/duplicado) podía **sobrescribir** una atribución ya correcta con una espontánea (plan ya consumido → `plan=null`). Fix: las 3 puntas escriben con **create-if-absent en TRANSACCIÓN** — leen `ejecRef`+`planRef`, escriben la ejecución **solo si no existe** (nunca pisan la atribución canónica) y borran el plan atómicamente; la guarda anti-carrera se mueve dentro de la tx. El barrido usa `Promise.allSettled` y retira de la vista solo las aseguradas OK. Sintaxis `node --check` (3+2 scripts + mtto.js); `test_mtto.mjs` **77/77**. **Council (4ª auditoría) → APROBADO** (ver entrada del merge). |
| 2026-07-08 | **`firestore.rules` desplegado a prod — regla `mtto_ejecuciones` VIVA** (ruleset `38608b68`, vía `~/Documents/migrar_db/deploy_rules.js` con Admin SDK, orden explícita del usuario). Habilita la escritura de ejecuciones desde la app (antes en `.catch` por permiso denegado). Cambio **aditivo** (verificado por diff: el único delta vs el ruleset vivo era la regla nueva). Respaldo del ruleset previo en `~/Documents/migrar_db/_rollback_rules_pre_mtto_ejecuciones.txt` (+ copia en `/tmp/firestore.rules.live-*`). Afecta el Firestore compartido develop+prod. Verificado: `mtto_ejecuciones` presente en las reglas vivas. Con esto la feature "mtto grupal + KPIs" (Etapas 0/1/2/3) queda **plenamente operativa en develop** para la verificación del usuario. **Sin merge a main.** |
| 2026-07-08 | **Mtto grupal + KPIs — Etapa 3: dashboard de KPIs (tab 👷 Desempeño)** (`mantenimiento_multiaire.html`; cierra la feature de 4 etapas). Tab nuevo **👷 Desempeño** (junto a Checklist/Dashboard/Global/Historial) que muestra el desempeño de técnicos y cuadrillas leyendo **`mtto_ejecuciones`** (poblada por la Etapa 2). **Filtros:** período + año (re-consultan Firestore) y sede + tipo + técnico (re-filtran en cliente, patrón del tab Historial; las opciones se derivan de los datos). **KPIs:** actividades ejecutadas (planificadas vs espontáneas), tiempo estimado (Σ minutos → h), técnicos activos, % planificado; **ranking por técnico** (nº actividades, individual/cuadrilla, tiempo — una actividad de **cuadrilla cuenta para cada integrante** = crédito al equipo, por diseño); y desgloses **por sede** y **por tipo**. Solo lectura (no escribe). **Query** `where(periodo==).where(anio==)` — solo igualdades → Firestore la sirve con índices de un solo campo (**sin índice compuesto**; el filtro por técnico va en cliente sobre `tecnicos[]`, no como `array-contains`, para no requerir índice). Todo el dato de Firestore escapado con `_actEsc` (incl. `value`+texto de las `<option>`). Verificado: 12 tests de la lógica de KPIs (cuadrilla-cuenta-a-cada-integrante, % planificado, por sede/tipo, sin técnico-fantasma, `fmtH`) + sintaxis (`node --check`, 3 scripts). **Council (Codex) APROBADO CON OBSERVACIONES** — 2 aplicados: (Media) token `_dspReq` anti-carrera si se cambia período/año rápido (una consulta vieja no pisa a la nueva); (Baja) texto del subíndice de cuadrilla más claro. **Nota UI documentada:** el cumplimiento vs lo planificado es "a la fecha" (el plan es estado vivo, se consume al ejecutar). **En develop, sin merge a main. Feature completa (Etapas 0/1/2/3) a la espera de tu verificación + el redeploy de `firestore.rules`.** |
| 2026-07-08 | **Mtto grupal + KPIs — Etapa 2: captura de la ejecución (app + bot)** (`mantenimiento_multiaire.html` + `api/_lib/mtto.js`). Al marcar una actividad de mtto preventivo (en la app o por el bot de WhatsApp) se escribe ahora un registro **persistente** en **`mtto_ejecuciones`** (base de los KPIs de la Etapa 3). Doc id determinístico `EQ|PERIODO|AÑO|IDX` (idempotente: re-marcar no duplica; **desmarcar borra**). Guarda `eq_id/nombreEq/tipo/area/sede/periodo/anio`, `tareaIdx/tarea`, `minutos`, `tecnicoIds[]/tecnicos[]/modo`, `planificada`(bool), `fechaEjec`, `registradoPor`, `origen('APP'|'BOT')`, `ts`. **Atribución derivada del PLAN** (`mtto_plan` de la Etapa 1), NO de quién marca: planificada individual→asignado, cuadrilla→los co-responsables; **sin plan** → en el bot al técnico que reporta (WhatsApp lo identifica), en la app a nadie (la web no tiene `colabId`); plan a nivel de bloque sin técnico → sin crédito (`[]`). **App:** `loadPlanBadges` cachea el doc de plan completo (`planDocs`); `registrarEjecucion(eqId, idxs, done)` (fire-and-forget) escribe/borra **antes** de `limpiarPlanDe` (orden leer→escribir→borrar); `toggleTarea`/`toggleSoloMarcar` lo invocan; `limpiarPlanDe` limpia el caché local (el 🗓 desaparece al ejecutar). **Bot** (`guardarRegistro`): antes de borrar el plan lee los docs de plan de las marcadas, escribe las ejecuciones (helpers puros exportados `ejecTecnicos`/`docEjecucion`), luego borra; `actividadesDeEquipo` refactorizado a `_resolverActs` que resuelve actividades **y minutos** en paralelo (nuevo `minutosDeEquipo`, retrocompat del array de actividades); `ses.area` guardado. Todo en fire-and-forget/try-catch → **nunca frustra el marcado/registro**. Verificado: suite `test_mtto.mjs` **77/77** (+12: atribución escalar/array/grupo/sin-plan/bloque-sin-técnico + forma del doc con minutos del plan vs plantilla) y las 8 suites del bot verdes (obs_v2 15 · asistencia 54 · fase4 17 · fase5 22 · avisos 11 · foto_ultima 8 · tier2 9 · tier3 7); sintaxis `node --check`. **Council (Codex) APROBADO CON OBSERVACIONES** — 1 hallazgo Baja aplicado (bot: con plan pero sin técnico → `[]`, no al reportante; coherente con la app). ⚠️ **La escritura de la app a `mtto_ejecuciones` requiere la regla de la Etapa 0 desplegada** (deploy pendiente de tu OK): sin ella, el marcado sigue funcionando pero la ejecución no se guarda (está en `.catch`); el bot escribe por Admin SDK, sin depender del deploy. **En develop, sin merge a main.** |
| 2026-07-08 | **Mtto grupal + KPIs — Etapa 0: colección `mtto_ejecuciones` (reglas + backup)** (`firestore.rules` + `configuracion.html`). Prepara la colección NUEVA **`mtto_ejecuciones`** — el registro **persistente de lo EJECUTADO** (base de los KPIs de desempeño de la Etapa 3; a diferencia de `mtto_plan`, que es estado vivo y se borra al ejecutar, esta es append/idempotente y sobrevive). Doc id determinístico `EQ|PERIODO|AÑO|IDX` (idempotente: marcar dos veces no duplica; desmarcar borra). **(1) `firestore.rules`:** regla nueva `mtto_ejecuciones` → `read: authorized()`, `create/update/delete: canWrite('mantenimiento')` — mismo criterio que `mtto_actividades_equipo`/`mantenimiento_fotos`/`mtto_plan` (la app escribe al marcar y borra al desmarcar; el bot usa Admin SDK que ignora reglas). **Aditiva** (no toca ninguna regla existente); necesaria por el catch-all fail-closed. **(2) `configuracion.html`:** backup export/import (patrón JSON-doc idéntico a `mtto_plan`) — `get()` + variable en el `Promise.all`, bloque export (ID/PERIODO/DATA), `'mtto_ejecuciones.csv'` en `expected`, `'mtto_ejecuciones'` en `NUEVAS` (27ª colección), bloque import (`batchWrite`/`_docId`) y línea visual. Sintaxis validada (`node --check`, 2 scripts). **Council (Codex) APROBADO** (0 hallazgos). ⚠️ **Requiere redeploy de `firestore.rules`** (afecta el Firestore compartido develop+prod; es aditivo y reversible —rollback guardado— pero el deploy quedó **PENDIENTE de autorización explícita del usuario**: sin la regla viva, las escrituras a `mtto_ejecuciones` de la Etapa 2 fallarán con permisos). **En develop, sin merge a main.** |
| 2026-07-08 | **Merge develop→main → feature "mtto grupal + KPIs" (Etapas 0/1/2/3) EN PRODUCCIÓN** (merge `5ffd5be`, `--no-ff`, orden explícita del usuario). Subió los commits de las 4 etapas + el deploy de reglas + los 2 commits de fixes de la auditoría de integración. Asignar actividades de mtto preventivo a **1 técnico (individual)** o **2+ (cuadrilla)** en el itinerario, **capturar quién ejecutó** (colección `mtto_ejecuciones`, atribución del plan, escritura **create-if-absent en transacción** que nunca pisa la atribución canónica) y ver **KPIs de desempeño** (tab 👷 Desempeño). Retrocompatible con los `mtto_plan` escalares viejos. Antes del merge: develop verificado como superset limpio de main (0 commits no-merge de main fuera de develop; prueba de merge en worktree temporal sin conflictos). **4 rondas de auditoría de integración Codex** (converge a APROBADO; obs opcional: ventana sub-ms desmarcar, autocorregible). Regla `mtto_ejecuciones` ya desplegada (ruleset `38608b68`). Deploy de producción **OK** (workflow "Deploy Main → Vercel Producción" success); verificado **HTTP 200 + marcadores** (`Desempeño`/`registrarEjecucion`, `planTecnicos`/`pedirTecnicos`, `mtto_ejecuciones`/`renderDesempeno`) en `app.multiaire.com.pe` y `multiaire-peru-app.vercel.app`. `main == develop` en contenido. ⚠️ **Adrián: `itinerario.html` y `mantenimiento_multiaire.html` cambiaron bastante — `git pull` obligatorio antes de tocarlos; tu stash pausado de itinerario casi seguro no aplica limpio (registrado en el Tablero).** |
| 2026-07-08 | **Mtto grupal + KPIs — ajuste UX del tab 👷 Desempeño: filtro por técnico = solo su fila** (`mantenimiento_multiaire.html`, `renderDesempenoFiltrado`). Al probar la feature en vivo, seleccionar un técnico en el filtro seguía mostrando en el ranking a sus co-responsables de cuadrilla (porque el filtro acotaba las *actividades* donde participó, y cada actividad de cuadrilla acredita a cada integrante). Ahora, con un técnico elegido, el ranking se limita a **su fila** (`if(tec) ranking = ranking.filter(r => r.nombre === tec)`) — desempeño individual, "Técnicos activos" pasa a 1; las actividades/tiempo/por-sede/por-tipo siguen siendo las suyas. Sin técnico → ranking completo (sin regresión). Council/Codex **APROBADO** sin hallazgos; `node --check` OK. **En develop, sin merge a main.** |
| 2026-07-08 | **Envío de itinerario por técnico — Etapa A: "Confirmar versión"** (`itinerario.html`). Base de la feature nueva (enviar a cada técnico sus tareas por WhatsApp desde el bot): una versión del itinerario se puede marcar como **final/confirmada** — requisito para poder enviarla. Botón **✓ Confirmar versión** + badge en el header (✓ Versión confirmada / ✎ Borrador); `confirmarVersion()` escribe `confirmado/confirmadoPor/confirmadoTs` en `bd_itinerarios/{id}_v{version}` vía nuevo `window.__fb.updateDoc` (merge parcial). Como **Editar siempre crea una versión nueva** (`version+1`, nace sin confirmar), editar obliga a **reconfirmar** — no se puede "colar" un cambio no revisado. Retrocompat (docs viejos sin `confirmado` = Borrador). Council/Codex **APROBADO CON OBSERVACIONES** (Media: race si se cambia de versión durante el await → corregido capturando id/version antes del await y re-render solo si sigue visible); `node --check` OK. **En develop, sin merge a main.** ⚠️ **Adrián: `itinerario.html` volvió a cambiar — `git pull` antes de tocarlo.** |
| 2026-07-08 | **Envío de itinerario por técnico — Etapa B: armado del mensaje (lib pura + tests)** (`api/_lib/itinerario_envio.js` nuevo + `plan_dia.js` exporta `bloqueIncluye`). Función PURA `mensajesPorTecnico(bloques, planes, opts)` que, a partir de `bd_bloques` + `mtto_plan`, arma el mensaje de WhatsApp de **cada técnico**: sus sedes/horarios del día y sus actividades separadas en **🔧 individuales** y **👥 en cuadrilla (con co-responsables)**; un técnico en un bloque sin actividades de mtto recibe igual sede+horario+detalle. Retrocompat con `mtto_plan` escalar (`tecnicoId/tecnicoNombre`), excluye bloques CANCELADO, quita el prefijo RIPLEY, formatea horas decimales. **31/31 tests** (`migrar_db/test_itinerario_envio.mjs`: individual/cuadrilla/legacy/sin-actividades/cancelado/multi-sede/orden/actualización). Council/Codex **APROBADO CON OBSERVACIONES** (2 Baja: `Number(nBloque)`→NaN robustecido con `numOr`; plan-stale-de-bloque-cancelado se cierra en Etapa C por el filtrado de carga). `node --check` OK. **En develop, sin merge a main.** |
| 2026-07-08 | **Envío de itinerario por técnico — Etapa C: endpoint + registro + botón** (`api/enviar_itinerario.js` nuevo, `firestore.rules`, `configuracion.html`, `itinerario.html`). El botón 💬 WhatsApp del itinerario ahora es **📲 Enviar a técnicos** (solo si la versión está confirmada y el usuario es ADMIN; si no, muestra "Confirmar versión"). Endpoint serverless que: gatea **ADMIN/SUPER_ADMIN** (Bearer idToken→verifyIdToken→rol en `usuarios/{email}`), exige versión **confirmada**, carga `bd_bloques`+`mtto_plan` (filtro estricto `itiId+itiVersion`), arma el mensaje de cada técnico (Etapa B), mapea técnico→`telefono` de `maestros_personal`, y **envía por WhatsApp**: texto libre (ventana 24h, caso normal) con **fallback a plantilla** (`WHATSAPP_TEMPLATE_ITINERARIO`, fuera de 24h). **Registro/auditoría** en colección nueva **`itinerario_envios`** (creada ANTES del envío con estado `en_progreso`→`completado`; si falla el pre-registro, aborta 503 sin enviar) → detecta **primer envío vs 🔄 actualización** (ignora pruebas). **Modo prueba** (envía todo a un número) restringido a **SUPER_ADMIN**, con target enmascarado y registrado. Regla `itinerario_envios` (create/update/delete `isSuper`; backend usa Admin SDK) + **backup** (6 touchpoints en configuracion.html). Council/Codex **RECHAZADO→APROBADO** en 3 rondas (Alta: modo prueba=exfiltración/spam → solo super+auditado; Alta: registro post-envío→duplicados → auditoría pre-envío bloqueante; +3 Media: regla, filtro estricto, gate UI=backend). `node --check` OK. ⚠️ **Regla `itinerario_envios` PENDIENTE de desplegar a prod** (requiere OK del usuario). **En develop, sin merge a main.** |
| 2026-07-08 | **Envío de itinerario por técnico — Etapa D: plantilla de Meta `itinerario_tecnico`** (`api/admin_plantillas.js`). Fallback FUERA de la ventana de 24h (aviso corto de 4 variables: nombre/fecha/nº actividades/sedes, coincide con los params del endpoint). El detalle completo va por texto libre dentro de la ventana (caso normal). **Activación (acción externa del usuario):** correr `admin_plantillas` con `accion:'crear'` (solo SUPER_ADMIN, usa el `WHATSAPP_TOKEN` de prod) → esperar aprobación de Meta → setear env `WHATSAPP_TEMPLATE_ITINERARIO=itinerario_tecnico`. Mientras tanto funciona por texto libre. Council/Codex **APROBADO**. `node --check` OK. **En develop, sin merge a main.** |
| 2026-07-08 | **Merge develop→main → "Enviar itinerario por técnico" (Etapas A/B/C/D) EN PRODUCCIÓN** (merge `b6cc26b`, `--no-ff`, orden de José "haz todo lo pendiente"; `main == develop`). El botón WhatsApp del itinerario ahora es **📲 Enviar a técnicos**: manda a cada técnico solo sus tareas (🔧 individuales + 👥 cuadrilla). **No dispara nada solo** — requiere versión confirmada + ADMIN + confirmación manual. Deploy de prod **OK**, verificado en `app.multiaire.com.pe` (endpoint `/api/enviar_itinerario` HTTP 405 = existe; botón presente). **Regla `itinerario_envios` DESPLEGADA a prod** (2026-07-08, ruleset `f2cb4e65`, orden explícita de José "despliega la regla"; respaldo del anterior en `/tmp/firestore.rules.live-*`). **PENDIENTES que solo puede hacer el usuario:** (1) **prueba en vivo** (modo prueba a un número, solo super); (2) opcional **crear/aprobar la plantilla de Meta** (super, token de prod; mientras tanto funciona por texto libre). ⚠️ **Adrián: `git pull` antes de tocar `itinerario.html`.** |
| 2026-07-08 | **Rendición de Caja: persistencia en Firestore + tab Historial + exports con fotos** (`comprobantes.html` + `firestore.rules` + `configuracion.html`; pedido de Adrián). Hasta ahora era una herramienta de captura sin guardado (escanear/exportar Excel en la sesión, nada persistía). **(1) Persistencia:** cada fila se guarda en la colección nueva **`comprobantes`** apenas Gemini responde (`fecha`/`fechaISO`/`tipo`/`numero`/`ruc`/`proveedor`/`descripcion`/`monto` + `createdAt/By`); editar un campo actualiza el doc al salir del campo (`onblur`, no por tecla — un solo write por edición); el ✕ de una fila borra el doc **y** su foto de Firestore (corregir un escaneo mal leído es operación normal del módulo, no un borrado admin). La foto va en colección aparte **`comprobantes_fotos`** (doc id = id del comprobante, patrón idéntico a `insumos_catalogo_fotos`). `fechaISO` (YYYY-MM-DD, derivada de la `fecha` DD/MM/YYYY que entrega Gemini) es necesaria porque un string DD/MM/YYYY no ordena igual que la fecha real — sin ella el filtro por rango del Historial habría comparado mal. **(2) Dos tabs** (patrón `.tab-btn`/`.tab-view`/`switchTab` traído de `mantenimiento_multiaire.html`): 🧾 **Escanear** (igual que antes, con "＋ Agregar fila" y "🧹 Limpiar tabla" — este último solo limpia la vista de la sesión, **no** toca Firestore, renombrado desde "🗑 Limpiar todo" para no sugerir que borra datos) y 🕘 **Historial** (rango Desde/Hasta sobre `fechaISO`, default 1º del mes actual→hoy; checkbox por fila + "seleccionar todo", mismo patrón que `toggleAllInstQR`/`selectedInstQRIds` de `insumos.html`). **(3) Columna Imagen** en ambas tablas (miniatura + lightbox, patrón `openInstFotoLightbox`). **(4) 4 exports** (solo en Historial; sin selección marcada exporta todo el rango visible): Excel-texto, Excel+fotos (Excel + un PDF aparte solo de fotos, 1 por página), PDF-texto (tabla dibujada en canvas→jsPDF, mismo patrón que el reporte de material de `insumos.html`: `_matPrepare`/`_matDraw*`, adaptado a `_cpBuildTablaPdf`/`_cpDraw*`) y PDF+fotos (mismo PDF de tabla + páginas de fotos al final). Se agregó `jspdf.umd.min.js` (antes solo tenía `pdf.js`, que lee PDFs, no los genera). **`firestore.rules`:** reglas nuevas para `comprobantes`/`comprobantes_fotos` — `delete` va con `canWrite('comprobantes')` (no `isSuper()` como la mayoría), mismo criterio que `mantenimiento_fotos`. **`configuracion.html`:** backup export/import completo (`comprobantes.csv` con metadatos, `comprobantes_fotos.csv` minimal ID+FOTO) + ambas colecciones agregadas a `NUEVAS` para que "Reemplazar todo" con un ZIP viejo las vacíe. Sintaxis validada (`node --check`, ambos HTML) y reglas compiladas (`firebase deploy --dry-run`). **Reglas YA desplegadas** a `multiaire-fee43` (`firebase deploy --only firestore:rules`, orden explícita de Adrián). Rama `feature/comprobantes-historial-firestore`. **Pendiente:** Adrián probando en vivo. |
| 2026-07-08 | **Envío de itinerario por técnico — fix del FORMATO DE HORA** (`api/_lib/itinerario_envio.js`; detectado por José en la **prueba en vivo**: el mensaje mostraba "⏰ 00:20 – 00:44" en vez del horario real). Causa: **error de unidades**. `horaInicio`/`horaFin` de `bd_bloques` se persisten como **fracción del día** (0..1) vía `horaToDecimal() = (h*60+m)/1440`, y la app los formatea con `fmtHora()` = `× 24 × 60`. Mi helper `hhmm` usaba `decimalAHHMM()` (de `fecha.js`), que asume **horas decimales** → interpretaba `0.347` (las 08:20) como "0 h y 0.347×60 ≈ 20 min" = **"00:20"**. Fix: `hhmm` replica `fmtHora` (`Math.round(frac*24*60)` → HH:MM; un string "HH:MM" viejo se deja tal cual); se retira el import de `decimalAHHMM`. Los **tests** usaban horas decimales (`8.5`, `18`) — por eso el bug pasó inadvertido: se corrigieron a **fracción de día** (helper `hd()` = `horaToDecimal`) + 2 aserciones anti-regresión (Hernán 08:00–17:30; ningún mensaje con "⏰ 00:"). `test_itinerario_envio.mjs` **33/33**; `node --check` OK. **EN PRODUCCIÓN** (merge develop→main `86131a8`, orden de José, junto con la Rendición de Caja de Adrián — él ya terminó su prueba). **Incidente de reglas resuelto:** el deploy de `firestore.rules` de Adrián (PR #66) había **revertido** la regla `itinerario_envios` que yo había desplegado (quedó fuera del ruleset vivo); se **re-desplegó** el `firestore.rules` de develop —que ya tiene ambas— restaurándola sin tocar `comprobantes` (ruleset `d57c94e6`, verificado ambas vivas). ⚠️ **Adrián: hacé `git pull` antes de correr `firebase deploy --only firestore:rules` — desplegar reglas desde una copia vieja borra las reglas que no tenés.** |
| 2026-07-09 | **Envío de itinerario por técnico — Trazabilidad Etapa 1: VISTA PREVIA antes de enviar** (`api/enviar_itinerario.js` + `itinerario.html`; feature de trazabilidad pedida por José tras notar que "confirmar versión" es foto de todo, priorizando "ver qué se está mandando"). Al tocar **📲 Enviar a técnicos** ya no se manda de una: primero se abre un modal (`pv-overlay`) con el **mensaje exacto que recibirá cada técnico** (con marca ⚠️ para los que no tienen teléfono, contador de actividades, y aviso 🔄 actualización vs primer envío); el envío real/prueba recién se dispara desde ahí. **Backend:** modo `preview` en el endpoint que arma los mensajes con la **misma función y misma carga** (`mensajesPorTecnico`) que el envío real pero **sin enviar ni registrar** en `itinerario_envios`; devuelve por técnico `{nombre,texto,nTareas,sedes,tieneTelefono}` (nunca el número). Requiere versión confirmada + ADMIN, igual que enviar. **Garantía "lo que ves es lo que se manda" (idea de Codex):** el preview devuelve un `previewHash` (sha1 sobre `[tecnicoId,texto]` de todos los mensajes) que el envío real **reenvía y el backend exige** — 400 si falta (no se puede enviar sin preview), 409 si no coincide (los datos cambiaron entre ver y enviar). **Front:** `_pvSending`+botones deshabilitados evitan envíos duplicados por doble clic; `_pvCerrar(force)` no deja cerrar el modal a mitad de envío; el modo prueba sigue **solo SUPER_ADMIN**; cuerpo del mensaje pintado con `textContent` (anti-XSS). De paso se escapó un XSS preexistente: `b.detalle` crudo dentro del `<textarea>` de edición → ahora con `escI`. **Council (Codex) APROBADO** (3 rondas: Alto doble-envío + Medio hash + Bajo XSS, todos cerrados). `node --check` OK (backend + 2 scripts del HTML); `test_itinerario_envio.mjs` 33/33 sin cambios. **En develop.** ⚠️ **Adrián: `git pull` antes de tocar `itinerario.html`.** Próximo: Etapa 2 (historial de envíos con snapshot) y Etapa 3 (sellar versión + versionar cambios de actividades). |
| 2026-07-09 | **Envío de itinerario por técnico — Trazabilidad Etapa 2: HISTORIAL DE ENVÍOS** (`api/enviar_itinerario.js` + `itinerario.html` + `firestore.rules`). Botón nuevo **🕘 Historial** (solo ADMIN) en la cabecera del itinerario → modal (`he-overlay`) que lista **todos los envíos** de ese itinerario (todas las versiones) leyendo `itinerario_envios` (orden en cliente por `ts` desc, sin índice compuesto): por envío muestra versión, fecha/hora, `🧪 prueba`/real, `🔄 actualización`/primer envío, `✅ enviados/total · ⚠️ sin tel · ❌ error`, quién lo envió, y un `<details>` por técnico con el **snapshot del mensaje que recibió** (lo que realmente se mandó). **Backend:** cada registro de `itinerario_envios` ahora guarda `previewHash` y, en cada entrada de `detalle`, el `texto` del mensaje (snapshot **truncado a 4000 chars** para acotar el tamaño del doc); el cierre de auditoría, si falla el update con detalle, reintenta `completado_sin_detalle` con solo el resumen (no queda colgado en `en_progreso`). Render del historial 100% con DOM API + `textContent` (anti-XSS). **`firestore.rules`:** `itinerario_envios` `read` pasó de `authorized()` a **`isAdmin()`** — ahora el doc guarda snapshots + auditoría de quién envió qué, así que solo ADMIN puede leerlo (el gate de UI no bastaba). El historial distingue el **canal**: si un técnico recibió por **plantilla** (fuera de 24 h), avisa que el texto guardado es el *preparado*, no el literal recibido; y los `sin_teléfono`/`error` se marcan "⚠️ Mensaje preparado — NO recibido". **Council (Codex) APROBADO** (RECHAZADO→resuelto: Alta regla `authorized`→`isAdmin`; Media tamaño de doc→truncado+reintento; Baja "no recibido"; + hallazgo nuevo canal plantilla → aclarado). `node --check` OK. **En develop.** ✅ **Regla `itinerario_envios read:isAdmin` DESPLEGADA a prod** (2026-07-09, ruleset `cd4e5082`, OK explícito de José; endurecimiento verificado = único delta vs el ruleset vivo; `comprobantes` intacta; respaldo en `/tmp/firestore.rules.live-*`). ⚠️ **Adrián: `git pull` antes de tocar `itinerario.html`/`firestore.rules`.** |
| 2026-07-09 | **Envío de itinerario por técnico — Trazabilidad Etapa 3a: SELLAR la versión confirmada** (`itinerario.html`). Cierra el hueco que José detectó en vivo: **cambiar/quitar actividades de mtto sobre una versión ya confirmada NO la desconfirmaba** → lo "confirmado/enviado" podía dejar de coincidir con el itinerario real. Ahora una versión con **`confirmado:true` queda SELLADA**: no se asignan ni quitan actividades de mtto preventivo manualmente sobre ella; para cambiarlas hay que **✏️ Editar → versión nueva (v+1) en borrador**. **Implementación (solo cliente, NO toca `firestore.rules`; escribe solo en `mtto_plan`):** helper síncrono **`_versionSellada()`** (lee `currentIti.confirmado`) + guards `if(_versionSellada()){ _avisoSellada(); return; }` en **quitarPlan, abrirPanelActs, _finalizarMulti, asignarPlan, _finalizarAsignacion** + **`paDragOver`** (drag&drop); **UI:** `renderBloqueCard` muestra **🔒 confirmada** en vez de "🔧 Asignar" y `renderPlanChips` **oculta el ✕**. **Revalidación colaborativa:** helper async **`_sigueEditable()`** que, JUSTO ANTES de escribir (delete de `quitarPlan`, `batch.commit` de `_finalizarMulti`, `set` de `_finalizarAsignacion`), relee `bd_itinerarios/{id}_v{version}` de Firestore — cubre que **Adrián confirme la versión en otra pestaña** mientras acá seguía como borrador en memoria; si detecta el sello marca `currentIti.confirmado=true`, avisa y **re-renderiza** (aparece el 🔒 al instante). El **barrido de ejecución** (`loadPlanDia`/delete automático de lo ya ejecutado) **NO se sella a propósito** — lo ejecutado debe seguir saliendo del plan aunque la versión esté confirmada. **Council (Codex) RECHAZADO→APROBADO CON OBSERVACIONES** (2 rondas): [Alto] `_sigueEditable` fallaba **abierto** (el `catch` de red/permisos dejaba escribir) → ahora **fail-closed** (toast "No se pudo verificar…" + `return false`; la escritura que seguía habría fallado igual por lo mismo); [Bajo] no refrescaba la UI al detectar el sello remoto → ahora re-renderiza; observación Baja restante (`!s.exists`→editable) **documentada como decisión consciente** (fail-closed en not-found bloquearía itinerarios viejos con otro esquema de id; el peor daño del caso extremo sería un `mtto_plan` huérfano, no corrupción). **Hallazgo [Alto] server-side DIFERIDO a decisión de José:** el sellado es una regla de **flujo de trabajo**, no una frontera de seguridad (quien escribe `mtto_plan` es ADMIN/SUPER de confianza); una regla con `get()` cross-document en `firestore.rules` agrega costo/complejidad y su deploy a prod requiere su OK explícito. `node --check` OK. **En develop, sin merge a main.** Próximo: Etapa 3b (migración de actividades al versionar — decisión de UX pendiente). ⚠️ **Adrián: `git pull` antes de tocar `itinerario.html`.** |
| 2026-07-09 | **Envío de itinerario por técnico — Trazabilidad Etapa 3b: MIGRAR actividades al versionar (automática con confirmación)** (`itinerario.html`; decisión de UX de José tras consultarle 3 opciones). Complementa el sellado (3a): al **editar** un itinerario ya existente se crea una **versión nueva (v+1 en borrador)**; antes las actividades de mtto asignadas a técnicos (`mtto_plan`) quedaban en la versión vieja y había que re-seleccionarlas a mano. Ahora, al guardar una v≥2, se **pregunta (`confirm`) si mover esas actividades a la versión nueva**; si acepta, las mueve remapeando el **bloque por sede** (primer bloque de esa sede en la v nueva); las que no encajan (su sede ya no está en la v nueva) **quedan como pendientes re-seleccionables** (flujo previo) y se **avisan**. Si dice "No", el flujo manual anterior queda intacto. **Modelo:** el doc-id de `mtto_plan` es determinístico `EQ|PERIODO|AÑO|IDX` (SIN versión) → hay un solo doc por actividad; actualizar sus campos `itiVersion`/`nBloque` la **MUEVE, no la duplica**. Por eso la migración busca por `itiId` **todas** las versiones `< vNueva` (no solo `vNueva-1`) → arrastra también lo que quedó sin migrar en ciclos previos. **Implementación:** helper **`_resolverSede(tiendaCruda, sedesInv)`** extraído de `pendientesDeSede` (match exacto o sufijo más largo que sea sede real del inventario; refactor equivalente, sin duplicar la lógica) + **`_migrarPlanAVersionNueva(idIti, vNueva, bloques)`** (query `where('itiId','==',idIti)` sin índice compuesto; **excluye las YA EJECUTADAS** con el mismo criterio que el barrido de `cargarPlanDia` —lee `mantenimiento/{sede|periodo|anio}.tareaStatus`— para que una actividad hecha no reaparezca en la v nueva; `update` por batch troceado a ≤450; huérfanas avisadas). Enganche en `saveItinerario` tras `batch.commit()` (`if(version>=2)`), reordenado para que el toast de migración quede como el último visible; de paso se **eliminó el `if(d.ok){}else{}` muerto** (`d.ok` siempre true; el error real lo cubre el `catch`). **Decisiones de diseño:** varios bloques de la misma sede → va al **primero**; la **atribución de técnico se conserva** (no se re-pregunta); migrar respeta el sello 3a (crear v+1 es justo cómo se de-sella). **Council (Codex) APROBADO CON OBSERVACIONES→APROBADO** (2 rondas: [Medio] excluir ejecutadas antes de mover + [Bajo] normalizar la sede del plan en el lookup, ambos aplicados). `node --check` OK. **En develop, sin merge a main. Cierra la trazabilidad del envío por técnico (Etapas 1/2/3a/3b) — falta verificación en vivo por José.** ⚠️ **Adrián: `git pull` antes de tocar `itinerario.html`.** |
| 2026-07-09 | **Merge develop→main → Trazabilidad del envío por técnico (Etapas 1/2/3a/3b) EN PRODUCCIÓN** (`--no-ff`, orden explícita de José tras probar la 3b en vivo). Sube a producción las 4 etapas de trazabilidad del **📲 Enviar a técnicos**: **1) VISTA PREVIA** (modal con el mensaje exacto de cada técnico + `previewHash` que garantiza "lo que ves es lo que se manda"), **2) HISTORIAL de envíos** (botón 🕘, snapshot por técnico; regla `itinerario_envios read:isAdmin` ya desplegada), **3a) SELLAR versión confirmada** (`_versionSellada()` + guards + `_sigueEditable()` fail-closed; una versión confirmada no se edita sin crear v+1) y **3b) MIGRAR actividades al versionar** (al editar, ofrece mover las actividades de mtto a la versión nueva remapeando el bloque por sede, excluyendo ejecutadas, huérfanas avisadas). Todas con Council/Codex aprobado; la 3b verificada con harness sobre el código real (22/22) + en vivo en develop (funciones desplegadas + queries reales sin índice). **Enforcement del sello dejado client-side** (decisión de José: es regla de flujo de trabajo, no frontera de seguridad). develop verificado superset limpio de main (three-dot vacío). **El merge también sube el PR #68 de Adrián** (insumos: los botones del panel izquierdo actúan en lote con selección — `insumos.html`), por orden de José. ⚠️ **Adrián: `git pull` en main/develop.** |
| 2026-07-09 | **Bot WhatsApp: fix del bucle infinito al desambiguar equipos** (`api/_lib/equipos.js`, `matchEquipo`; reportado por Adrián en vivo: "realizado cortina de aire 04 de ripley plaza norte" → el bot preguntaba "¿Cuál equipo de PLAZA NORTE?" entre "Cortina de aire 04 — PISO 2" y "Cortina de aire 02 — Ingreso Piso 2" y repreguntaba sin importar la respuesta —incluso el nombre exacto o "Piso 2" copiando el formato que el propio bot sugería—; solo "cancelar" salía). **Causa (doble):** el "número canónico" del equipo (bonus fuerte +3) mezclaba los dígitos del **nombre** con los del **eq_id** (código interno de importación, ej. `MA-PLN-CAI-002` — numera por orden de carga del Excel, no por el número que ve el técnico), así que si "Cortina de aire 02" quedó con eq_id `…004` coincide por azar con "cortina de aire 04" y empata siempre; además un número junto a palabra de ubicación ("Piso 2") también competía como número DE EQUIPO y podía empatar con "02". **Fix:** el número fuerte (+3) sale SOLO de `e.nombre` (el código exacto ya se resuelve aparte, en el paso 1 de la función); los números pegados a PISO/NIVEL —en ambos órdenes: "piso 2", "nivel nro 3", "2do piso", "1er/1° nivel"— solo aportan al desempate débil por área (+1). **Autorevisión `/code-review` (2 hallazgos CONFIRMADOS aplicados):** (1) la 1ª versión trataba "NRO/NUMERO + número" a secas como ubicación, degradando el número DEL equipo → "cortina de aire numero 4" volvía a empatar (el mismo bucle, con otra fórmula); ahora NRO/NUMERO solo cuenta como ubicación pegado a PISO/NIVEL. (2) solo cubría el orden "palabra + número" → "extractor del 1er nivel" elegía "Extractor 01" (número de ubicación compitiendo como número de equipo); ahora también se depone el orden "número + palabra". Validado con suite standalone (sin Firestore) **12/12** (caso real Plaza Norte con eq_id cruzado —incl. la respuesta literal del chat "cortina de aire 04 - Piso 2"— + numero/nro + 1er/1° nivel + guardas de regresión "extractor 1"/"uno" + eq_id exacto); `node --check` OK. **NO validado contra el inventario real de Firestore** (esta máquina no tiene `serviceAccount.json`/`migrar_db/`). Rama `feature/bot-desambiguacion-equipos` → PR a develop. **Pendiente: prueba en vivo por WhatsApp (Plaza Norte, "cortina de aire 04" vs "02") en develop antes de mergear.** |
| 2026-07-09 | **Bot WhatsApp: "realizado \<equipo\>…" ahora entra al flujo de REGISTRO de mtto** (`api/_lib/mtto.js`, `esIntencionMtto`; mismo PR #69, pedido de Adrián: hay actividades que se llaman literalmente "Realizado" y él reporta "realizado cortina de aire 04 de ripley plaza norte" — ese mensaje caía a OBSERVACIONES, como se vio en las capturas del bug del bucle). **Por qué caía:** el detector exige la palabra mantenimiento/mtto/preventivo (`RE_MTTO`) antes de mirar los verbos; la lista de verbos tenía "realicé" pero no "realizado"; y `esActividadConocida` excluye a propósito nombres cortos como "Realizado" (≥2 palabras y ≥10 chars, decisión del Council). **Fix mínimo:** (1) un mensaje que EMPIEZA con "realizado/a(s)" cuenta como intención de registro — misma filosofía "empieza con la intención" de la regla existente; (2) "realizad" se suma a la lista de verbos para el caso con contexto ("el mantenimiento del chiller 1 quedó realizado"). **La guarda de incidentes del Council queda INTACTA:** "realizado el ajuste pero sigue la fuga" sigue siendo observación (a "realizad" NO se le dio rango de verbo explícito en `esRegistroActividad`). Validado con test standalone de ruteo **13/13** (caso real + guardas Council + regresiones); `node --check` OK. Las suites del bot en `migrar_db/` no se pudieron correr en esta máquina. **Nota (plan de Adrián):** renombrar actividades en la app NO requiere redeploy del bot — la lista efectiva se lee de Firestore (`tareas_config` + `mtto_actividades_equipo`) con caché de 5 min. |
| 2026-07-09 | **Bot WhatsApp: fix de un hallazgo Media del Council sobre el #69 (antes de subirlo a producción)** (`api/_lib/equipos.js`, `matchEquipo`; el Council/Codex —corrido por mí porque Adrián se quedó sin tokens y José pidió subir el #69— encontró que el fix del bucle **reintroducía el mismo empate en una variante de ubicación**). **Detalle:** el número "fuerte" del equipo se calcula quitando los números pegados a PISO/NIVEL. El 1er `replace` cubría "piso 2"/"piso nro 2" pero **no** "piso **n°** 2" ni "piso **no** 2" (tras `norm()`, "n°"→"N", "no"→"NO"); entonces en "cortina 04 piso n° 2" el "2" de piso NO se removía en el paso 1, y el 2º `replace` (`\d+…PISO`) se comía el "04" del equipo → quedaba el "2" como número fuerte → **volvía a empatar con la "Cortina 02"** (bucle). Como el bot muestra la ubicación en sus sugerencias, un técnico repitiéndola podía re-disparar el bug. **Fix:** el conector de ubicación pasa de `(NRO|NUMERO)` a **`(N|NO|NRO|NUMERO)`** → "piso n° 2"/"piso no 2" se remueven en el paso 1 y el número del equipo sobrevive. **Validado EN VIVO contra el Firestore REAL** (`scratchpad`, código real del #69 + inventario real de PLAZA NORTE, donde la trampa es literal: "Cortina de aire 04" = `MA-PLN-CAI-001` y "02" = `…004`): las cortinas 01-04 resuelven directo sin bucle, y "cortina 04 piso 2 / piso n° 2 / piso no 2 / nivel 1" **todas** resuelven a la 04 (17/17 incl. el ruteo "realizado"→mtto y las guardas de incidente→obs). Council re-APROBADO. `node --check` OK. ⚠️ **Adrián: toqué tu `equipos.js` (fix del Council) por orden de José mientras no tenías tokens — `git pull`; anotado en el Tablero.** |
| 2026-07-10 | **Operatividad de equipos — Frente A: el bot pregunta el % tras registrar una observación** (`api/_lib/operatividad.js` nuevo + `api/_lib/conversacion.js`; Etapa 1 de la nueva línea "Operatividad", repartida con Adrián — Frente B = panel `operatividad.html`; contrato en `docs/operatividad-spec.md`). Tras confirmar una observación, el motor conversacional entra en una **fase nueva `OPERATIVIDAD`** (máquina de estados sobre `wa_sesiones`) y pregunta *"¿En qué estado quedó el equipo?"* con **5 niveles: 100 🟢 / 75 🟡 / 50 🟠 / 25 🔴 / 0 ⚫**, opción **"omitir"**. Al responder, `operatividad.js` escribe **1 evento en `operatividad_eventos`** (historial, append-only) **+ estado vivo** (`operatividad`/`operatividadFecha`/`operatividadPor`/`operatividadObsId`/`operatividadOrigen`) en el doc del equipo de `inventario` — ambos en un **`db.batch()` atómico** (contrato "1 evento + estado vivo"). La **foto sigue 100% opcional**: una foto enviada durante `OPERATIVIDAD` se adjunta a la observación (`agregarFotoAObservacion`) y se sigue pidiendo el %; el recordatorio "¿olvidaste la foto?" se mantiene al cierre si no hubo. `parsearOperatividad` acepta `1..5`, `100/75/50/25/0` y `N%`, y **repregunta** (no redondea en silencio) ante un número fuera de escala. Escapes de la fase: `RE_CANCELA`/"omitir" → cierra sin registrar; `RE_NUEVA` → reinicia; entrada inválida repregunta 1 vez y luego omite (no traba al técnico). Robustez: la observación se guarda ANTES de abrir la fase, y si `guardarSesion` falla se **limpia la sesión** (no queda en `CONFIRMANDO` → sin doble guardado). El **router NO cambia** (una sesión de obs viva en `OPERATIVIDAD` rutea a observaciones; "1".."5"/"omitir" no disparan los escapes de saludo/mtto). **NO toca `firestore.rules`** (Admin SDK) — las reglas + backup de las 2 colecciones nuevas van en el Frente B. **Council (Codex) RECHAZADO→APROBADO CON OBSERVACIONES** (2 rondas): cerró [Alto] doble-guardado por sesión colgada, [Medio] evento+inventario no atómicos → batch, [Medio] `RE_NUEVA` atrapado, [Bajo] parseo con redondeo silencioso → estricto, [Bajo] `nivelDeOperatividad` sin `color` → alineado al spec. Validado con harness sobre el **código real** sin tocar producción: lógica pura 38/38 (niveles + parseo) + flujo conversacional 35/35 (Firestore fake en memoria: confirmar→operatividad→registrar/omitir/nueva/inválido/foto). `node --check` OK. Rama `feature/operatividad-bot` → **PR #70 mergeado a develop (`c54760e`)**. **Pendiente:** suite `migrar_db` contra Firestore real + prueba en vivo por WhatsApp en develop; merge a main. **Etapa 2 (no arrancada):** gestor de clientes como entidad. |
| 2026-07-10 | **Operatividad de equipos (Etapa 1) — Frente B: PANEL** (módulo nuevo `operatividad.html` + `index.html` + `firestore.rules` + `configuracion.html`; construido por Claude porque Adrián se quedó sin tokens y no llegó a pushear nada, orden de José "construí el panel vos"). Complementa el **Frente A (bot, PR #70)**: donde el técnico reporta por WhatsApp el % de operatividad tras una observación, este panel lo **visualiza y edita**. **Contrato:** `docs/operatividad-spec.md`. **`operatividad.html`** (reusa el scaffold auth/roles/toast de `observaciones.html`): **① vista Por Sede** — tarjetas por sede/cliente con **% ponderado por criticidad** (semáforo de 5 niveles 100🟢/75🟡/50🟠/25🔴/0⚫; bandas del agregado continuo ≥90/75/50/25/<25), stat tiles (global, equipos, inoperativos, a re-verificar), peores sedes primero, drill-down a equipos; **② vista Por Equipo** — selector Cliente→Sede→Tipo→Equipo + detalle con **historial** (`operatividad_eventos`, query por `eqId` sin índice compuesto). **Sin reporte = 100% operativo** con **badge ⚠️ de frescura** (nunca reportado o >60 días). **Edición manual** (gate `MA.isAdmin()` — el update de `inventario` exige `canWrite('inventario')`): fija el % con `db.batch()` atómico (evento en `operatividad_eventos` **origen APP** + estado vivo en campos `operatividad*` de `inventario`), **mismo shape que escribe el bot**. **Maestro de criticidad** (peso 1-5 por tipo, gate `MA.isSuperAdmin()`) en modal → `maestros_criticidad`. **`firestore.rules`:** 2 colecciones nuevas antes del catch-all fail-closed — `maestros_criticidad` (read authorized / write isSuper) y `operatividad_eventos` (read authorized / **create isAdmin** / update-delete isSuper, log inmutable estilo `mtto_log`). **`index.html`:** tile 🚦 Operatividad (`data-app="operatividad"`, gate por defecto `canEnter`), antes de Configuración. **`configuracion.html`:** backup/import de ambas colecciones (patrón JSON-doc) + los campos `operatividad*` en `inventario.csv` (round-trip del estado vivo, merge-safe con backups viejos). **Council (Codex) RECHAZADO→APROBADO** (4 rondas: XSS `onclick` con datos de Firestore → data-attrs+listeners; el backup no preservaba el estado vivo; `createdAt` unificado a ISO string; permiso `create`=`isAdmin`; `_hasOp` desde header crudo). Sintaxis JS inline OK (`node --check`). **Rama `feature/operatividad-panel` → PR #71; NO toca `api/`** (disjunto del Frente A). **Pendiente: prueba en vivo end-to-end junto al bot en develop → merge a main. ⚠️ Antes de `firebase deploy --only firestore:rules`: `git pull` (el deploy REEMPLAZA todo el ruleset).** |
| 2026-07-10 | **EN PRODUCCIÓN 🚀: Operatividad de equipos (Etapa 1) — Frentes A+B** (merge develop→main `dcacb89`, `--no-ff`, orden explícita de José "pasalo todo a main"). Sube a producción la nueva línea **Operatividad**: **Frente A (bot)** — tras confirmar una observación por WhatsApp, el bot entra en la fase `OPERATIVIDAD` y pregunta el **% del equipo** (5 niveles 100🟢/75🟡/50🟠/25🔴/0⚫ + omitir) → escribe evento en `operatividad_eventos` + estado vivo en `inventario` (`db.batch()` atómico); foto opcional; router intacto. **Frente B (panel)** — `operatividad.html` (vista **Por Sede** con % ponderado por criticidad + semáforo + drill-down; vista **Por Equipo** con historial; **edición manual** `isAdmin`; **maestro de criticidad** super-only) + tile 🚦 en `index.html` + backup/import de las 2 colecciones y de los campos `operatividad*` de `inventario` en `configuracion.html`. **`firestore.rules`:** `operatividad_eventos` (read authorized / create isAdmin / update-delete isSuper) + `maestros_criticidad` (read authorized / write isSuper); catch-all fail-closed intacto; **ya desplegadas al Firestore vivo `multiaire-fee43`** (`firebase deploy --only firestore:rules`, cuenta `marchenaangulojoseluis@gmail.com`). Ambos frentes con **Council/Codex APROBADO** (bot 2 rondas, panel 4 rondas); develop verificado **superset limpio de main** (sin revertir nada, merge sin conflicto salvo el changelog). El bot de producción (`multiaire-peru-app.vercel.app`, webhook ya apuntando ahí) queda con la fase `OPERATIVIDAD` **activa** para los técnicos reales. **Pendiente:** verificación en vivo end-to-end (José/Adrián) + (bot) suite `migrar_db`. ⚠️ **Adrián: `git pull` en main/develop; las reglas de Firestore YA están vivas — un `firebase deploy --only firestore:rules` SIN `git pull` previo las BORRARÍA (reemplaza todo el ruleset).** |
| 2026-07-10 | **Clientes como entidad (Etapa 2) — arranque COMPLETO** (módulo nuevo `clientes.html` + `index.html` + `firestore.rules` + `configuracion.html` + `docs/clientes-spec.md`; rama `feature/clientes-fase2a`). Convierte el "cliente" —hoy un **string denormalizado** (`'RIPLEY'`) en `maestros_tiendas.cliente`/`inventario.cliente`— en una **ficha real** `maestros_clientes` (doc-id = ese mismo string = **llave de join**, sin refactor a IDs → no rompe el bot productivo). **`clientes.html`** (reusa el scaffold auth/roles/toast de `operatividad.html`): lista de clientes (tarjetas con nº de sedes/equipos) + ficha con 4 secciones — **Identidad** (RUC/razón social/contacto, gate `MA.can('editar','clientes')`), **Contrato/licitación** (número/vigencia), **Sedes** (leídas de `maestros_tiendas` con conteo de equipos de `inventario`; crear/editar sede = super, escribe `maestros_tiendas`), **Actividades** por tipo (`tareas_config/{cliente|tipo}` con **fallback** a la global `{tipo}`; badge propia/heredada; editar/heredar = super). **Backfill "Generar desde datos actuales"** (super, idempotente, batch troceado): crea las fichas de los `cliente` distintos detectados en tiendas+inventario y **copia las plantillas globales de actividades a `{cliente|tipo}`** sin pisar overrides → RIPLEY arranca con sus actividades ya establecidas. **`firestore.rules`:** `maestros_clientes` (read authorized / create-update isAdmin / delete isSuper); `tareas_config` **sin cambio de regla** (el `match` cubre doc-id `tipo` o `cliente|tipo`); catch-all fail-closed intacto. **`index.html`:** tile 🏢 Clientes (`data-app="clientes"`), antes de Operatividad/Configuración. **`configuracion.html`:** backup/import de `maestros_clientes` (JSON-doc) + **rework de `tareas_config`** (columna `CLIENTE` + agrupación por (cliente,tipo) en import, que de paso **corrige un bug previo** que colapsaba cada tipo a su última tarea). **Decisión de alcance (José):** completo de una (identidad+sedes+contrato+actividades) con RIPLEY poblado; **el CONSUMO por cliente (que el bot/mtto/itinerario LEAN `cliente|tipo`) queda como interruptor FUTURO** — con un solo cliente cuya plantilla == la global, encenderlo no cambia nada y solo mete riesgo en producción; se activará con el 2.º cliente (helper `getPlantilla(cliente,tipo)` + Council + prueba en vivo). Contrato en `docs/clientes-spec.md`. **Council (Codex) RECHAZADO→APROBADO** (2 rondas): [CRÍTICO] introducir docs `cliente|tipo` en `tareas_config` **rompía a los lectores globales existentes** (agrupan por el CAMPO `tipo` → un override de cliente podía sobrescribir la plantilla global en memoria). **Fix (blindaje retrocompat):** todo lector global de `tareas_config` ahora **ignora los doc-id con `|`** — `api/_lib/mtto.js` (`esActividadConocida`), `itinerario.html`, `mantenimiento_multiaire.html`, `inventario_multiaire.html` y el editor global de `configuracion.html`; el editor global al guardar **preserva** los overrides (`if(id.includes('|'))return` en el delete-all) y el import de `tareas_config` solo hace delete-all en modo `replace` (en `merge` upsert). `_resolverActs` del bot lee `tareas_config/{tipo}` por doc-id directo → no colisiona. **El cambio NO altera el comportamiento actual** (hoy no existen docs `cliente|tipo`), solo blinda ante el backfill. `node --check` OK (bot + 6 HTML). **Pendiente: PR a develop (sin merge a main); prueba en vivo del backfill + gestor por José.** ⚠️ **Adrián: toqué `api/_lib/mtto.js`, `itinerario.html`, `mantenimiento_multiaire.html`, `inventario_multiaire.html` (blindaje del Council, cambio mínimo/defensivo) — `git pull` antes de tocarlos.** |
| 2026-07-11 | **Clientes (Etapa 2) — ENCENDIDO el consumo por cliente** (`api/_lib/mtto.js` + `mantenimiento_multiaire.html` + `itinerario.html`; rama `feature/consumo-por-cliente`; sigue a la Fase 2a #72). El bot, Mantenimiento e Itinerario dejan de leer SIEMPRE la plantilla global y ahora **resuelven la plantilla del cliente del equipo** (`tareas_config/{cliente|tipo}`) **con FALLBACK a la global** (`tareas_config/{tipo}`). **Bot:** `_resolverActs(eqId, tipo, cliente)` lee `{cliente|tipo}` ?? `{tipo}`; el caller pasa `r.equipo.cliente` (de `matchEquipo`, que ya filtra por cliente) y la sesión guarda `ses.cliente` (para los minutos al guardar); `esActividadConocida` (detección de intención) vuelve a incluir los nombres de actividades por cliente (Set de nombres → sin colisión). **Mantenimiento:** `actividadesDe(eq)` usa `TAREAS_CLI['{cliente|tipo}']` ?? `TAREAS[tipo]` (carga y cachea ambas). **Itinerario:** el armado del plan usa `plantillasCli['{cliente|tipo}']` ?? `plantillas[tipo]` por equipo. El override **por equipo** (`mtto_actividades_equipo`) se aplica igual **encima** de la plantilla resuelta. **Retrocompat total:** como RIPLEY (backfill) tiene `RIPLEY|tipo == global`, **no cambia nada observable hoy**; el efecto aparece cuando un cliente edita su plantilla o entra un 2.º cliente. Los lectores que solo listan tipos (`inventario_multiaire.html`) o editan la plantilla global (`configuracion.html`) siguen en globales. Validado con **harness sobre el código REAL del bot** (Firestore fake, `actividadesDeEquipo`/`minutosDeEquipo`): **8/8** (override cliente / fallback global / sin cliente / override por equipo encima). `node --check` OK (bot + 2 HTML). **Pendiente: Council + PR a develop; prueba en vivo (crear un 2.º cliente o editar la plantilla de RIPLEY y verificar bot+app). NO toca `firestore.rules`.** ⚠️ **Adrián: toqué `api/_lib/mtto.js`, `mantenimiento_multiaire.html`, `itinerario.html` — `git pull`.** |
| 2026-07-11 | **Ajustes post-prueba (Etapa 2): orden de Observaciones + filtros de Operatividad** (`observaciones.html` + `operatividad.html`; feedback de José tras la prueba en vivo del gestor de clientes). (1) **Observaciones** — el orden PREDEFINIDO de la tabla pasa a **más recientes primero**: `obsSort` ordena por `fecha` desc y, dentro del mismo día, por `createdAt` desc (hora real de registro), con desempate estable por tienda/equipo; antes ordenaba por tienda→equipo→fecha (parecía "por nombre"). (2) **Operatividad** (vista Por sede) — nuevo **selector de orden** `f-orden` (🔴 peor estado primero [**predefinido**] · 🟢 mejor primero · 🔤 sede A–Z · 📊 más equipos primero); `renderSedes` aplica el orden elegido (el default replica el "peores primero" que ya existía). El **filtro de cliente** (`f-cliente`) pasa a estar **siempre visible** (antes solo con >1 cliente) para poder filtrar por cliente al crecer la plataforma; ambos controles se ocultan en la vista Por equipo (`switchTab`). Sintaxis JS inline validada. **En develop, sin merge a main.** *(Relacionado, pendiente de decisión de José: la "oficina" —sede `TIE013`, `cliente="-"`— se coló como ficha fantasma `maestros_clientes/-` en el backfill del gestor; hay que excluirla como cliente sin romper el geofencing de asistencia, y decidir dónde se gestionan las sedes internas antes de quitar "Tiendas" de Maestros del Sistema en configuración.)* |
| 2026-07-11 | **Oficina NO es cliente: sedes internas en el gestor + fuera "Tiendas" de configuración** (`clientes.html` + `configuracion.html` + borrado de dato en Firestore; feedback de José tras la prueba en vivo del gestor de clientes). La "oficina" (sede `TIE013`, `cliente="-"`, coords para el geofencing de asistencia, radio 500, **0 equipos**) se había colado como **ficha fantasma** `maestros_clientes/-` en el backfill del gestor. Fix en 3 frentes: **(1) `clientes.html` — blindaje + sedes internas:** helper `esClienteValido(c)` (vacío o `"-"` = NO es cliente) → el **backfill** (`detectados`/`todos`) y la **lista** ignoran los no-clientes (no recrean el fantasma); nueva sección **"🏢 Sedes internas (sin cliente)"** en la vista lista (`renderInternas`/`openSedeInterna`) que lista las sedes internas (la oficina) y permite editar sus coords/radio con el **mismo editor de sedes** (super admin), preservando su `cliente` (`_sedeCliente` → no la convierte en cliente; `saveSede` re-render seguro con/sin ficha abierta). **(2) `configuracion.html` — se retira el editor de "Tiendas"** de "Maestros del sistema" (ahora redundante con Clientes): fuera el tab + panel + `renderTiendas`/`updateTienda`/`updateTiendaGeo`/`toggleTienda`/`deleteTienda`/`addTienda`, la carga de `maestros_tiendas` en `loadMaestros` y el bloque `b2` (delete-all+recrear) de `saveMaestros` — **este último era clave**: dejarlo sin la carga habría **borrado todas las tiendas** al guardar grupos/feriados. Grupos pasa a ser el tab activo; nota que apunta a Clientes. **El backup/restore de `maestros_tiendas` se conserva** intacto (sección Respaldo). El gestor de clientes escribe sedes con `{merge:true}` (sin delete-all) → editor estrictamente más seguro que el viejo. **(3) Dato:** borrada `maestros_clientes/-` de producción vía Admin SDK (`migrar_db`, con respaldo en `_backup_cliente_fantasma.json` + guarda: 0 equipos con `cliente="-"`); **verificado que la sede TIE013 y sus coordenadas quedan intactas** (lat/lon/radio 500 sin cambios) → **el geofencing de asistencia NO se toca**. Sintaxis JS inline validada (ambos HTML). Rama `feature/oficina-no-cliente`. **En develop, sin merge a main.** ⚠️ **Adrián: toqué `configuracion.html` — `git pull`. Las sedes/tiendas ahora se editan en `clientes.html`, no en configuración.** |
| 2026-07-11 | **Council (Codex) sobre lo pendiente de Etapa 2 → APROBADO CON OBSERVACIONES + 3 fixes** (`clientes.html` + `configuracion.html`). Auditoría independiente de Codex sobre TODO lo pendiente para producción (develop con #72+#73 + PRs #74+#75, vs main). Verificó OK: retrocompat del consumo por cliente (bot/mtto/itinerario: `cliente|tipo`→global; sin cliente→global; override por equipo encima), backup/restore de `maestros_tiendas` intacto y sin referencias colgantes tras retirar el editor de Tiendas, `saveMaestros` ya no borra tiendas, `esClienteValido`/sedes internas correctas, orden de observaciones/operatividad sin regresión. **3 hallazgos, todos aplicados:** (Medio) **`canEditCli()` alineado a las reglas** — la UI dejaba editar identidad/contrato a SUPERVISOR con app `clientes`, pero `firestore.rules` (`maestros_clientes` create/update `isAdmin`) lo rechazaba → botón visible + write denegado; pasa a `MA.isAdmin()` (ADMIN/SUPER, mismo criterio que `personal.html`). (Medio) **guard de `|` en el nombre de cliente** (`crearCliente`) — el `|` es el separador de `tareas_config/{cliente|tipo}`; ahora se prohíbe `|`, `/` y el centinela `-` al crear (evita colisión de doc-ids). (Bajo) **XSS en `renderGrupos`/`renderFeriados`** de configuración — `g.grupo`/`f.nombre`/`f.fecha` iban sin escape al `innerHTML` (value + badge); ahora con `escAttr` (ya existente). Nota (aceptada, bajo riesgo): el import de `tareas_config` sigue parseando por `split('|')`; como ningún cliente/tipo real tiene `|` y ahora la creación lo bloquea, el defensivo principal está en el origen. Sintaxis JS inline validada. Council re-corrido tras corregir un merge propio de la rama de auditoría (el 1er intento auditó sin #75). **En develop, sin merge a main.** | **PR #73 (consumo por cliente) MERGEADO A DEVELOP** (`gh pr merge 73 --merge`; CLEAN) — el consumo por cliente ya está integrado en develop para la prueba en vivo. |
| 2026-07-11 | **Mantenimiento: Checklist imprimible en blanco (para capacitación + campo)** (`mantenimiento_multiaire.html`; pedido de José para la capacitación de técnicos). Nuevo botón **🖨️ Checklist** en el header (junto a 📤 WhatsApp / 📄 PDF; misma visibilidad: aparece con equipos seleccionados + permiso `exportar`). `exportChecklist()` genera —con el mismo patrón ventana+`window.print()` del export PDF— una hoja imprimible/PDF de los **equipos seleccionados de la sede+período**, agrupados por tipo→equipo, con **casillas vacías ☐** por cada actividad (de `actividadesDe(eq)`, respeta el consumo por cliente + overrides por equipo), columna **min** y columna **Observación** en blanco para tildar/anotar a mano en campo. Encabezado con sede/cliente/período + fila para **Técnico(s)/Fecha ejecución/Firma** + conteo de equipos y actividades. A diferencia del PDF de reporte (que muestra ✓/✗ del avance real), este es una **plantilla en blanco**. Muestra real verificada (ATOCONGO, 8 equipos, 22 actividades). Sintaxis JS inline validada. Rama `feature/checklist-imprimible`. Council (Codex) sobre el diff: escape `_actEsc` en todos los datos de Firestore (sin XSS), gate `MA.can('exportar','mantenimiento')`, solo-lectura. **EN PRODUCCIÓN** (merge develop→main `ad9c027`, `--no-ff`, autorización explícita de José para la capacitación) — junto con los ajustes de Etapa 2 ya auditados (#73 consumo por cliente, #74 orden observaciones/filtros operatividad, #75 oficina no cliente/fuera Tiendas de config). ⚠️ **Adrián: `git pull` en main/develop — cambió `mantenimiento_multiaire.html`, `itinerario.html`, `observaciones.html`, `operatividad.html`, `inventario_multiaire.html`, `configuracion.html` + nuevo `clientes.html`.** |
| 2026-07-11 | **Bot: verificación pre-capacitación — observaciones robustas vs registro de mtto sensible a typos** (sin cambios de código; `migrar_db/test_mtto_resolucion.mjs` + `test_mtto_codigo.mjs` nuevos). Confirmado contra el inventario real: **Observaciones** toleran typos fuertes (el flujo pasa por Gemini que corrige antes de resolver) — E2E 14/14. **Registro de mantenimiento** (`mtto.js`, `manejarMtto`→`resolverEquipo` **sin Gemini**) NO tolera typos de sede: `chiller 1 de atocongo`/`cortina de aire 2 de atocongo`/`rooftop 3 de plaza norte` resuelven directo ✅, pero `atokongo`/`plasa norte`/`megaplasa`/`sjl`/`rutop` → el bot **repregunta la sede** (no se cuelga). Además, en registro **el código MA-… solo NO basta** (`resolverEquipo` intenta la sede primero y falla): `MA-ATO-CHI-001` → pide sede; `MA-ATO-CHI-001 de atocongo` ✅. Corregido en la hoja de capacitación (regla: sede bien escrita; ante duda, **código + sede**). Mejora futura opcional: endurecer `equipos.js` para tolerar typos fonéticos de sede + alias `rutop` sin Gemini. |
| 2026-07-13 | **Bot: el resolver de sede/equipo aguanta typos SIN Gemini** (`api/_lib/equipos.js`; cierra la "mejora futura opcional" que dejó anotada la verificación pre-capacitación del 2026-07-11). El PR #77 puso a **Gemini a corregir el texto ANTES** del matcher; esto endurece **el matcher en sí**, que es lo único que queda de pie si Gemini se cae, se queda sin cuota o su key rota (justo lo que pasa al rotar la `GEMINI_API_KEY`). **Orden intacto: Gemini sigue corriendo primero** (decisión de José); este matcher es la red de abajo. **`matchSede` queda con 5 capas**, de más a menos precisa: ① exacta · ② **parcial** (la SEDE contiene lo dicho: "san juan" → SAN JUAN DE LURIGANCHO) · ③ tokens sueltos · ④ **sigla** ("sjl" → SAN JUAN DE LURIGANCHO; mínimo 3 letras) · ⑤ **scoring por ventanas** — busca la sede DENTRO de la frase ("chiller 1 de atokongo") con **clave fonética española** (`fonetica()`: atokongo=atocongo, plasa=plaza, jokey=jockey; dígrafos CH/LL, ce/ci→S, Z→S, V→B, H muda, dobles; **las vocales NO se tocan** — son las que separan sedes parecidas) + **Levenshtein acotado** (`distancia(a,b,max)` con early-exit por diferencia de largos y por fila mínima). **Ninguna capa adivina:** ante empate devuelve candidatos y el bot repregunta, igual que hoy. **`tipoMencionado`** gana los alias **`rutop`/`ruftop`/`ruf top`** → PAQUETE (roof top como se dice en obra) + la misma tolerancia fonética en las keywords del tipo ("chiler"→CHILLER, "extraktor"→EXTRACTOR). **Council (Codex) RECHAZADO→RECHAZADO→APROBADO CON OBSERVACIONES (3 rondas), todos los hallazgos cerrados:** [Alto] **falso positivo `m plaza norte`** → resolvía PLAZA NORTE en silencio pudiendo ser MAC PLAZA NORTE (escribir el registro contra la sede equivocada es el peor daño del dominio) → **regla de CONTENCIÓN**: si la sede ganadora es subfrase fonética de otra sede real que también calza, NO se resuelve → candidatos; [Alto] `chiller 1 de mac plasa norte` no resolvía (la ventana `MAK PLASA NORTE` y la subventana `PLASA NORTE` empataban en distancia 0) → **a igual distancia gana la sede MÁS LARGA** (especificidad); [Alto, 2ª ronda] el blindaje solo cubría el camino fuzzy: **la rama literal `q.includes(n)` resolvía antes y sin blindaje** → por eso `m plasa norte` (typo) preguntaba pero `m plaza norte` (bien escrito) resolvía solo → **se eliminó `q.includes(n)`** y ahora hay **UN SOLO camino** para "la sede va dentro de la frase" (el scoring, el único que sabe de especificidad y contención); se eliminó también la capa de fonética-de-frase-completa por ser un 2.º camino sin blindaje; [Medio] Levenshtein sin cutoff sobre input no confiable de WhatsApp → acotado (medido: mensaje de **4096 chars = 3 ms**, 50 seguidos = 42 ms); [Medio] `MAX_TEXTO` prometido en un comentario y no aplicado → constante muerta eliminada. **Corrige de paso un bug PREEXISTENTE de producción:** `chiller 1 de mac plaza norte` **bien escrito** hoy REPREGUNTA (empata `MAC PLAZA NORTE` con `PLAZA NORTE` en la capa `contiene`) — ahora resuelve MAC PLAZA NORTE. **Validado contra el inventario REAL** (12 sedes, `migrar_db/test_resolver_typos.mjs` nuevo — inyecta un `corregir` no-op para **simular Gemini caído** y medir solo el matcher): **42/42** — resuelven `atokongo`/`plasa norte`/`megaplasa`/`chorrilos`/`jokey plasa`/`sjl`/`rutop`/`chiler`; **piden sede** (no adivinan) `m plaza norte`/`ma plaza norte`/`m plasa norte`/`chiller 1 de m plaza norte`/`callao`/`xyz`. Regresión verde: `test_mtto` 77/77 · `test_obs_v2` 15/15 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8. `node --check` OK. Rama `feature/resolver-typos-sede`. **En develop, sin merge a main.** ⚠️ **Pendiente aparte (NO es de este cambio): `test_fase4`/`test_fase5` tienen 1 fallo cada una — son tests DESACTUALIZADOS por la Etapa 1 de Operatividad (ya en producción): esperan que la sesión se borre tras guardar la observación, pero ahora sigue viva en la fase `OPERATIVIDAD` para preguntar el %. Verificado que fallan idéntico en develop sin este cambio.** |
| 2026-07-13 | **Bot: EL MATCHER PRIMERO, GEMINI ES EL RESCATE — cierre de la auditoría de INTEGRACIÓN (#77 + #81)** (`api/_lib/mtto.js` + `api/_lib/equipos.js`; **Council de integración, 5 rondas: RECHAZADO ×3 → APROBADO CON OBSERVACIONES → APROBADO**). Al auditar juntos por primera vez el **#77 de Adrián** (Gemini corrige el texto ANTES del matcher) y el **#81** (matcher endurecido), Codex encontró una familia de fallos que **ninguna auditoría por separado podía ver** — todos con el mismo desenlace: **escribir el registro de mantenimiento contra la sede EQUIVOCADA**. (1) **[Alto] Gemini DESEMPATABA:** ante un texto genuinamente ambiguo entre dos sedes reales (`m plaza norte` = ¿PLAZA NORTE o MAC PLAZA NORTE?), Gemini contesta `"Plaza Norte"` con total aplomo; esa **corazonada** entraba al matcher como **sede canónica**, ganaba por coincidencia exacta y **salteaba la regla de contención** del #81. (2) **[Alto] Gemini PISABA un match correcto:** aunque el matcher resolviera bien (`chiller 1 de mac plaza norte` → MAC PLAZA NORTE), Gemini corría igual después y su `g.sede` sobrescribía la sede. (3) **[Alto] Gemini MUDABA la sede** cuando el matcher había resuelto la sede pero fallaba el equipo (`cortina 99 de mac plaza norte`): el rescate podía terminar registrando en PLAZA NORTE. (4) **[Medio] BUCLE en la repregunta:** la respuesta del técnico se concatenaba al texto original, que seguía trayendo la sede ambigua → el bot volvía a preguntar lo mismo, para siempre (solo se salía con "cancelar"). (5) **[Medio]** `pidiendoSede` no se limpiaba → el nombre del equipo se leía como sede. (6) **[Medio]** la sede fijada se imponía aunque el técnico **cambiara de idea** y nombrara otra sede. **DECISIÓN DE ARQUITECTURA (José, tras ver la evidencia — invierte el orden que él mismo había pedido):** el **matcher determinístico resuelve PRIMERO** y **Gemini solo entra si el matcher NO pudo** (rescate). Una corrección probabilística **no puede tener autoridad sobre un dato ya resuelto con certeza**. Efectos secundarios buenos: la mayoría de los mensajes ya **no llaman a Gemini** (menos latencia y costo por registro) y el bot **sigue resolviendo typos con la key rota**. **Implementación:** `equipos.js` distingue **ambigüedad REAL entre sedes concretas** (contención/empate/parcial/sigla múltiple → `ambiguo:true`) de "no reconocí ninguna sede", y lo expone como **`sedeAmbigua`**; `mtto.js` (`intentarResolver`) → ① matcher con el texto crudo; ② si `sedeAmbigua` → **repregunta** (Gemini NO desempata); ③ si no resolvió → **rescate de Gemini**, con la **sede CONGELADA** si el matcher ya la tenía (`mudoDeSede` rechaza cualquier resultado que cambie de sede) y aceptando la corrección solo si **avanza**; ④ estados nuevos **`ses.pidiendoSede`** (el próximo mensaje ES la sede, no se acumula → sin bucle) y **`ses.sedeFijada`** (manda sobre el texto acumulado —que arrastra la frase ambigua— pero **no sobre el último mensaje**: si el técnico nombra otra sede sin ambigüedad, gana la nueva). **Validado contra el inventario REAL** simulando a Gemini devolviendo las correcciones peligrosas (`migrar_db/test_gemini_no_desempata.mjs` nuevo): **11/11** — Gemini no desempata / no pisa / no muda la sede congelada; sin bucle; no queda trabado; el rescate de Gemini sigue sirviendo (`atokongo`, `rutop 3 de plasa norte`); el técnico puede cambiar de sede a mitad de camino. Regresión: `test_resolver_typos` 42/42 · `test_mtto` 77/77 · `test_obs_v2` 15/15 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8. `node --check` OK. Rama `fix/gemini-no-desempata`. **Observaciones** (que comparte `equipos.js`/`gemini.js`) sin regresión — su flujo sigue confiando en `g.sede` como siempre (preexistente, fuera del alcance de este fix). |
| 2026-07-13 | **Actividad "Realizado" → "Mantenimiento integral"** (renombre de DATOS en Firestore; pedido de José: *"las actividades que están como 'realizado' al ingresarlo en el bot es confuso"*). `"Realizado"` era la actividad principal de **8 de los 14 tipos** (CORTINA DE AIRE, ESCALERA, EXTRACTOR, INYECTOR, SPLIT DECORATIVO, TVR MINI PLUS, UNIDAD CONDENSADOR, UNIDAD EVAPORADORA) → **348 de los 557 equipos**. Un nombre tan genérico confunde al técnico al dictarlo por WhatsApp **y al bot**: el detector `esActividadConocida` (`api/_lib/mtto.js`) excluye a propósito los nombres de <2 palabras o <10 chars —para no dar falsos positivos—, y "Realizado" (1 palabra, 9 chars) caía justo ahí; por eso hubo que meterle una regla especial de ruteo (`^realizad[oa]s?` → registro, changelog 2026-07-09). **"Mantenimiento integral"** (2 palabras, 22 chars) **sí entra** en el detector → el bot ahora **PRE-MARCA la actividad sola** y salta directo a confirmar. **Alcance del renombre (39 docs, script `migrar_db/renombrar_realizado.js`, idempotente, con simulacro por defecto y respaldo JSON automático):** las **24 plantillas** de `tareas_config` (las 8 `RIPLEY|{tipo}` + las 8 globales + las 8 del cliente fantasma `-|{tipo}`), los **2 docs vivos** de `mtto_plan` y los **13 del histórico** `mtto_ejecuciones` (campo `tarea`, denormalizado). **NO se toca `mantenimiento.tareaStatus`**: el estado del checklist se guarda **por ÍNDICE**, no por nombre → las marcas ya hechas quedan intactas (igual que los doc-id `EQ\|PERIODO\|AÑO\|IDX`). `mtto_actividades_equipo.quitadas` guarda nombres, pero hay **0 docs**. Verificado end-to-end contra el Firestore REAL (`scratchpad/test_mantto_integral.mjs`, 5/5, sin escribir en `mantenimiento`): "mantenimiento integral de la cortina de aire 4 de atocongo" → *"🔎 Detecté 1 actividad(es)"*; "realizado cortina de aire 4 de atocongo" sigue ruteando a registro (la regla especial se mantiene por si el técnico habla así) y resuelve el equipo. Respaldo previo en `migrar_db/_backup_realizado_*.json`. |
| 2026-07-13 | **La plantilla de actividades vive SOLO en el CLIENTE — se retira la plantilla global** (`api/_lib/mtto.js` + `mantenimiento_multiaire.html` + `itinerario.html` + `inventario_multiaire.html` + `configuracion.html` + `clientes.html` + `docs/clientes-spec.md`; decisión de José: *"la configuración general de actividades ya no va: la plantilla viene por cliente"*). Las actividades **salen de la licitación de cada cliente**, así que una plantilla "global para todos" no significaba nada — y encima **casi ningún equipo la usaba** (los lectores ya preferían `{cliente|tipo}` desde el consumo por cliente del 2026-07-11), mientras la UI seguía prometiendo que "aplica a todos sus equipos". **Modelo definitivo:** única fuente `tareas_config/{cliente|tipo}`, **SIN fallback**; el ajuste por equipo (`mtto_actividades_equipo`) se mantiene y se aplica **encima**. Un (cliente, tipo) sin plantilla **no tiene actividades** y se dice explícito (el bot: *"no tiene actividades configuradas — configúralas en la app de Clientes"*; la ficha del cliente: badge **⚠️ sin configurar**) — registrar contra una lista que el cliente no contrató es peor que no registrar. **Etapa 0 (datos, ejecutada — `migrar_db/etapa0_plantillas.js`, simulacro por defecto + respaldo):** se creó **`RIPLEY|CHILLER`** (el único de los 14 tipos sin plantilla propia: sus **8 chillers** sobrevivían solo por el fallback → sin esto quedaban sin actividades) y se borraron los **14 docs `-|{tipo}`** del cliente fantasma `-` (la oficina, **0 equipos**; su ficha ya se había borrado el 2026-07-11). Verificado: los **557 equipos** tienen la plantilla de su cliente. **Etapa 1 (código):** el **editor de plantillas globales de `configuracion.html` se retira entero** (~218 líneas de JS + la sección HTML + su CSS; el **backup/restore de `tareas_config` queda intacto**); el editor de Mantenimiento pierde el modo PLANTILLA y queda como **⚙️ Ajustar actividades por equipo** (quitarle una heredada / agregarle una propia); se quita el fallback a global en los 3 lectores (`_resolverActs` del bot, `actividadesDe`→`plantillaDeEq` de mantenimiento, `pendientesDeSede` del itinerario) **y el fallback hardcodeado** de `loadTareas` (que se desactualizaba en silencio — todavía decía "Realizado"); el **selector de tipos de `inventario_multiaire.html` deja de leer `tareas_config`** y se alimenta del **inventario** (la fuente de verdad de qué tipos hay) + opción **"✏️ Otro tipo (escribir)…"** para dar de alta el primer equipo de un tipo nuevo; en `clientes.html` el botón "↩︎ Usar plantilla global" pasa a **🗑 Borrar plantilla** (confirm explícito: sus N equipos quedan sin actividades) y aparece **⧉ Copiar de otro cliente**; el **backfill ya no copia globales** (solo crea las fichas de cliente faltantes). **Council (Codex) RECHAZADO→APROBADO CON OBSERVACIONES (2 rondas):** [Alto] el tab **Global** de mantenimiento armaba su índice de equipos (`eqIdx`) **sin `cliente`** → con el resolver nuevo todos habrían salido en 0% ("sin actividades") aunque su plantilla existiera → se agrega `cliente` (**con `''`, NO con el `'RIPLEY'` que sugería Codex**: hardcodear un cliente por defecto es exactamente el fallback silencioso que este cambio elimina); [Bajo ×2] copy obsoleto en `clientes.html` que aún hablaba de "heredada / plantilla global"; [Bajo] **3 fallbacks legacy `cliente || 'RIPLEY'`** (mantenimiento, inventario, import de backup) — preexistentes pero contrarios al contrato nuevo: un equipo sin cliente habría resuelto en silencio la plantilla **de otro cliente** → ahora se preserva `''`, y el **alta de equipos exige que la sede tenga cliente** (la única sin él es la OFICINA, sede interna con 0 equipos — bloquearla es lo correcto). Validado contra el Firestore REAL con un equipo temporal de un cliente ficticio sin plantilla (`migrar_db/test_plantilla_por_cliente.mjs`, **7/7**, se borra al final): cliente sin plantilla → **0 actividades** (ya no hereda la global) + el bot lo dice y manda a Clientes; RIPLEY sigue resolviendo sus 5 actividades de CHILLER; "Mantenimiento integral" está en la plantilla y "Realizado" ya no. Regresión verde: `test_mtto` 77/77 · `test_resolver_typos` 42/42 · `test_gemini_no_desempata` 11/11 · `test_obs_v2` 15/15 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8 · `test_avisos` 11/11 (`test_fase4`/`test_fase5` siguen con su 1 fallo **preexistente** de Operatividad Etapa 1). `node --check` OK (5 HTML + el bot). **NO toca `firestore.rules`** (el `match /tareas_config/{doc}` ya cubre cualquier doc-id). **⚠️ PASO FINAL PENDIENTE:** las **14 plantillas globales siguen vivas pero INERTES** (el código nuevo las ignora; el viejo nunca las alcanza, porque todos los `{cliente|tipo}` existen y ganan por precedencia). Se borran **recién tras el merge a main** con `migrar_db/etapa1_borrar_globales.js` (guarda que aborta si algún equipo quedaría huérfano) — **antes no**, porque el `clientes.html` de producción todavía tiene el botón "↩︎ Usar plantilla global", que sin global de destino dejaría al cliente sin ninguna plantilla. **En develop.** ⚠️ **Adrián: `git pull` — cambiaron `mantenimiento_multiaire.html`, `itinerario.html`, `inventario_multiaire.html`, `configuracion.html`, `clientes.html` y `api/_lib/mtto.js`. Las actividades ya NO se editan en Configuración: la plantilla se edita en Clientes y el ajuste por equipo en Mantenimiento.** |
| 2026-07-13 | **EN PRODUCCIÓN 🚀: la plantilla de actividades vive SOLO en el cliente + las 14 plantillas globales BORRADAS** (merge develop→main `9ecb53c`, `--no-ff`, orden explícita de José; deploy success, verificado en `app.multiaire.com.pe` y `multiaire-peru-app.vercel.app`). Cierra las Etapas 0 y 1. **Orden de la operación (importante):** primero el **merge a main** (para que el `clientes.html` de producción deje de tener el botón *"↩︎ Usar plantilla global"* — que sin una global de destino habría dejado al cliente sin ninguna plantilla), después el **borrado de datos**. Verificado en producción ANTES de borrar: `heredarGlobal` ausente, `plantillaDeEq` presente, editor global de `configuracion.html` retirado, backup de `tareas_config` intacto. **Borrado ejecutado** (`migrar_db/etapa1_borrar_globales.js --confirmar`, con respaldo `_backup_globales_*.json` y una guarda que aborta si algún equipo quedaría sin plantilla): **14 plantillas globales eliminadas**. **Estado final de `tareas_config`: 14 docs, TODOS por cliente** (`RIPLEY|{tipo}`), **0 globales**, **0 fantasma**; los **557 equipos** tienen la plantilla de su cliente; `"Realizado"` no aparece en ninguna plantilla y `"Mantenimiento integral"` sí. **Regresión contra el Firestore ya SIN globales** (la prueba que de verdad importa: si algo dependía en secreto del fallback, acá se rompía): `test_plantilla_por_cliente` **7/7** · `test_mtto` **77/77** · `test_resolver_typos` 42/42 · `test_gemini_no_desempata` 11/11 · `test_obs_v2` 15/15 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8 · `test_avisos` 11/11 — **todo verde**. ⚠️ **Adrián: `git pull` en main y develop.** Las actividades de mtto ya **no** se editan en Configuración: la **plantilla** se edita en **Clientes** (viene de la licitación de cada cliente) y el **ajuste de un equipo puntual** en **Mantenimiento → ⚙️ Ajustar actividades por equipo**. |
| 2026-07-14 | **Las actividades se identifican por NOMBRE, no por posición** (`mantenimiento_multiaire.html` + `itinerario.html` + `api/_lib/mtto.js` + `api/_lib/itinerario_envio.js` + `configuracion.html` + `clientes.html` + `firestore.rules`; rama `feature/actividades-por-periodo`). **El problema de fondo:** el estado del checklist se guardaba por **ÍNDICE** — `mantenimiento/{SEDE\|PERIODO\|AÑO}.tareaStatus[eq] = {0:true, 2:true}`, doc-ids `EQ\|PERIODO\|AÑO\|IDX` en `mtto_plan`/`mtto_ejecuciones`, y `mantenimiento_fotos.tareaIdx`. Quitar una actividad de la lista **corre los índices de todas las siguientes**: la marca que decía "hice la #2" pasa a apuntar a otra actividad. Producción **ya permitía quitar actividades** (13 usos de `quitadas`) y **no remapeaba nada** → hoy, quitar una actividad mal-apunta en silencio cada marca posterior, para siempre. Y hay **cinco escritores** de ese estado (checklist, autosave, bot de WhatsApp, itinerario, registrador de KPIs), así que cada uno era una carrera distinta. **La decisión (José):** migrar a NOMBRE ahora, en esta rama, en vez de seguir parchando. **El modelo nuevo:** `tareaStatus{'Mantenimiento integral':true}`, doc-ids `EQ\|PERIODO\|AÑO\|<actKey(nombre)>`, fotos por el campo `tarea`. `actKey` escapa `%`, `\|` y `/` de forma **INYECTIVA** (`%` primero) — "Lavado a/c" y "Lavado a-c" son actividades distintas y no pueden colapsar en el mismo doc. Esto **borra 418 líneas de maquinaria de remapeo** y hace las 5 carreras estructuralmente imposibles: si la marca está pegada al nombre, cambiar la lista no la mueve. Además una actividad quitada **conserva su marca** (inerte), así que volver a agregarla la recupera. **Lectura retrocompatible:** `tareaHecha(st, tarea, i)` acepta la marca por nombre **y** la vieja por índice → el código nuevo funciona con los datos viejos desde el minuto cero. La app **NO reinterpreta** un índice viejo por su posición de hoy (eso cristalizaría la corrupción); esa conversión la hace **una sola vez** el script de migración, con evidencia. **Ajuste de actividades por PERÍODO** (la feature que destapó todo): nueva colección **`mtto_actividades_periodo`** (doc-id `SEDE\|PERIODO\|AÑO\|TIPO`) — "este bimestre, a las cortinas de Atocongo hay que hacerles además X"; vence con el período. El resolver queda en 3 capas: plantilla del cliente → ajuste del período → ajuste del equipo, cada una puede QUITAR y AGREGAR. Editor en Mantenimiento (⚙️ Ajustar actividades), regla en `firestore.rules` y backup en `configuracion.html`. **Council (Codex): 13 rondas.** Cada ronda encontró bugs reales; dos de ellos **ya estaban en producción** desde antes de esta rama (el autosave y el backup). Encontró, en este orden: [Alto] el bot no hallaba un `mtto_plan` legacy (id por índice) → registraba sin atribución y dejaba el plan fantasma; [Alto] el script de migración podía **pisar** un doc destino ya existente; [Alto] migrar `tareaStatus` por POSICIÓN cristalizaba la corrupción si la lista ya se había acortado → ahora resuelve con **EVIDENCIA** (`mtto_ejecuciones`/`mantenimiento_fotos` guardan `tareaIdx` **Y** `tarea`: dicen qué nombre tenía esa posición **en el momento** del registro); [Alto] `actKey` era *lossy* (`\|`,`/`→`-`) → colisión entre actividades distintas; [Alto] `normalizarStatus()` de la app reintroducía la conversión por posición → **eliminada**; [Alto] `registrarEjecucion` atribuía desde el caché `planDocs` (stale/vacío) → ahora lee `mtto_plan` **fresco por CAMPOS** (encuentra también los legacy) y si la lectura falla **no escribe nada** (el plan sobrevive y el barrido del itinerario lo convierte con su técnico); [Alto] tras el `await`, la función seguía leyendo globals (`currentSede`, `tareaStatus`) que cambian si el usuario navega → **todo el contexto se congela antes del primer await**, y la guarda anti-carrera valida contra la **referencia** al mapa donde se marcó (no contra el global, que al navegar habla de otra sede); [Alto] **el autosave tenía el mismo bug, y era PREEXISTENTE en producción**: `doSave` (debounce 800ms) leía los globals al disparar → marcar y cambiar de sede dentro de esa ventana escribía el mapa de la sede nueva bajo la clave de la vieja, y un segundo marcado **cancelaba** el guardado pendiente de la primera → **la marca del técnico se perdía**. Ahora `scheduleSave` congela el contexto y, si hay un pendiente de otra sede, lo **vacía** en vez de cancelarlo. **Duplicados de nombre**, cortados en el origen: `clientes.html` los rechaza al guardar la plantilla, el import del backup los descarta, `actsGuardar` los valida y el bot se niega a registrar (`ambiguo`). [Alto] **el autosave escribía el MAPA ENTERO desde memoria** — otro bug PREEXISTENTE de producción, y el peor: el técnico reporta una actividad por WhatsApp, el bot la marca en Firestore, y el autosave de la app (que tenía en memoria el mapa de antes) la **pisa sin error ni rastro**. Ahora `doSave` corre en una **transacción**: relee los mapas frescos y reemplaza **solo los equipos que el usuario tocó** → la marca del bot sobrevive. [Alto] **destildar no borraba la ejecución LEGACY** (doc-id terminado en índice) → el script de migración la habría resucitado como un KPI fantasma: un mantenimiento que el usuario deshizo, contado igual. Ahora el destildado borra por doc-id **y** por query de campos. [Alto, ronda 8] **el backup de `mantenimiento` estaba roto desde siempre** (`configuracion.html`, preexistente): el export leía `r.tarea_status`, un campo **que no existe** (el real es `tareaStatus`) → **todo respaldo salía con el checklist vacío**, y el import escribía el texto JSON crudo en vez de parsearlo. Esto importa justo ahora porque el plan de cutover dice *"respaldá antes de migrar"*: la red de seguridad no existía, y restaurar ese backup **borraba el checklist de todas las sedes**. Arreglado en las dos puntas, con round-trip verificado. [Alto, ronda 9] **el script de migración tenía la misma carrera que el autosave**: escribía el `tareaStatus` calculado desde el snapshot inicial, así que una marca que el bot escribiera *mientras el script pensaba* se perdía. Y al ir a arreglarlo apareció algo peor: **`set(..., {merge:true})` hace merge PROFUNDO de los mapas anidados** — escribir `{EQ:{'Nombre':true}}` sobre `{EQ:{0:true}}` deja **`{0:true, 'Nombre':true}`**: las claves numéricas **sobrevivían**. La migración no habría migrado nada, solo habría apilado nombres encima, dejando viva la mina que vino a desactivar (el día que la lista cambiara, ese `0` volvía a mal-apuntar). Verificado contra Firestore real, no asumido. Ahora `mantenimiento` se migra **doc por doc en transacción**: relee fresco (las marcas del bot se copian tal cual), recalcula sobre ese dato y escribe con **`update()`**, que sí reemplaza el mapa y deja intactos `seleccionados`/`clave`. [Alto, ronda 10] **el mismo merge profundo estaba en el autosave — y ahí lo había metido YO** al hacer `doSave` transaccional: producción escribía con `setDoc` **sin** merge (pisaba el doc entero, que era justamente el bug del bot), y al arreglarlo le puse `{merge:true}`. Con merge profundo, **destildar una actividad no se habría persistido**: el usuario destilda, ve la casilla vacía, recarga y vuelve tildada — y esa marca zombi es justo la que la migración habría convertido a nombre, dejando registrado un mantenimiento que se deshizo. `doSave` pasa a `tx.update()` (y `tx.set()` solo si el doc no existe). Se borra además la función muerta `saveNow()`, que arrastraba el patrón viejo de escritura completa. [Medio, ronda 11] **destildar una actividad de un equipo en "solo marcar" apagaba TODAS las demás**: `tareaStatus[eq] === true` significa "el equipo entero está hecho" (todas sus actividades se ven tildadas), y `toggleTarea` lo colapsaba a `{}` antes de aplicar el toggle → el usuario destildaba una y perdía el resto, sin aviso. Ahora el `true` se **expande** a los nombres explícitos de las actividades vigentes y recién ahí se aplica el toggle. [Medio, ronda 12] **el script podía reasignar una foto a otra actividad**: al realinear `mantenimiento_fotos` caía a `acts[tareaIdx]` (la posición de HOY) — el mismo pecado original, esta vez contra las fotos. Pero la foto **ya trae su evidencia**: su campo `tarea` es cómo se llamaba la actividad cuando se sacó. Ahora la posición de hoy **no decide nunca**: si el nombre sigue vigente no se toca; si quedó huérfano se corrige **solo** con evidencia dura (la ejecución de ese índice, o un renombre documentado — el mapa explícito `Realizado → Mantenimiento integral` del 2026-07-13); y si no hay ninguna de las dos, se **reporta y se deja quieta**. Una foto invisible se recupera a mano; una foto mal atribuida miente para siempre. *(En el simulacro real las 2 fotos desfasadas se resuelven por evidencia de ejecución y quedan **0 huérfanas**.)* [Alto, ronda 13] **`mtto_plan`/`mtto_ejecuciones` se movían con `batch.set()` desde el snapshot**, sin releer: si el técnico ejecutaba esa actividad mientras el script corría (la app borra el plan al registrar), el script **resucitaba el plan como fantasma** con el id nuevo — y le reaparecía en el itinerario. Ahora también se mueven **doc por doc en transacción**: si el origen ya no está, no se resucita; si el destino ya existe, gana el que tenga atribución de técnico (evaluado sobre el dato **fresco**, no sobre la foto vieja); y a un doc ya canónico se le quita el `tareaIdx` con un `update` **quirúrgico**, que no pisa lo que el bot le haya cambiado en el medio. **Tests** (todos extraen la función REAL del HTML/bot/script y corren contra un Firestore falso con semántica de transacción real —incluido el merge profundo—, o contra el Firestore REAL; cada fix se verificó **reinyectando el bug** y confirmando que el test se pone rojo): `test_atribucion` 23/23 · `test_autosave_ctx` 17/17 · `test_backup_mantenimiento` 11/11 · `test_migracion_carrera` 20/20 · `test_acts_guardar` 23/23 · `test_toggle_legacy` 15/15 · `test_bot_lista_cambiada` 18/18 · `test_actividades_periodo` 14/14. *(Un apunte de método, porque costó caro dos veces: **los dos bugs de merge profundo pasaron sus tests en verde** hasta que los Firestore falsos se volvieron fieles. `test_migracion_carrera` daba 12/12 **con el bug puesto** porque el fake devolvía `data()` perezoso —releía el store, así que la "lectura vieja" veía igual lo que el bot escribió después—; y `test_autosave_ctx` no veía el `set(merge)` porque su fake mergeaba **plano**, o sea se portaba como el `update()` correcto. Un fake que no modela la semántica real del motor no prueba nada: convierte el test en decoración. Reinyectar el bug y exigir que el test se ponga **rojo** es lo único que distingue una cosa de la otra — es lo que se hizo con cada fix de esta rama.)* Regresión del bot verde: `test_mtto` 81/81 · `test_plantilla_por_cliente` 7/7 · `test_resolver_typos` 42/42 · `test_gemini_no_desempata` 11/11 · `test_obs_v2` 15/15 · `test_avisos` 11/11 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8. **⚠️ CUTOVER (ver sección "Migración índice→nombre" más abajo): el script de datos se corre JUSTO DESPUÉS del merge a main, no antes** — Firestore es uno solo y producción todavía lee por índice. |
| 2026-07-14 | **Fix: el shim `setDoc` descartaba el `{merge:true}` → cambiar de rol a un usuario le BORRABA sus `apps[]`** (`configuracion.html`, `inventario_multiaire.html`, `itinerario.html`, `mantenimiento_multiaire.html`, `asistencia_multiaire.html`, `insumos.html`; rama `fix/shim-setdoc-merge`). Bug **preexistente en producción**, encontrado por el Council auditando otra rama. El shim de compatibilidad definía `setDoc: (ref, data) => ref.set(data)` — **descarta el 3er argumento**. Los paneles de "Gestión de accesos" llaman `setDoc(doc(db,'usuarios',email), {email, rol}, {merge:true})`: al ignorarse el merge, el `.set()` **reemplaza el documento entero**, así que `changeUserRol` y `addUser` **borraban el array `apps[]`**. Un **SUPERVISOR** (cuyo acceso depende íntegramente de `apps[]`, tanto en `auth-guard.js` como en `firestore.rules`) quedaba **sin acceso a NINGÚN módulo** — y cambiarle el rol a alguien es una operación normal, no un caso raro. **Fix:** `setDoc: (ref, data, opts) => opts ? ref.set(data, opts) : ref.set(data)` en los **5** shims (los 4 con llamadas + `asistencia`, que lo tenía definido pero muerto — se arregla igual para que el próximo que lo use no caiga en la trampa). Las llamadas **sin** `opts` conservan el overwrite completo: el fix agrega soporte para las opciones, no convierte todo en merge. **Dos variantes del MISMO daño, fuera del shim, encontradas al auditar todas las escrituras de `usuarios`:** (1) **`insumos.addUser`** hacía `.set({email,rol})` **sin merge y sin validar duplicado** → agregar un email que ya existía le borraba las `apps[]` (los otros paneles —personal/comprobantes/proveedores/observaciones— ya usaban `{merge:true}`); (2) **`inventario.gasPost`** (acción *update*) hacía `setDoc(ref, equipo)` sin merge con 9 campos fijos, pero el doc de `inventario` **también** guarda el estado vivo de operatividad (`operatividad`/`operatividadFecha`/`operatividadPor`/`operatividadOrigen`, que escriben `operatividad.html` y el bot) → **editar la ficha de un equipo le borraba su % de operatividad**; ahora va con `{merge:true}` (los 9 campos son los únicos que ese módulo posee y se reescriben siempre, así que ninguno queda obsoleto). **Test** (`migrar_db/test_shim_setdoc.mjs`): **no reimplementa nada** — extrae del HTML el shim y las funciones REALES (`addUserAccess`/`changeUserRol`/`addUser`/`gasPost`) y las corre contra un Firestore falso con la semántica fiel del motor (`.set(data)` reemplaza el doc entero; `.set(data,{merge:true})` mergea en profundidad los mapas y reemplaza arrays/escalares). Corre en **2 fases**: VERDE con el código real (**19/19**) y **ROJO reinyectando el bug** en el fuente — los **16** casos críticos **deben fallar**; si pasaran con el bug puesto, el runner **aborta** avisando que el test no prueba nada — y además exige que el rojo venga de una **aserción** y no de un `SyntaxError` (una mutación mal hecha rompe el código y simula detectar el bug sin detectarlo: pasó de verdad durante esta rama y el harness lo cazó) (la lección de las dos rondas de merge profundo del 2026-07-14: un fake infiel convierte el test en decoración). La guarda "`setDoc` sin `opts` sigue reemplazando el doc entero" pasa en **ambas** fases → prueba que el fix no cambió el comportamiento existente. `node --check` OK (6 HTML). **Council (Codex) APROBADO CON OBSERVACIONES → 2 hallazgos cerrados:** [Medio] `insumos.addUser` no validaba duplicado, así que "agregar" un email existente actualizaba su rol y además metía una **fila repetida** en la lista hasta recargar. [Ronda 2, Bajo] con `{merge:true}` ya no borraba `apps[]`, pero con la lista desactualizada **todavía podía cambiarle el rol** a un usuario existente — no era un *create* de verdad. Cerrado: `addUser` pasa también a **create-if-absent en transacción** (agregar es CREAR; el rol se cambia desde el selector de la fila, no re-agregando). [Bajo] los **`maAddUser` legacy** (que **están vivos**: el botón "+ Agregar" del panel viejo, en los **5** archivos) escribían `.set({email,rol,apps:[]})` sin merge, protegidos **solo** por la lista en memoria — si esa lista estaba desactualizada (otro admin creó al usuario en paralelo) **pisaban `apps[]`**; el merge no los salvaría porque su `apps:[]` es explícito, así que se pasaron a **create-if-absent en transacción** (`runTransaction`: si el doc ya existe, aborta con "Este usuario ya existe" y **no escribe nada**). El shim `query: (col,...args) => col` (marcado *simplified*) **descarta los filtros**, pero hoy **no tiene ningún uso** en el repo. **En develop, sin merge a main.** |
| 2026-07-14 | **Fix: las 🔔 Alertas del bot de `personal.html` no se podían activar — el toggle escribía en `window.__db`, que en ese módulo es `undefined`** (rama `fix/alertas-personal-db`; reportado en vivo por José: *"no puedo activarle alertas a otras personas"*). En el tab **🔔 Alertas del bot** (matriz colaborador × obs/asistencia/mtto), `toggleAvisoTipo` guardaba con `await window.__db.collection('maestros_personal')…` — pero **`personal.html` NUNCA define `window.__db`**: define `window.__fb` (solo helpers de auth) y su Firestore es la const **`db`** (línea 709). El shim `window.__db` solo existe en los 5 módulos viejos (`configuracion`, `inventario`, `itinerario`, `mantenimiento`, `asistencia`). Resultado: la escritura lanzaba **`TypeError`**, el `catch` mostraba un toast de error y llamaba `renderAlertas()` → **el checkbox volvía solo a su estado anterior y no se guardaba nada**. Los únicos que recibían avisos eran los que ya tenían el **`recibeAvisos` legacy** en el dato; **activar a alguien nuevo era imposible desde la UI** (los avisos de asistencia y mtto del bot, agregados el 2026-07-05, nunca se pudieron asignar). **NO era un problema de permisos** (`firestore.rules`: `maestros_personal` `update: isAdmin()`, y José es SUPER_ADMIN) ni del contrato con el bot (`avisos.js` `tieneAviso(p,tipo)` lee `p.avisos[tipo]` con fallback a `recibeAvisos` para `obs` — exactamente lo que el toggle escribe). **Fix de una palabra:** `window.__db` → **`db`**. Era el **único** uso de `window.__db` en todo el archivo (verificado: de los 10 módulos, `personal.html` era el único que lo usaba sin definirlo). **Test** `migrar_db/test_alertas_personal.mjs`: extrae del HTML las funciones **reales** (`toggleAvisoTipo`/`avisoDe`) y las corre contra un Firestore falso con merge fiel — **3/3** en verde (persiste el aviso + sincroniza el `recibeAvisos` legacy + no pisa los otros tipos ni el resto del expediente), y **reinyectando el bug los 3 se ponen ROJOS**. **Council (Codex) APROBADO CON OBSERVACIONES** (1 Bajo, aplicado: mi comentario decía "no define el shim `__fb`" cuando en realidad sí lo define — lo que no define es `window.__db`). `node --check` OK. **En develop, sin merge a main.** |
| 2026-07-14 | **🚀 CUTOVER COMPLETO — actividades por NOMBRE EN PRODUCCIÓN + 3 fixes** (merge develop→main `f08ae1c`, `--no-ff`, orden explícita de José; reglas desplegadas; datos migrados). **Orden ejecutado (el que documenta la sección "Migración índice→nombre"):** ① **`firestore.rules` desplegadas PRIMERO** (ruleset `23b70189`) — el Council de integración marcó [Alto] que `mantenimiento_multiaire.html` e `itinerario.html` **leen** `mtto_actividades_periodo`, así que sin esa regla el catch-all fail-closed habría denegado la lectura y **Mantenimiento caía a "sin actividades" e Itinerario no resolvía pendientes**: desplegarla antes elimina la ventana rota (verificado que el único delta vs las reglas vivas eran esas 4 líneas → estrictamente aditivo; respaldo del ruleset previo en `/tmp/firestore.rules.live-*`). ② **Merge a main** → producción pasa a **leer por nombre con fallback a índice** (nada se rompe aunque el script tarde). Deploy **success**, verificado HTTP 200 + marcadores (`tareaHecha`, shim arreglado, fix de alertas) en `app.multiaire.com.pe` y `multiaire-peru-app.vercel.app`. ③ **`node migrar_indices_a_nombres.js --confirmar`** (respaldo JSON en `migrar_db/_backup_indices_2026-07-14-13-45-04.json`). **Resultado de la migración:** `mantenimiento` 14 docs · `mtto_plan` **28** re-llaveados (0 a limpiar) · `mtto_ejecuciones` **116** re-llaveados (0 a limpiar) · `mantenimiento_fotos` **53** sin `tareaIdx`, con **2 realineadas por evidencia de ejecución** ("Realizado"→"Mantenimiento integral") y **0 huérfanas**. **La verificación que importaba:** el simulacro reportó **0 índices CORRIDOS** — en las **124** marcas con evidencia, la evidencia **nunca** contradice a la posición de hoy → las listas **no se movieron** desde que se marcaron, así que resolver las **703 marcas sin evidencia** por posición era correcto y no una apuesta (era el supuesto peligroso que advertía esta misma sección). Post-migración contra el Firestore real: **827 marcas hechas (`true`) por NOMBRE · 0 marcas `true` por índice** (ninguna quedó sin migrar) · doc-ids de `mtto_plan`/`mtto_ejecuciones` **0** por índice y **0** con campo `tareaIdx`. **Regresión verde contra el Firestore YA MIGRADO** (la prueba que de verdad cuenta): `test_mtto` 81/81 · `test_actividades_periodo` 14/14 · `test_atribucion` 23/23 · `test_toggle_legacy` 15/15 · `test_bot_lista_cambiada` 18/18 · `test_migracion_carrera` 20/20 · `test_acts_guardar` 23/23 · `test_autosave_ctx` 17/17 · `test_backup_mantenimiento` 11/11 · `test_plantilla_por_cliente` 7/7 · `test_asistencia` 56/56 · `test_obs_v2` 15/15 · `test_resolver_typos` 42/42 · `test_gemini_no_desempata` 11/11 · `test_avisos` 11/11. **El mismo merge subió a producción 3 fixes:** **#87** el shim `setDoc` descartaba `{merge:true}` (cambiar de rol **borraba las `apps[]`** → un SUPERVISOR sin acceso a ningún módulo; + `insumos.addUser`, los `maAddUser` legacy y `inventario.gasPost`, que al editar un equipo le **borraba su % de operatividad**); **#86** (Adrián) el bot confirmaba la **salida con la hora de entrada**; **#88** el toggle de **🔔 Alertas del bot** de `personal.html` escribía en `window.__db` (undefined ahí) → **nunca se pudo activar un aviso nuevo**, así que los avisos de **asistencia** y **mtto** (creados el 2026-07-05) jamás se le pudieron asignar a nadie. **Residuos conocidos (inocuos, NO los tocó la migración):** **11** marcas por índice con valor **`false`** (= "no hecho", el default → ruido inerte, `tareaHecha` solo cuenta `true`) y **2 docs con `tareaStatus` CORRUPTO preexistente** — `SANTA ANITA\|ENE-FEB\|2026` y `SANTA ANITA\|MAR-ABR\|2026` tienen el campo como el **string `"{}"`** en vez de un mapa (un guardado viejo que serializó mal; representa "sin marcas", no hay datos que perder). **Pendiente menor:** decidir si se limpian esos 2 docs. |
| 2026-07-14 | **Cutover — residuos limpiados** (`migrar_db/limpiar_residuos_indices.js`, simulacro por defecto + `--confirmar`, respaldo `_backup_residuos_2026-07-14-14-41-29.json`). Cierra los dos residuos que dejó la migración índice→nombre, ninguno con datos que perder. **(A)** **11 marcas por índice con valor `false`** en 9 equipos (5 docs de `mantenimiento`): la migración solo convirtió las marcas **`true`** (las que dicen "esto se hizo"), así que las `false` quedaron como **ruido inerte** (`false` = "no hecho", que es el valor por defecto; `tareaHecha` solo cuenta `=== true`) → borradas. **(B)** **2 docs con `tareaStatus` CORRUPTO PREEXISTENTE** (`SANTA ANITA\|ENE-FEB\|2026` y `SANTA ANITA\|MAR-ABR\|2026`): el campo era el **string `"{}"`** en vez de un mapa —un guardado viejo que lo serializó mal, **NO lo causó la migración**— → normalizados a `{}`. **Cómo se hizo (mismas lecciones de la migración):** cada doc se limpia **en TRANSACCIÓN** (relee fresco → filtra sobre ESE dato → escribe), porque el Firestore es el vivo y el bot puede estar marcando; y se escribe con **`update()`, NO `set(merge)`** — el merge de Firestore es **profundo** en los mapas anidados y dejaría las claves numéricas viejas conviviendo con las nuevas (no limpiaría nada). **Guarda dura:** el script **aborta** si encuentra alguna marca por índice con valor `true` (un mantenimiento realmente hecho) — solo limpia `false`. **Verificado post-limpieza contra Firestore real:** las **827** marcas por NOMBRE **intactas** · **0** marcas por índice (ni `true` ni `false`) · **0** docs corruptos · **0** docs que hayan perdido `seleccionados`/`clave`. Regresión verde (`test_mtto` 81/81 · `test_actividades_periodo` 14/14 · `test_toggle_legacy` 15/15 · `test_migracion_carrera` 20/20 · `test_autosave_ctx` 17/17 · `test_plantilla_por_cliente` 7/7). |
| 2026-07-14 | **Fix: el bot registraba el marcaje en la sede del PLAN aunque el técnico estuviera a 20 km — la UBICACIÓN REAL pasa a mandar** (`api/_lib/asistencia.js` + `api/_lib/geo.js`; rama `fix/asistencia-sede-por-ubicacion`). Reportado en vivo por José al revisar el marcaje de **Enrique Yaranga**. **El bug (estaba en producción):** con plan del día, `resolverSedePorUbicacion` elegía `sedeMasCercana(punto, sedesDelPlan)` y la aceptaba **sin mirar si el punto caía dentro de su radio** → con un plan de UNA sola sede, **cualquier coordenada del planeta caía en ella**. Enrique compartió una ubicación a **420 m de JOCKEY PLAZA** (dentro de su radio de 1000 m) y quedó registrado en **COMAS, a 19 775 m**, porque su itinerario de ese día —**un itinerario de PRUEBA que no se borró**— decía COMAS. La asimetría delataba el bug: el camino **sin** plan sí exigía `mejor.dentro`, o sea el bot **acertaba cuando NO había plan y fallaba cuando SÍ lo había** (el día anterior, sin plan, lo ubicó bien en Jockey a 133 m). **No era el GPS ni "mandar la ubicación muy rápido": la coordenada era correcta y esperar no habría cambiado nada** (la pregunta original de José). **La regla nueva:** ① dentro del radio de una sede de su plan → esa; ② si no, pero está parado dentro de **otra** sede real → esa, con bandera **fuera de plan** (la evidencia debe decir dónde estuvo **de verdad**); ③ **fuera del radio de TODAS → NO se asume ninguna: se pregunta** (fase `ELIGE_SEDE`). Con o sin plan, **la misma regla**. Nuevo helper puro **`sedeQueContiene(punto, sedes)`** en `geo.js` — **no basta con filtrar `sedeMasCercana` por `dentro`**: cada sede tiene su propio radio, así que la más cercana puede quedar fuera del suyo mientras otra más lejana sí contiene al punto. **Council (Codex): 5 rondas, RECHAZADO ×4 → APROBADO.** Encontró, en orden: [Alto] un **bypass mío** que aceptaba la sede del plan cuando no tenía coordenadas (yo lo justifiqué como *"ausencia de dato, no discrepancia"* — es un agujero: un técnico a 30 km con una "zona de trabajo" escrita a mano quedaba marcado sin preguntarle y con `distancia:null`) → eliminado, y para que quitarlo no lo deje sin poder marcar, `sedesElegibles` **ofrece también las zonas libres de su plan** (si elige una, el registro sale con `geovalidada:false`, que es exactamente lo que corresponde); [Medio] `ELIGE_SEDE` **hardcodeaba `fueraDePlan = true`** → le colgaba una bandera falsa al técnico que estaba justo donde debía estar (solo que fuera del radio) → ahora se deriva de `estaEnLista(sede, plan)`; [Alto, ronda 2] **`resolverSedePorNombre` desempataba solo**, quedándose con la **primera del array** — responder *"norte"* calza igual con PLAZA NORTE y MAC PLAZA NORTE (las dos existen) y el bot elegía una: **escribía el marcaje en una sede donde el técnico no estaba**. Era **preexistente**, pero este fix lo amplifica (ahora se pregunta la sede mucho más seguido) → ante empate **NO se elige**: se ofrece una **lista NUMERADA** y responde el número (el match exacto sigue ganando al parcial, así que *"plaza norte"* resuelve directo y solo lo genuinamente ambiguo repregunta); [Medio] **`nombresDe()`**: UNA sola definición de "cómo se llama esta sede" (compartida por `mismaSede` y el matcher), que deriva el prefijo del campo **`cliente`** en vez de hardcodear `/^ripley/` —que se desactualizaría al entrar el segundo cliente—; [Alto ×2, ronda 3] dos bugs **que introduje yo con el flujo numerado**: al resolver la sede por una ubicación **nueva** estando en `ELIGE_SEDE` no se limpiaba la fase ni las opciones → un texto suelto posterior **pisaba la sede que el GPS ya había resuelto** con la lista vieja; y `listaNumerada` mostraba **15** opciones pero la sesión guardaba **todas** → responder *"16"* elegía **una sede que el técnico nunca vio** (ahora no se recorta: el índice tiene que corresponder **siempre** a una opción visible, y `sedesElegibles` pone las sedes de su plan **primero**); [Medio, ronda 4] el parser del número aceptaba solo `\d{1,2}`. **Un hallazgo [Alto] del Council se RECHAZÓ con evidencia** (y José lo ratificó): pedía **bloquear** el marcaje si la sede declarada queda fuera de su radio (*"nunca registrar una sede donde el técnico no está"*). Se implementó su fix tal cual y **crea un BUCLE**: se llega a preguntarle la sede **precisamente porque ninguna la contiene**, así que rechazar toda respuesta fuera de radio lo deja **sin poder marcar nunca** (0 marcajes) — y solo podría marcar eligiendo una sede **sin coordenadas**, o sea premiando la opción no verificable y castigando declarar la sede real. **La distinción que importa: el bug era que el BOT INVENTABA la sede; que el TÉCNICO la DECLARE y quede `dentroRadio:false` + la distancia real es la evidencia que lo delata, no un agujero** (ya era la decisión del 2026-07-01: *fuera de radio → registra con bandera, no rechaza*). Queda **blindado**: comentario en el punto exacto donde se endurecería + mutación `bloqueo` en el test, que se pone roja si alguien lo "arregla". **Test** `migrar_db/test_sede_por_ubicacion.mjs`: corre el flujo REAL (`manejarAsistencia`, deps inyectadas, sin tocar Firestore) con las **coordenadas reales** de las sedes y **la coordenada exacta que Enrique compartió** — **18/18 en verde**, y reinyecta **6 regresiones** por separado (`radio`, `bypass`, `fuzzy`, `fase`, `lista15`, `bloqueo`), **exigiendo que cada caso ponga ROJA la suya**; si una pasa con el bug puesto, el runner **aborta**, y también aborta si la mutación no encuentra su blanco (correr la fase roja sobre código sin mutar daría verde y no probaría nada). *(De paso, el harness cazó dos errores míos: un **fixture mal calibrado** —la sede sintética del caso "radios distintos" estaba a 23 m, o sea DENTRO de su radio de 50 m, así que el caso "pasaba" por el motivo equivocado; ahora **verifica sus propias precondiciones**— y un **caso mal planteado** —creí que "plaza norte" era ambiguo, pero es el nombre EXACTO de una sede y el exacto debe ganar—. Misma lección de las dos rondas de merge profundo: un fixture que no modela lo que creés no prueba nada.)* `test_asistencia` sube a **57/57**: el test *"ENTRADA fuera de radio → registra con la ubicación fuera"* codificaba la regla vieja → se reemplaza por dos (no asume ninguna + tras responder la sede sí registra con `dentro:false`). Regresión verde: `test_mtto` 81/81 · `test_obs_v2` 15/15 · `test_avisos` 11/11 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8. `node --check` OK. **No toca `firestore.rules`** (el bot escribe con Admin SDK). **En develop, sin merge a main.** |
| 2026-07-14 | **EN PRODUCCIÓN 🚀 el fix de sede por ubicación + corrección de los datos del incidente** (merge develop→main `665f2ef`, `--no-ff`, orden explícita de José; deploy success, verificado HTTP 200 en `app.multiaire.com.pe` y `multiaire-peru-app.vercel.app`). El bot ya resuelve la sede por **dónde está el técnico**, no por lo que dice su itinerario. **Se corrigieron además los dos datos que dejó el incidente, ambos con respaldo JSON y guardas duras:** **(1) el marcaje de Enrique** (`migrar_db/corregir_marcaje_enrique.js`): `AST-PER003-2026-07-14.marcajeEntrada` pasa de **COMAS · 19 775 m · `dentroRadio:false`** a **JOCKEY PLAZA · 420 m · `dentroRadio:true` · `fueraDePlan:true`** — la distancia se **recalcula con la misma haversine del bot** (no se inventa el número) y el script **aborta si la coordenada no cae dentro de Jockey** (la premisa de la corrección); las coordenadas, la hora y el resto del registro **no se tocan** (eran correctos: el GPS nunca falló). Escrito en **transacción** que relee fresco y **no pisa** si alguien ya lo corrigió a mano. **(2) el itinerario de prueba** (`migrar_db/borrar_itinerario_prueba.js`): el `ITI-20260714-001` (v1 y v2, RIPLEY COMAS) era una **prueba que quedó sin borrar** — y fue *la causa próxima* del registro errado: sin ese plan basura, el bot habría caído al camino "sin plan", que **sí verificaba el radio**, y lo habría ubicado bien. **La guarda del script frenó el borrado y obligó a preguntar**: el itinerario estaba **confirmado** y tenía **4 actividades de mtto asignadas a Rafael Santos** (Mantenimiento integral y Toma de parámetros de las cortinas de aire 03 y 04 de Comas) — el dato **contradecía** la descripción de "es solo una prueba". José confirmó que **todo** era prueba, así que se borraron los **2 itinerarios + 2 bloques + los 4 `mtto_plan`**. Borrar los `mtto_plan` era **imprescindible**: dejarlos huérfanos los habría mantenido contando como *"ya planificados"* → esas 4 actividades **no habrían vuelto a aparecer como pendientes en ningún otro día** (regla de `pendientesDeSede`). Verificado post-borrado contra el Firestore real: **0 itinerarios / 0 bloques / 0 `mtto_plan` del 2026-07-14**, las 4 actividades **vuelven a estar pendientes** (0 hechas, 0 planificadas) y el marcaje de Enrique dice **JOCKEY PLAZA, dentro del radio**. *(Lección de proceso: la guarda que aborta ante lo inesperado valió más que el script — el borrado "obvio" habría dejado 4 actividades de mantenimiento bloqueadas para siempre, en silencio.)* |
| 2026-07-14 | **Observaciones: el matcher manda sobre Gemini + el equipo se nombra y se elige por su UBICACIÓN** (`api/_lib/conversacion.js` + `equipos.js` + `mtto.js` + `operatividad.js`; rama `feature/obs-matcher-ubicacion`; pedido de José: *"cuando se identifique el equipo, o cada que se lo mencione, mostrale su ubicación — ellos no dicen 'extractor 04', dicen extractor 'ubicación'"*). **(1) EL ORDEN — deuda, no bug nuevo.** El flujo de **mtto** se arregló el 2026-07-13 (el matcher determinístico resuelve PRIMERO, Gemini es el rescate) pero **observaciones quedó con el orden viejo**: Gemini corría siempre y su `sede` entraba al resolver como dato canónico. Ante un texto genuinamente ambiguo (*"m plaza norte"* = ¿PLAZA NORTE o MAC PLAZA NORTE? — las dos existen) Gemini contesta una **con total aplomo** → la observación quedaba registrada **contra la sede equivocada**. Nueva `resolverSedeEquipo`: ① matcher con el texto crudo; ② `sedeAmbigua` → **se repregunta** (Gemini no desempata); ③ solo si el matcher no pudo entra el **rescate** de Gemini, con la sede **CONGELADA** (no puede mudar la observación a otra tienda) y **aceptado solo si AVANZA**. **"Avanzar" incluye no ENSANCHAR la lista** — y ese fue un bug que me encontró el propio test: el técnico responde la ubicación (*"ingreso tienda 2do nivel"*), el matcher ya acotó a las **9** cortinas de ese ingreso, pero Gemini extrae el equipo en genérico (*"cortina de aire"*) y su "corrección" **devolvía las 27**, repitiéndole la misma lista que acababa de contestar: el bucle, otra vez, en su variante de equipo. **(2) LA UBICACIÓN, cada vez que se nombra el equipo.** Helper compartido **`etiquetaEquipo(eq)`** (`Extractor 04 · 📍 comedor`) aplicado a **todos** los puntos donde el bot nombra un equipo: confirmación, acuse de guardado, pregunta y acuse de **operatividad** (`menuOperatividad(equipo)` — su última chance de notar que se registró contra el equipo equivocado), etiqueta de la última obs, y en **mtto**: candidatos, `pedirActividades`, resumen, aviso al supervisor y cierre de fotos (en los params de la plantilla de Meta **sin emoji** — un carácter raro puede hacer que Meta rechace el envío). **(3) LA LISTA DE CANDIDATOS, que es donde el técnico ELIGE.** Con >15 equipos el bot pedía *"¿de qué **tipo** y número?"* — le pedía el tipo **que él acababa de decir** y un número que **no usa** (y que ni siquiera coincide con el del código: el `eq_id` numera por orden de carga del Excel). Nuevo helper compartido **`opcionesEquipo(cands)`** (uno solo, para que observaciones y mtto **no diverjan**), con los umbrales sacados del **inventario real**: el grupo (sede, tipo) mayor son **28** equipos → con un tope de **30** caben todos, así que dicho el tipo **nunca hace falta acotar más**. Y cómo se presentan depende de **cuánto discrimina el área**: los 18 extractores de ATOCONGO tienen casi una ubicación cada uno (14 áreas) → **una línea por equipo con su sitio al lado**; las 27 cortinas comparten **3 ingresos** (9 por área) → se **agrupan bajo el encabezado de su ubicación** y el técnico elige el número dentro del suyo (repetir el área 27 veces sería una pared de texto, y ofrecer "elegí un ingreso" no desambigua nada: seguirían quedando 9). Verificado en vivo: *"el del comedor"*, *"el de la subestación"*, *"sshh gerencia"* **resuelven directo**, y responder un ingreso **ACOTA** de 27 a 9. **Council (Codex): RECHAZADO → APROBADO (2 rondas), los 2 hallazgos cerrados.** [Alto] *"Gemini todavía puede ensanchar la lista de candidatos en MTTO"* — el fix de "no ensanchar" lo había aplicado **solo en observaciones**, y mtto (que comparte el resolver y, desde este cambio, la lista por ubicación) arrastraba el mismo bucle. La causa raíz no era el olvido: **la regla del rescate estaba DUPLICADA en los dos flujos**, que es exactamente cómo se arregla uno y el otro se queda roto (ya pasó: el fix del 2026-07-13 fue solo a mtto). Así que no se portó — se **extrajo** a un helper compartido **`elegirRescate(r, rg)`** (`equipos.js`) que ahora consumen los dos: **una sola regla, imposible que un flujo se arregle y el otro no.** [Medio] *"Gemini aún puede DESEMPATAR el equipo"* — si el matcher dejó **varios candidatos**, el texto era ambiguo de verdad (*"la cortina de aire"*, a secas) y una resolución única de Gemini (que **inventa el número**: "cortina de aire 4") entraba como buena → registro contra un equipo **que el técnico nunca nombró**. Ahora se rechaza y se repregunta, **mismo criterio que ya regía para la sede ambigua** (y repreguntar ahora es barato: la lista trae la ubicación). Las **3 reglas** de `elegirRescate`: la corrección de Gemini (1) no puede **mudar de sede**, (2) no puede **desempatar** entre equipos reales, (3) no puede **ensanchar** la lista. **Tests** — `migrar_db/test_obs_matcher_primero.mjs` (nuevo): corre el código REAL (`manejarMensaje`) contra el **inventario real** inyectando un Gemini que devuelve justamente las corazonadas peligrosas — **18/18** — y **reinyecta 8 regresiones** (`gemini_manda`, `gemini_muda`, `gemini_desempata`, `gemini_ensancha`, `sin_ubicacion`, `no_matchea_area`, `tipo_y_numero`, `sin_agrupar`) exigiendo que **cada caso ponga ROJO al suyo**; si uno pasa con el bug puesto, el runner **aborta**. Y `test_gemini_no_desempata.mjs` (mtto) sube a **13/13** con 2 casos nuevos que cubren el hallazgo [Alto] **en su propio flujo** (verificado: reinyectando la regla vieja, mtto vuelve a las 27 cortinas —el bucle— y **resuelve** el equipo que Gemini inventó). *(El harness volvió a ganarse el sueldo dos veces: (a) reusar el mismo nombre de archivo mutado hacía que **Node sirviera la copia cacheada sin mutar** a partir de la 2ª mutación → 4 casos daban un verde falso; (b) el caso de "no mudar de sede" **no probaba nada**, porque mi propia regla de "no ensanchar" ya lo cubría — hubo que buscar el escenario donde la mudanza **parece un avance** (Gemini alucina una sede con MENOS equipos del tipo, así que su corrección *acorta* la lista mientras se lleva la observación a otra tienda); (c) y ese mismo caso seguía sin morir porque la sede va congelada en **DOS capas** (el caller ya le pasa la sede resuelta al resolver, y `elegirRescate` además rechaza un `rg` que se haya movido): con defensa en profundidad, **ninguna mutación individual mata el caso** —para eso está la segunda capa—, así que la mutación tiene que quitar las dos. Misma lección que las dos rondas de merge profundo: un test que pasa con el bug puesto es decoración.)* Regresión verde: `test_mtto` 81/81 · `test_obs_v2` 15/15 · `test_asistencia` 57/57 · `test_sede_por_ubicacion` 18/18 · `test_resolver_typos` 42/42 · `test_gemini_no_desempata` 11/11 · `test_avisos` 11/11 · `test_tier2` 9/9 · `test_tier3` 7/7 · `test_foto_ultima` 8/8 · `test_actividades_periodo` 14/14 · `test_atribucion` 23/23 · `test_toggle_legacy` 15/15 · `test_plantilla_por_cliente` 7/7. `node --check` OK. **No toca `firestore.rules`** (el bot escribe con Admin SDK). **En develop, sin merge a main.** **Dato sucio detectado (para José, NO lo arregla este cambio):** las **UMA de COMAS** tienen como "ubicación" un **código interno** (`UMA-P1-01`, `UMA-P2-03`) en vez de un sitio → ahí el técnico no va a poder decir "la del comedor"; y hay equipos con **nombre y área repetidos** (tres "Uma 02" en la misma área), indistinguibles salvo por código. |
| 2026-07-15 | **Asistencia: los colaboradores con régimen `NO_MARCA` (jefes/gerencia) dejan de aparecer en el módulo** (`asistencia_multiaire.html`, `loadColabs`; pedido del usuario: quiere dar de alta a los jefes en Personal —igual que a cualquier colaborador— para que el bot les avise por WhatsApp, pero sin que entren al control de asistencia). El campo **`regimenAsistencia`** (`personal.html`, agregado 2026-06-26: MARCA/FIJO/NO_MARCA) ya lo respetaba el bot geolocalizado (`api/_lib/asistencia.js`) pero la app web nunca lo miraba — `loadColabs` traía TODO `maestros_personal` filtrando solo por `activo` (+ `fechaIngreso`/`fechaCese` desde el PR #91), así que un jefe activo habría aparecido en el selector de registro y, peor, en **Ausencias** (que marca falta por cada colaborador activo × día laborable sin registro — un jefe habría salido con falta todos los días). Fix: `loadColabs` ahora lee también `regimenAsistencia` (default `MARCA` si el doc no lo tiene, retrocompatible) y excluye `NO_MARCA` de `allColabs` — como selector/Historial/Vista E/S/Resumen HE/Ausencias/manager de colaboradores comparten ese mismo array, quedan todos cubiertos con un solo filtro. `FIJO` (Andrés/Leslie/Marchena) no se toca — sigue apareciendo, solo que con horario fijo en vez de marcado manual. Sintaxis validada (`new Function` sobre los 8 bloques `<script>` del archivo). Sin cambios de `firestore.rules` ni del bot. Rama `feature/asistencia-excluir-no-marca`. |
| 2026-07-15 | **Fix (Council post-cutover): la vigencia (ingreso/cese) ahora se respeta AUNQUE exista un registro + borrado de asistencia admin-only real** (`asistencia_multiaire.html` + `auth-guard.js` + `firestore.rules`; rama `fix/asistencia-vigencia-council`). Tras subir a main los 3 PRs de asistencia de Adrián (#91/#92/#93), José pidió pasarles Council: Codex marcó 3 hallazgos, todos cerrados en 3 rondas (APROBADO CON OBSERVACIONES → APROBADO CON OBSERVACIONES → **APROBADO**). **[Alto] La vigencia solo se aplicaba `if(!reg)`** — un registro de **Falta (`F`)** o cualquier registro suelto que cayera **antes de `fechaIngreso` o después de `fechaCese`** seguía sumando **deuda / H.E. negativa** en Resumen y exportaciones (un `F` fuera de vigencia daba −8/−4.5). La causa: `fueraDeVigencia(c,f)` se consultaba **después** de encontrar el registro, así que solo saltaba las fechas sin registro. Arreglado **anteponiendo la vigencia en los 6 sitios** que consumen registros: vista **T/A** (`loadVistaTA` — antes un `F` fuera de vigencia se pintaba como falta "X"), vista **E/S** (`loadVistaES`), **Resumen** (se filtran los `cRegs` fuera de vigencia **y** el loop de ausencias hace `if(fueraDeVigencia(c,f))return` **antes** de buscar el `reg`), y los **dos** `celdasColab` de export (**PDF** y **Excel**). Regla uniforme en las 3 vistas y 2 exports: **fuera de vigencia = fecha que no cuenta para nada** (ni deuda ni H.E. ni celda), haya o no registro. `loadResumen`/lista de ausencias con `fueraDeVigencia` primero ya estaban bien y no se tocaron. **[Medio] `borrarRegistroAsistencia` seguía en `OPERATIVAS`** de `auth-guard.js`: hoy un SUPERVISOR quedaba bloqueado **solo** porque la pantalla llama `MA.can('borrarRegistroAsistencia')` **sin app** (doble gate con `firestore.rules`), pero llamarlo con app lo habría colado — inconsistente con `asistencia_registros → allow delete: if isAdmin()`. Ahora es **rama explícita admin-only** en `can()`: `if(accion==='borrarRegistroAsistencia') return isAdmin()` (ADMIN o SUPER_ADMIN, nunca SUPERVISOR/TECNICO, sin depender de `app`) y se sacó de `OPERATIVAS`; comentario de `firestore.rules` alineado. Validado por Codex: `SUPER_ADMIN=true · ADMIN=true · SUPERVISOR=false · TECNICO=false`. **[Bajo]** la vista E/S mostraba "Sin ingresar aún" también **después del cese** → ahora distingue **"Cesado"** (`f>fechaCese`) de **"Sin ingresar aún"**. Sin regresión: un registro real `A` con horas **dentro** de vigencia se sigue mostrando y contando igual; feriados/domingos/fechas futuras intactos. Sintaxis validada (`node --check` en `auth-guard.js` + `new Function` sobre los `<script>` de asistencia). **EN PRODUCCIÓN 🚀** (merge `fix/…` → develop `f2da0cb` → main `84335a3`, `--no-ff`, orden explícita de José; deploy success, HTTP 200 en `app.multiaire.com.pe` y `multiaire-peru-app.vercel.app`; **`firestore.rules` publicadas** vía `firebase deploy --only firestore:rules` — el comentario cambió pero la regla de borrado ya venía de antes). **Verificado en vivo** con sesión SUPER_ADMIN: `MA.can('borrarRegistroAsistencia')` = ADMIN✅ / SUPER_ADMIN✅ / SUPERVISOR+app❌ / TECNICO❌; botón `🗑 Eliminar` presente y `body.no-delete`=false. |
| 2026-07-15 | **Decisión: se DIFIERE el pendiente Bajo del #86 (validar `ses.tipo` al retomar la sesión de asistencia) — NO se toca el bot.** El Council del #86 (Adrián) dejó un hallazgo **Bajo**: `manejarAsistencia` (`api/_lib/asistencia.js`) no valida `ses.tipo` al retomar una sesión persistida, y `construirRegistro` (`asistencia_registros.js`) trata cualquier valor ≠ `'ENTRADA'` como **SALIDA** → una sesión con `tipo` corrupto/legacy registraría una salida equivocada. **Se evaluó y se decidió diferir** (José): el escenario **no puede ocurrir con el esquema actual** — `nuevaSesion()` **siempre** setea `tipo` a `ENTRADA`/`SALIDA`, el bot de asistencia **nació con ese campo** (no hay sesiones *legacy* sin `tipo` en `wa_asistencia_sesiones`) y las sesiones tienen **TTL 20 min**. Hallazgo no bloqueante, sin impacto vivo; el bot está estable y no se toca por un caso que hoy no se da. Se **prototipó** la guarda (`if(ses && ses.tipo!=='ENTRADA' && ses.tipo!=='SALIDA'){ limpiarSesion(); pedir empezar de nuevo }`) y **se revirtió** — no aportaba sobre el riesgo real. **Se retoma solo si** se cambia el esquema de la sesión o se agrega un tercer `tipo`. Registrado también en el *Tablero de equipo* del vault (Adrián: ya no es su pendiente). Sin cambios de código. |
